---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: aclrulev1s.cfw.opentelekomcloud.m.crossplane.io
spec:
  group: cfw.opentelekomcloud.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - opentelekomcloud
    kind: ACLRuleV1
    listKind: ACLRuleV1List
    plural: aclrulev1s
    singular: aclrulev1
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ACLRuleV1 is the Schema for the ACLRuleV1s API. Manages a CFW
          ACL rule resource within OpenTelekomCloud.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ACLRuleV1Spec defines the desired state of ACLRuleV1
            properties:
              forProvider:
                properties:
                  actionType:
                    description: 'Specifies the rule action: 0 (permit), 1 (deny).'
                    type: number
                  addressType:
                    description: 'Specifies the Internet protocol type of an address:
                      0 (IPv4), 1 (IPv6).'
                    type: number
                  applications:
                    description: 'Specifies the rule application list . Allowed list
                      values: HTTP, HTTPS, TLS1, DNS, SSH, MYSQL, SMTP, RDP, RDPS,
                      VNC, POP3, IMAP4, SMTPS, POP3S, FTPS, ANY, or BGP.'
                    items:
                      type: string
                    type: array
                  applicationsJsonString:
                    description: Specifies the JSON string converted from the applications
                      field in the application list.
                    type: string
                  description:
                    description: Specifies the description of the rule.
                    type: string
                  destination:
                    description: |-
                      Specifies the destination address Data Transport Object.
                      The destination structure is documented below.
                    items:
                      properties:
                        address:
                          description: Specifies the IP address information. It cannot
                            be left blank if type is set to 0.
                          type: string
                        addressGroup:
                          description: Specifies the Address group ID list. This parameter
                            cannot be left blank when type is set to 5 (multiple objects).
                          items:
                            type: string
                          type: array
                        addressSetId:
                          description: Specifies the ID of an associated IP address
                            group. This parameter cannot be left blank when type is
                            set to 1.
                          type: string
                        addressSetName:
                          description: Specifies the name of an associated IP address
                            group. This parameter cannot be left blank when type is
                            set to 1.
                          type: string
                        addressSetType:
                          description: Specifies the Address group type. It cannot
                            be left blank when type is set to 1 (associated IP address
                            group). It value can be 0 (user-defined address group),
                            1 (WAF back-to-source IP address group), 2 (DDoS back-to-source
                            IP address group), or 3 (NAT64 address group).
                          type: number
                        addressType:
                          description: 'Specifies theInternet protocol type of an
                            address: 0 (IPv4), 1 (IPv6). If type is 0, this parameter
                            cannot be left blank.'
                          type: number
                        domainAddressName:
                          description: Specifies the name of a domain name address.
                            This parameter is valid when type is set to 2 (domain
                            name) or 7 (application domain name group).
                          type: string
                        domainSetId:
                          description: Specifies the domain group ID. The value cannot
                            be left blank when type is set to 4 (domain name group)
                            or 7 (domain name group - application).
                          type: string
                        domainSetName:
                          description: Specifies the domain group name. The value
                            cannot be left blank when type is set to 4 (domain name
                            group) or 7 (domain name group - application).
                          type: string
                        ipAddress:
                          description: Specifies the IP address list. This parameter
                            cannot be left blank when type is set to 5 (multiple objects).
                          items:
                            type: string
                          type: array
                        predefinedGroup:
                          description: Specifies the pre-defined address group ID
                            list. This parameter cannot be left blank when type is
                            set to 5 (multiple objects).
                          items:
                            type: string
                          type: array
                        regionList:
                          description: Specifies the rule region list.
                          items:
                            properties:
                              regionId:
                                description: Specifies the region ID.
                                type: string
                              regionType:
                                description: 'Specifies the region type: 0 (country),
                                  1 (province), and 2 (continent).'
                                type: number
                            type: object
                          type: array
                        regionListJson:
                          description: Specifies the JSON value of the rule region
                            list.
                          type: string
                        type:
                          description: 'Rule type: 0 (Internet border rule), 1 (inter-VPC
                            rule), or 2 (NAT rule). When type is set to 0, the source
                            and destination addresses of the rule must be EIPs or
                            domain names of the public network. For an inter-VPC rule,
                            the source and destination addresses must be private IP
                            addresses. For a NAT rule, the source address must be
                            a private IP address, and the destination address must
                            be an EIP or domain name of the public network.'
                          type: number
                      type: object
                    type: array
                  direction:
                    description: 'Specifies the Direction: 0 (inbound) or 1 (outbound).
                      This parameter is mandatory when type is set to 0 (Internet
                      rule) or 2 (NAT rule).'
                    type: number
                  longConnectEnable:
                    description: 'Specifies whether to support persistent connections:
                      0 (no), 1 (yes).'
                    type: number
                  longConnectTime:
                    description: Specifies the persistent connection duration.
                    type: number
                  longConnectTimeHour:
                    description: Specifies the persistent connection duration (hour).
                    type: number
                  longConnectTimeMinute:
                    description: Specifies the persistent connection duration (minute).
                    type: number
                  longConnectTimeSecond:
                    description: Specifies the persistent connection duration (second).
                    type: number
                  name:
                    description: Specifies the CFW ACL rule name. The CFW ACL rule
                      name of the same type is unique in the same firewall instance.
                    type: string
                  objectId:
                    description: Protected object ID, which is used to distinguish
                      between Internet border protection and VPC border protection
                      after a cloud firewall is created. If the value of type is 0,
                      the protected object ID belongs to the Internet border. If the
                      value of type is 1, the protected object ID belongs to the VPC
                      border.
                    type: string
                  sequence:
                    description: Specifies the request body for changing the rule
                      sequence. The sequence structure is documented below.
                    items:
                      properties:
                        bottom:
                          description: 'Specifies whether to pin to bottom: 0 (no),
                            1 (yes).'
                          type: number
                        destRuleId:
                          description: Specifies the ID of the target rule.
                          type: string
                        top:
                          description: 'Specifies whether to pin on top: 0 (no), 1
                            (yes).'
                          type: number
                      type: object
                    type: array
                  service:
                    description: |-
                      Specifies the service object.
                      The service structure is documented below.
                    items:
                      properties:
                        customService:
                          description: Specifies the custom service.
                          items:
                            properties:
                              description:
                                description: Specifies the description of the rule.
                                type: string
                              destPort:
                                description: Specifies the destination port.
                                type: string
                              name:
                                description: Specifies the CFW ACL rule name. The
                                  CFW ACL rule name of the same type is unique in
                                  the same firewall instance.
                                type: string
                              protocol:
                                description: 'Specifies the protocol type: 6 (TCP),
                                  17 (UDP), 1 (ICMP), 58 (ICMPv6), or -1 (any). It
                                  cannot be left blank when type is set to 0 (manual).'
                                type: number
                              sourcePort:
                                description: Specifies the source port.
                                type: string
                            type: object
                          type: array
                        destPort:
                          description: Specifies the destination port.
                          type: string
                        predefinedGroup:
                          description: Specifies the pre-defined service group ID
                            list.
                          items:
                            type: string
                          type: array
                        protocol:
                          description: 'Specifies the protocol type: 6 (TCP), 17 (UDP),
                            1 (ICMP), 58 (ICMPv6), or -1 (any). It cannot be left
                            blank when type is set to 0 (manual).'
                          type: number
                        protocols:
                          description: 'Specifies the protocol list. Permitted list
                            values: 6 (TCP), 17 (UDP), 1 (ICMP), 58 (ICMPv6), or -1
                            (any). It cannot be left blank when type is set to 0 (manual).'
                          items:
                            type: number
                          type: array
                        serviceGroup:
                          description: Specifies the service group ID list.
                          items:
                            type: string
                          type: array
                        serviceGroupNames:
                          description: Specifies the service group name list.
                          items:
                            properties:
                              name:
                                description: Specifies the CFW ACL rule name. The
                                  CFW ACL rule name of the same type is unique in
                                  the same firewall instance.
                                type: string
                              protocols:
                                description: 'Specifies the protocol list. Permitted
                                  list values: 6 (TCP), 17 (UDP), 1 (ICMP), 58 (ICMPv6),
                                  or -1 (any). It cannot be left blank when type is
                                  set to 0 (manual).'
                                items:
                                  type: number
                                type: array
                              serviceSetType:
                                description: 'Specifies the service group type: 0
                                  (user-defined service group), 1 (predefined service
                                  group).'
                                type: number
                              setId:
                                description: Specifies the service group ID.
                                type: string
                            type: object
                          type: array
                        serviceSetId:
                          description: Specifies the Service group ID. This parameter
                            cannot be left blank when type is set to 1 (associated
                            IP address group).
                          type: string
                        serviceSetName:
                          description: Specifies the Service group name. This parameter
                            cannot be left blank when type is set to 1 (associated
                            IP address group).
                          type: string
                        serviceSetType:
                          description: 'Specifies the service group type: 0 (user-defined
                            service group), 1 (common web service), 2 (common remote
                            login and ping), or 3 (common database).'
                          type: number
                        sourcePort:
                          description: Specifies the source port.
                          type: string
                        type:
                          description: 'Specifies the service input type: 0 (manual),
                            1 (automatic).'
                          type: number
                      type: object
                    type: array
                  source:
                    description: |-
                      Specifies the source address Data Transport Object.
                      The source structure is documented below.
                    items:
                      properties:
                        address:
                          description: Specifies the IP address information. It cannot
                            be left blank if type is set to 0.
                          type: string
                        addressGroup:
                          description: Specifies the Address group ID list. This parameter
                            cannot be left blank when type is set to 5 (multiple objects).
                          items:
                            type: string
                          type: array
                        addressSetId:
                          description: Specifies the ID of an associated IP address
                            group. This parameter cannot be left blank when type is
                            set to 1.
                          type: string
                        addressSetName:
                          description: Specifies the name of an associated IP address
                            group. This parameter cannot be left blank when type is
                            set to 1.
                          type: string
                        addressSetType:
                          description: Specifies the Address group type. It cannot
                            be left blank when type is set to 1 (associated IP address
                            group). It value can be 0 (user-defined address group),
                            1 (WAF back-to-source IP address group), 2 (DDoS back-to-source
                            IP address group), or 3 (NAT64 address group).
                          type: number
                        addressType:
                          description: 'Specifies theInternet protocol type of an
                            address: 0 (IPv4), 1 (IPv6). If type is 0, this parameter
                            cannot be left blank.'
                          type: number
                        domainAddressName:
                          description: Specifies the name of a domain name address.
                            This parameter is valid when type is set to 2 (domain
                            name) or 7 (application domain name group).
                          type: string
                        domainSetId:
                          description: Specifies the domain group ID. The value cannot
                            be left blank when type is set to 4 (domain name group)
                            or 7 (domain name group - application).
                          type: string
                        domainSetName:
                          description: Specifies the domain group name. The value
                            cannot be left blank when type is set to 4 (domain name
                            group) or 7 (domain name group - application).
                          type: string
                        ipAddress:
                          description: Specifies the IP address list. This parameter
                            cannot be left blank when type is set to 5 (multiple objects).
                          items:
                            type: string
                          type: array
                        predefinedGroup:
                          description: Specifies the pre-defined address group ID
                            list. This parameter cannot be left blank when type is
                            set to 5 (multiple objects).
                          items:
                            type: string
                          type: array
                        regionList:
                          description: Specifies the rule region list.
                          items:
                            properties:
                              regionId:
                                description: Specifies the region ID.
                                type: string
                              regionType:
                                description: 'Specifies the region type: 0 (country),
                                  1 (province), and 2 (continent).'
                                type: number
                            type: object
                          type: array
                        regionListJson:
                          description: Specifies the JSON value of the rule region
                            list.
                          type: string
                        type:
                          description: 'Specifies the Address type: 0 (manual input),
                            1 (associated IP address group), 2 (domain name), 3 (geographical
                            location), 4 (domain name group) 5 (multiple objects),
                            6 (domain name group - network), 7 (domain name group
                            - application).'
                          type: number
                      type: object
                    type: array
                  status:
                    description: 'Specifies the rule status: 0 (disabled), 1 (enabled).'
                    type: number
                  type:
                    description: 'Rule type: 0 (Internet border rule), 1 (inter-VPC
                      rule), or 2 (NAT rule). When type is set to 0, the source and
                      destination addresses of the rule must be EIPs or domain names
                      of the public network. For an inter-VPC rule, the source and
                      destination addresses must be private IP addresses. For a NAT
                      rule, the source address must be a private IP address, and the
                      destination address must be an EIP or domain name of the public
                      network.'
                    type: number
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  actionType:
                    description: 'Specifies the rule action: 0 (permit), 1 (deny).'
                    type: number
                  addressType:
                    description: 'Specifies the Internet protocol type of an address:
                      0 (IPv4), 1 (IPv6).'
                    type: number
                  applications:
                    description: 'Specifies the rule application list . Allowed list
                      values: HTTP, HTTPS, TLS1, DNS, SSH, MYSQL, SMTP, RDP, RDPS,
                      VNC, POP3, IMAP4, SMTPS, POP3S, FTPS, ANY, or BGP.'
                    items:
                      type: string
                    type: array
                  applicationsJsonString:
                    description: Specifies the JSON string converted from the applications
                      field in the application list.
                    type: string
                  description:
                    description: Specifies the description of the rule.
                    type: string
                  destination:
                    description: |-
                      Specifies the destination address Data Transport Object.
                      The destination structure is documented below.
                    items:
                      properties:
                        address:
                          description: Specifies the IP address information. It cannot
                            be left blank if type is set to 0.
                          type: string
                        addressGroup:
                          description: Specifies the Address group ID list. This parameter
                            cannot be left blank when type is set to 5 (multiple objects).
                          items:
                            type: string
                          type: array
                        addressSetId:
                          description: Specifies the ID of an associated IP address
                            group. This parameter cannot be left blank when type is
                            set to 1.
                          type: string
                        addressSetName:
                          description: Specifies the name of an associated IP address
                            group. This parameter cannot be left blank when type is
                            set to 1.
                          type: string
                        addressSetType:
                          description: Specifies the Address group type. It cannot
                            be left blank when type is set to 1 (associated IP address
                            group). It value can be 0 (user-defined address group),
                            1 (WAF back-to-source IP address group), 2 (DDoS back-to-source
                            IP address group), or 3 (NAT64 address group).
                          type: number
                        addressType:
                          description: 'Specifies theInternet protocol type of an
                            address: 0 (IPv4), 1 (IPv6). If type is 0, this parameter
                            cannot be left blank.'
                          type: number
                        domainAddressName:
                          description: Specifies the name of a domain name address.
                            This parameter is valid when type is set to 2 (domain
                            name) or 7 (application domain name group).
                          type: string
                        domainSetId:
                          description: Specifies the domain group ID. The value cannot
                            be left blank when type is set to 4 (domain name group)
                            or 7 (domain name group - application).
                          type: string
                        domainSetName:
                          description: Specifies the domain group name. The value
                            cannot be left blank when type is set to 4 (domain name
                            group) or 7 (domain name group - application).
                          type: string
                        ipAddress:
                          description: Specifies the IP address list. This parameter
                            cannot be left blank when type is set to 5 (multiple objects).
                          items:
                            type: string
                          type: array
                        predefinedGroup:
                          description: Specifies the pre-defined address group ID
                            list. This parameter cannot be left blank when type is
                            set to 5 (multiple objects).
                          items:
                            type: string
                          type: array
                        regionList:
                          description: Specifies the rule region list.
                          items:
                            properties:
                              regionId:
                                description: Specifies the region ID.
                                type: string
                              regionType:
                                description: 'Specifies the region type: 0 (country),
                                  1 (province), and 2 (continent).'
                                type: number
                            type: object
                          type: array
                        regionListJson:
                          description: Specifies the JSON value of the rule region
                            list.
                          type: string
                        type:
                          description: 'Rule type: 0 (Internet border rule), 1 (inter-VPC
                            rule), or 2 (NAT rule). When type is set to 0, the source
                            and destination addresses of the rule must be EIPs or
                            domain names of the public network. For an inter-VPC rule,
                            the source and destination addresses must be private IP
                            addresses. For a NAT rule, the source address must be
                            a private IP address, and the destination address must
                            be an EIP or domain name of the public network.'
                          type: number
                      type: object
                    type: array
                  direction:
                    description: 'Specifies the Direction: 0 (inbound) or 1 (outbound).
                      This parameter is mandatory when type is set to 0 (Internet
                      rule) or 2 (NAT rule).'
                    type: number
                  longConnectEnable:
                    description: 'Specifies whether to support persistent connections:
                      0 (no), 1 (yes).'
                    type: number
                  longConnectTime:
                    description: Specifies the persistent connection duration.
                    type: number
                  longConnectTimeHour:
                    description: Specifies the persistent connection duration (hour).
                    type: number
                  longConnectTimeMinute:
                    description: Specifies the persistent connection duration (minute).
                    type: number
                  longConnectTimeSecond:
                    description: Specifies the persistent connection duration (second).
                    type: number
                  name:
                    description: Specifies the CFW ACL rule name. The CFW ACL rule
                      name of the same type is unique in the same firewall instance.
                    type: string
                  objectId:
                    description: Protected object ID, which is used to distinguish
                      between Internet border protection and VPC border protection
                      after a cloud firewall is created. If the value of type is 0,
                      the protected object ID belongs to the Internet border. If the
                      value of type is 1, the protected object ID belongs to the VPC
                      border.
                    type: string
                  sequence:
                    description: Specifies the request body for changing the rule
                      sequence. The sequence structure is documented below.
                    items:
                      properties:
                        bottom:
                          description: 'Specifies whether to pin to bottom: 0 (no),
                            1 (yes).'
                          type: number
                        destRuleId:
                          description: Specifies the ID of the target rule.
                          type: string
                        top:
                          description: 'Specifies whether to pin on top: 0 (no), 1
                            (yes).'
                          type: number
                      type: object
                    type: array
                  service:
                    description: |-
                      Specifies the service object.
                      The service structure is documented below.
                    items:
                      properties:
                        customService:
                          description: Specifies the custom service.
                          items:
                            properties:
                              description:
                                description: Specifies the description of the rule.
                                type: string
                              destPort:
                                description: Specifies the destination port.
                                type: string
                              name:
                                description: Specifies the CFW ACL rule name. The
                                  CFW ACL rule name of the same type is unique in
                                  the same firewall instance.
                                type: string
                              protocol:
                                description: 'Specifies the protocol type: 6 (TCP),
                                  17 (UDP), 1 (ICMP), 58 (ICMPv6), or -1 (any). It
                                  cannot be left blank when type is set to 0 (manual).'
                                type: number
                              sourcePort:
                                description: Specifies the source port.
                                type: string
                            type: object
                          type: array
                        destPort:
                          description: Specifies the destination port.
                          type: string
                        predefinedGroup:
                          description: Specifies the pre-defined service group ID
                            list.
                          items:
                            type: string
                          type: array
                        protocol:
                          description: 'Specifies the protocol type: 6 (TCP), 17 (UDP),
                            1 (ICMP), 58 (ICMPv6), or -1 (any). It cannot be left
                            blank when type is set to 0 (manual).'
                          type: number
                        protocols:
                          description: 'Specifies the protocol list. Permitted list
                            values: 6 (TCP), 17 (UDP), 1 (ICMP), 58 (ICMPv6), or -1
                            (any). It cannot be left blank when type is set to 0 (manual).'
                          items:
                            type: number
                          type: array
                        serviceGroup:
                          description: Specifies the service group ID list.
                          items:
                            type: string
                          type: array
                        serviceGroupNames:
                          description: Specifies the service group name list.
                          items:
                            properties:
                              name:
                                description: Specifies the CFW ACL rule name. The
                                  CFW ACL rule name of the same type is unique in
                                  the same firewall instance.
                                type: string
                              protocols:
                                description: 'Specifies the protocol list. Permitted
                                  list values: 6 (TCP), 17 (UDP), 1 (ICMP), 58 (ICMPv6),
                                  or -1 (any). It cannot be left blank when type is
                                  set to 0 (manual).'
                                items:
                                  type: number
                                type: array
                              serviceSetType:
                                description: 'Specifies the service group type: 0
                                  (user-defined service group), 1 (predefined service
                                  group).'
                                type: number
                              setId:
                                description: Specifies the service group ID.
                                type: string
                            type: object
                          type: array
                        serviceSetId:
                          description: Specifies the Service group ID. This parameter
                            cannot be left blank when type is set to 1 (associated
                            IP address group).
                          type: string
                        serviceSetName:
                          description: Specifies the Service group name. This parameter
                            cannot be left blank when type is set to 1 (associated
                            IP address group).
                          type: string
                        serviceSetType:
                          description: 'Specifies the service group type: 0 (user-defined
                            service group), 1 (common web service), 2 (common remote
                            login and ping), or 3 (common database).'
                          type: number
                        sourcePort:
                          description: Specifies the source port.
                          type: string
                        type:
                          description: 'Specifies the service input type: 0 (manual),
                            1 (automatic).'
                          type: number
                      type: object
                    type: array
                  source:
                    description: |-
                      Specifies the source address Data Transport Object.
                      The source structure is documented below.
                    items:
                      properties:
                        address:
                          description: Specifies the IP address information. It cannot
                            be left blank if type is set to 0.
                          type: string
                        addressGroup:
                          description: Specifies the Address group ID list. This parameter
                            cannot be left blank when type is set to 5 (multiple objects).
                          items:
                            type: string
                          type: array
                        addressSetId:
                          description: Specifies the ID of an associated IP address
                            group. This parameter cannot be left blank when type is
                            set to 1.
                          type: string
                        addressSetName:
                          description: Specifies the name of an associated IP address
                            group. This parameter cannot be left blank when type is
                            set to 1.
                          type: string
                        addressSetType:
                          description: Specifies the Address group type. It cannot
                            be left blank when type is set to 1 (associated IP address
                            group). It value can be 0 (user-defined address group),
                            1 (WAF back-to-source IP address group), 2 (DDoS back-to-source
                            IP address group), or 3 (NAT64 address group).
                          type: number
                        addressType:
                          description: 'Specifies theInternet protocol type of an
                            address: 0 (IPv4), 1 (IPv6). If type is 0, this parameter
                            cannot be left blank.'
                          type: number
                        domainAddressName:
                          description: Specifies the name of a domain name address.
                            This parameter is valid when type is set to 2 (domain
                            name) or 7 (application domain name group).
                          type: string
                        domainSetId:
                          description: Specifies the domain group ID. The value cannot
                            be left blank when type is set to 4 (domain name group)
                            or 7 (domain name group - application).
                          type: string
                        domainSetName:
                          description: Specifies the domain group name. The value
                            cannot be left blank when type is set to 4 (domain name
                            group) or 7 (domain name group - application).
                          type: string
                        ipAddress:
                          description: Specifies the IP address list. This parameter
                            cannot be left blank when type is set to 5 (multiple objects).
                          items:
                            type: string
                          type: array
                        predefinedGroup:
                          description: Specifies the pre-defined address group ID
                            list. This parameter cannot be left blank when type is
                            set to 5 (multiple objects).
                          items:
                            type: string
                          type: array
                        regionList:
                          description: Specifies the rule region list.
                          items:
                            properties:
                              regionId:
                                description: Specifies the region ID.
                                type: string
                              regionType:
                                description: 'Specifies the region type: 0 (country),
                                  1 (province), and 2 (continent).'
                                type: number
                            type: object
                          type: array
                        regionListJson:
                          description: Specifies the JSON value of the rule region
                            list.
                          type: string
                        type:
                          description: 'Specifies the Address type: 0 (manual input),
                            1 (associated IP address group), 2 (domain name), 3 (geographical
                            location), 4 (domain name group) 5 (multiple objects),
                            6 (domain name group - network), 7 (domain name group
                            - application).'
                          type: number
                      type: object
                    type: array
                  status:
                    description: 'Specifies the rule status: 0 (disabled), 1 (enabled).'
                    type: number
                  type:
                    description: 'Rule type: 0 (Internet border rule), 1 (inter-VPC
                      rule), or 2 (NAT rule). When type is set to 0, the source and
                      destination addresses of the rule must be EIPs or domain names
                      of the public network. For an inter-VPC rule, the source and
                      destination addresses must be private IP addresses. For a NAT
                      rule, the source address must be a private IP address, and the
                      destination address must be an EIP or domain name of the public
                      network.'
                    type: number
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.actionType is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.actionType)
                || (has(self.initProvider) && has(self.initProvider.actionType))'
            - message: spec.forProvider.addressType is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.addressType)
                || (has(self.initProvider) && has(self.initProvider.addressType))'
            - message: spec.forProvider.destination is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.destination)
                || (has(self.initProvider) && has(self.initProvider.destination))'
            - message: spec.forProvider.longConnectEnable is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.longConnectEnable)
                || (has(self.initProvider) && has(self.initProvider.longConnectEnable))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.objectId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.objectId)
                || (has(self.initProvider) && has(self.initProvider.objectId))'
            - message: spec.forProvider.sequence is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.sequence)
                || (has(self.initProvider) && has(self.initProvider.sequence))'
            - message: spec.forProvider.service is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.service)
                || (has(self.initProvider) && has(self.initProvider.service))'
            - message: spec.forProvider.source is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.source)
                || (has(self.initProvider) && has(self.initProvider.source))'
            - message: spec.forProvider.status is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.status)
                || (has(self.initProvider) && has(self.initProvider.status))'
            - message: spec.forProvider.type is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.type)
                || (has(self.initProvider) && has(self.initProvider.type))'
          status:
            description: ACLRuleV1Status defines the observed state of ACLRuleV1.
            properties:
              atProvider:
                properties:
                  actionType:
                    description: 'Specifies the rule action: 0 (permit), 1 (deny).'
                    type: number
                  addressType:
                    description: 'Specifies the Internet protocol type of an address:
                      0 (IPv4), 1 (IPv6).'
                    type: number
                  applications:
                    description: 'Specifies the rule application list . Allowed list
                      values: HTTP, HTTPS, TLS1, DNS, SSH, MYSQL, SMTP, RDP, RDPS,
                      VNC, POP3, IMAP4, SMTPS, POP3S, FTPS, ANY, or BGP.'
                    items:
                      type: string
                    type: array
                  applicationsJsonString:
                    description: Specifies the JSON string converted from the applications
                      field in the application list.
                    type: string
                  createdDate:
                    description: Indicates the Rule creation time in YYYY-MM-DD hh:mm:ss
                      format.
                    type: string
                  description:
                    description: Specifies the description of the rule.
                    type: string
                  destination:
                    description: |-
                      Specifies the destination address Data Transport Object.
                      The destination structure is documented below.
                    items:
                      properties:
                        address:
                          description: Specifies the IP address information. It cannot
                            be left blank if type is set to 0.
                          type: string
                        addressGroup:
                          description: Specifies the Address group ID list. This parameter
                            cannot be left blank when type is set to 5 (multiple objects).
                          items:
                            type: string
                          type: array
                        addressSetId:
                          description: Specifies the ID of an associated IP address
                            group. This parameter cannot be left blank when type is
                            set to 1.
                          type: string
                        addressSetName:
                          description: Specifies the name of an associated IP address
                            group. This parameter cannot be left blank when type is
                            set to 1.
                          type: string
                        addressSetType:
                          description: Specifies the Address group type. It cannot
                            be left blank when type is set to 1 (associated IP address
                            group). It value can be 0 (user-defined address group),
                            1 (WAF back-to-source IP address group), 2 (DDoS back-to-source
                            IP address group), or 3 (NAT64 address group).
                          type: number
                        addressType:
                          description: 'Specifies theInternet protocol type of an
                            address: 0 (IPv4), 1 (IPv6). If type is 0, this parameter
                            cannot be left blank.'
                          type: number
                        domainAddressName:
                          description: Specifies the name of a domain name address.
                            This parameter is valid when type is set to 2 (domain
                            name) or 7 (application domain name group).
                          type: string
                        domainSetId:
                          description: Specifies the domain group ID. The value cannot
                            be left blank when type is set to 4 (domain name group)
                            or 7 (domain name group - application).
                          type: string
                        domainSetName:
                          description: Specifies the domain group name. The value
                            cannot be left blank when type is set to 4 (domain name
                            group) or 7 (domain name group - application).
                          type: string
                        ipAddress:
                          description: Specifies the IP address list. This parameter
                            cannot be left blank when type is set to 5 (multiple objects).
                          items:
                            type: string
                          type: array
                        predefinedGroup:
                          description: Specifies the pre-defined address group ID
                            list. This parameter cannot be left blank when type is
                            set to 5 (multiple objects).
                          items:
                            type: string
                          type: array
                        regionList:
                          description: Specifies the rule region list.
                          items:
                            properties:
                              regionId:
                                description: Specifies the region ID.
                                type: string
                              regionType:
                                description: 'Specifies the region type: 0 (country),
                                  1 (province), and 2 (continent).'
                                type: number
                            type: object
                          type: array
                        regionListJson:
                          description: Specifies the JSON value of the rule region
                            list.
                          type: string
                        type:
                          description: 'Rule type: 0 (Internet border rule), 1 (inter-VPC
                            rule), or 2 (NAT rule). When type is set to 0, the source
                            and destination addresses of the rule must be EIPs or
                            domain names of the public network. For an inter-VPC rule,
                            the source and destination addresses must be private IP
                            addresses. For a NAT rule, the source address must be
                            a private IP address, and the destination address must
                            be an EIP or domain name of the public network.'
                          type: number
                      type: object
                    type: array
                  direction:
                    description: 'Specifies the Direction: 0 (inbound) or 1 (outbound).
                      This parameter is mandatory when type is set to 0 (Internet
                      rule) or 2 (NAT rule).'
                    type: number
                  id:
                    description: Indicates the ACL rule ID.
                    type: string
                  lastOpenTime:
                    description: Indicates the Last time when the rule was enabled
                      in  YYYY-MM-DD hh:mm:ss format.
                    type: string
                  longConnectEnable:
                    description: 'Specifies whether to support persistent connections:
                      0 (no), 1 (yes).'
                    type: number
                  longConnectTime:
                    description: Specifies the persistent connection duration.
                    type: number
                  longConnectTimeHour:
                    description: Specifies the persistent connection duration (hour).
                    type: number
                  longConnectTimeMinute:
                    description: Specifies the persistent connection duration (minute).
                    type: number
                  longConnectTimeSecond:
                    description: Specifies the persistent connection duration (second).
                    type: number
                  name:
                    description: Specifies the CFW ACL rule name. The CFW ACL rule
                      name of the same type is unique in the same firewall instance.
                    type: string
                  objectId:
                    description: Protected object ID, which is used to distinguish
                      between Internet border protection and VPC border protection
                      after a cloud firewall is created. If the value of type is 0,
                      the protected object ID belongs to the Internet border. If the
                      value of type is 1, the protected object ID belongs to the VPC
                      border.
                    type: string
                  sequence:
                    description: Specifies the request body for changing the rule
                      sequence. The sequence structure is documented below.
                    items:
                      properties:
                        bottom:
                          description: 'Specifies whether to pin to bottom: 0 (no),
                            1 (yes).'
                          type: number
                        destRuleId:
                          description: Specifies the ID of the target rule.
                          type: string
                        top:
                          description: 'Specifies whether to pin on top: 0 (no), 1
                            (yes).'
                          type: number
                      type: object
                    type: array
                  service:
                    description: |-
                      Specifies the service object.
                      The service structure is documented below.
                    items:
                      properties:
                        customService:
                          description: Specifies the custom service.
                          items:
                            properties:
                              description:
                                description: Specifies the description of the rule.
                                type: string
                              destPort:
                                description: Specifies the destination port.
                                type: string
                              name:
                                description: Specifies the CFW ACL rule name. The
                                  CFW ACL rule name of the same type is unique in
                                  the same firewall instance.
                                type: string
                              protocol:
                                description: 'Specifies the protocol type: 6 (TCP),
                                  17 (UDP), 1 (ICMP), 58 (ICMPv6), or -1 (any). It
                                  cannot be left blank when type is set to 0 (manual).'
                                type: number
                              sourcePort:
                                description: Specifies the source port.
                                type: string
                            type: object
                          type: array
                        destPort:
                          description: Specifies the destination port.
                          type: string
                        predefinedGroup:
                          description: Specifies the pre-defined service group ID
                            list.
                          items:
                            type: string
                          type: array
                        protocol:
                          description: 'Specifies the protocol type: 6 (TCP), 17 (UDP),
                            1 (ICMP), 58 (ICMPv6), or -1 (any). It cannot be left
                            blank when type is set to 0 (manual).'
                          type: number
                        protocols:
                          description: 'Specifies the protocol list. Permitted list
                            values: 6 (TCP), 17 (UDP), 1 (ICMP), 58 (ICMPv6), or -1
                            (any). It cannot be left blank when type is set to 0 (manual).'
                          items:
                            type: number
                          type: array
                        serviceGroup:
                          description: Specifies the service group ID list.
                          items:
                            type: string
                          type: array
                        serviceGroupNames:
                          description: Specifies the service group name list.
                          items:
                            properties:
                              name:
                                description: Specifies the CFW ACL rule name. The
                                  CFW ACL rule name of the same type is unique in
                                  the same firewall instance.
                                type: string
                              protocols:
                                description: 'Specifies the protocol list. Permitted
                                  list values: 6 (TCP), 17 (UDP), 1 (ICMP), 58 (ICMPv6),
                                  or -1 (any). It cannot be left blank when type is
                                  set to 0 (manual).'
                                items:
                                  type: number
                                type: array
                              serviceSetType:
                                description: 'Specifies the service group type: 0
                                  (user-defined service group), 1 (predefined service
                                  group).'
                                type: number
                              setId:
                                description: Specifies the service group ID.
                                type: string
                            type: object
                          type: array
                        serviceSetId:
                          description: Specifies the Service group ID. This parameter
                            cannot be left blank when type is set to 1 (associated
                            IP address group).
                          type: string
                        serviceSetName:
                          description: Specifies the Service group name. This parameter
                            cannot be left blank when type is set to 1 (associated
                            IP address group).
                          type: string
                        serviceSetType:
                          description: 'Specifies the service group type: 0 (user-defined
                            service group), 1 (common web service), 2 (common remote
                            login and ping), or 3 (common database).'
                          type: number
                        sourcePort:
                          description: Specifies the source port.
                          type: string
                        type:
                          description: 'Specifies the service input type: 0 (manual),
                            1 (automatic).'
                          type: number
                      type: object
                    type: array
                  source:
                    description: |-
                      Specifies the source address Data Transport Object.
                      The source structure is documented below.
                    items:
                      properties:
                        address:
                          description: Specifies the IP address information. It cannot
                            be left blank if type is set to 0.
                          type: string
                        addressGroup:
                          description: Specifies the Address group ID list. This parameter
                            cannot be left blank when type is set to 5 (multiple objects).
                          items:
                            type: string
                          type: array
                        addressSetId:
                          description: Specifies the ID of an associated IP address
                            group. This parameter cannot be left blank when type is
                            set to 1.
                          type: string
                        addressSetName:
                          description: Specifies the name of an associated IP address
                            group. This parameter cannot be left blank when type is
                            set to 1.
                          type: string
                        addressSetType:
                          description: Specifies the Address group type. It cannot
                            be left blank when type is set to 1 (associated IP address
                            group). It value can be 0 (user-defined address group),
                            1 (WAF back-to-source IP address group), 2 (DDoS back-to-source
                            IP address group), or 3 (NAT64 address group).
                          type: number
                        addressType:
                          description: 'Specifies theInternet protocol type of an
                            address: 0 (IPv4), 1 (IPv6). If type is 0, this parameter
                            cannot be left blank.'
                          type: number
                        domainAddressName:
                          description: Specifies the name of a domain name address.
                            This parameter is valid when type is set to 2 (domain
                            name) or 7 (application domain name group).
                          type: string
                        domainSetId:
                          description: Specifies the domain group ID. The value cannot
                            be left blank when type is set to 4 (domain name group)
                            or 7 (domain name group - application).
                          type: string
                        domainSetName:
                          description: Specifies the domain group name. The value
                            cannot be left blank when type is set to 4 (domain name
                            group) or 7 (domain name group - application).
                          type: string
                        ipAddress:
                          description: Specifies the IP address list. This parameter
                            cannot be left blank when type is set to 5 (multiple objects).
                          items:
                            type: string
                          type: array
                        predefinedGroup:
                          description: Specifies the pre-defined address group ID
                            list. This parameter cannot be left blank when type is
                            set to 5 (multiple objects).
                          items:
                            type: string
                          type: array
                        regionList:
                          description: Specifies the rule region list.
                          items:
                            properties:
                              regionId:
                                description: Specifies the region ID.
                                type: string
                              regionType:
                                description: 'Specifies the region type: 0 (country),
                                  1 (province), and 2 (continent).'
                                type: number
                            type: object
                          type: array
                        regionListJson:
                          description: Specifies the JSON value of the rule region
                            list.
                          type: string
                        type:
                          description: 'Specifies the Address type: 0 (manual input),
                            1 (associated IP address group), 2 (domain name), 3 (geographical
                            location), 4 (domain name group) 5 (multiple objects),
                            6 (domain name group - network), 7 (domain name group
                            - application).'
                          type: number
                      type: object
                    type: array
                  status:
                    description: 'Specifies the rule status: 0 (disabled), 1 (enabled).'
                    type: number
                  type:
                    description: 'Rule type: 0 (Internet border rule), 1 (inter-VPC
                      rule), or 2 (NAT rule). When type is set to 0, the source and
                      destination addresses of the rule must be EIPs or domain names
                      of the public network. For an inter-VPC rule, the source and
                      destination addresses must be private IP addresses. For a NAT
                      rule, the source address must be a private IP address, and the
                      destination address must be an EIP or domain name of the public
                      network.'
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
