---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: vaultv3s.cbr.opentelekomcloud.m.crossplane.io
spec:
  group: cbr.opentelekomcloud.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - opentelekomcloud
    kind: VaultV3
    listKind: VaultV3List
    plural: vaultv3s
    singular: vaultv3
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VaultV3 is the Schema for the VaultV3s API. Manages a CBR Vault
          resource within OpenTelekomCloud.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VaultV3Spec defines the desired state of VaultV3
            properties:
              forProvider:
                properties:
                  autoBind:
                    description: Whether automatic association is supported.
                    type: boolean
                  autoExpand:
                    description: |-
                      Whether to automatically expand the vault capacity. Only pay-per-use vaults support this
                      function.
                    type: boolean
                  backupPolicyId:
                    description: |-
                      Backup policy ID. If the value of this parameter is empty, automatic backup is not
                      performed. Deprecated use policy instead.
                    type: string
                  billing:
                    description: 'Billing parameter information for creation. Billing
                      fields:'
                    items:
                      properties:
                        chargingMode:
                          description: Billing mode. Possible value is post_paid (pay-per-use).
                          type: string
                        cloudType:
                          description: Cloud platform. One of public (default), hybrid
                          type: string
                        consistentLevel:
                          description: Backup specifications. The default value is
                            crash_consistent
                          type: string
                        consoleUrl:
                          description: Redirection URL.
                          type: string
                        extraInfo:
                          additionalProperties:
                            type: string
                          description: Map of extra info.
                          type: object
                          x-kubernetes-map-type: granular
                        isAutoPay:
                          description: |-
                            Whether the fee is automatically deducted from the customer's account balance after an
                            order is submitted. The non-automatic payment mode is used by default.
                          type: boolean
                        isAutoRenew:
                          description: |-
                            Whether to automatically renew the subscription after expiration. By default, it is
                            not renewed.
                          type: boolean
                        objectType:
                          description: Object type. One of server, disk, turbo.
                          type: string
                        periodNum:
                          type: number
                        periodType:
                          description: Type of the resource to be backed up. Possible
                            values are OS::Nova::Server, OS::Cinder::Volume and OS::Sfs::Turbo.
                          type: string
                        protectType:
                          description: Operation type. One of backup, replication
                          type: string
                        size:
                          description: Capacity, in GB. Minimum 1, maximum 10485760
                          type: number
                      type: object
                    type: array
                  bindRules:
                    description: |-
                      Tag map, a rules for automatic association. You can only select tag keys and values from
                      the existing ones. If there are no tags available, go to the corresponding service to create one.
                      You can add a maximum of 5 tags for a search. If more than one tag is added, the backups containing one of the
                      specified tags will be returned.
                    items:
                      properties:
                        key:
                          type: string
                        value:
                          type: string
                      type: object
                    type: array
                  description:
                    description: User-defined vault description.
                    type: string
                  enterpriseProjectId:
                    description: ID of the resource to be backed up.
                    type: string
                  locked:
                    description: |-
                      Specifies whether the vault is locked. A locked vault cannot be unlocked.
                      Defaults to false.
                    type: boolean
                  name:
                    description: Vault name.
                    type: string
                  policy:
                    description: |-
                      Specifies the policy details to associate with the CBR vault.
                      The object structure is documented below.
                      The policy details to associate with the CBR vault.
                    items:
                      properties:
                        destinationVaultId:
                          description: |-
                            Specifies the ID of destination vault to which the replication policy
                            will be associated.
                          type: string
                        id:
                          description: Specifies the policy ID.
                          type: string
                      type: object
                    type: array
                  resource:
                    description: 'Associated resources. Multiple. Resource fields:'
                    items:
                      properties:
                        backupCount:
                          type: number
                        backupSize:
                          description: Capacity, in GB. Minimum 1, maximum 10485760
                          type: number
                        excludeVolumes:
                          description: List of excluded volumes.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        id:
                          description: Specifies the policy ID.
                          type: string
                        includeVolumes:
                          description: List of included volumes.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        name:
                          description: Vault name.
                          type: string
                        protectStatus:
                          description: Vault status.
                          type: string
                        size:
                          description: Capacity, in GB. Minimum 1, maximum 10485760
                          type: number
                        type:
                          description: Type of the resource to be backed up. Possible
                            values are OS::Nova::Server, OS::Cinder::Volume and OS::Sfs::Turbo.
                          type: string
                      type: object
                    type: array
                  tags:
                    additionalProperties:
                      type: string
                    description: Tag map.
                    type: object
                    x-kubernetes-map-type: granular
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  autoBind:
                    description: Whether automatic association is supported.
                    type: boolean
                  autoExpand:
                    description: |-
                      Whether to automatically expand the vault capacity. Only pay-per-use vaults support this
                      function.
                    type: boolean
                  backupPolicyId:
                    description: |-
                      Backup policy ID. If the value of this parameter is empty, automatic backup is not
                      performed. Deprecated use policy instead.
                    type: string
                  billing:
                    description: 'Billing parameter information for creation. Billing
                      fields:'
                    items:
                      properties:
                        chargingMode:
                          description: Billing mode. Possible value is post_paid (pay-per-use).
                          type: string
                        cloudType:
                          description: Cloud platform. One of public (default), hybrid
                          type: string
                        consistentLevel:
                          description: Backup specifications. The default value is
                            crash_consistent
                          type: string
                        consoleUrl:
                          description: Redirection URL.
                          type: string
                        extraInfo:
                          additionalProperties:
                            type: string
                          description: Map of extra info.
                          type: object
                          x-kubernetes-map-type: granular
                        isAutoPay:
                          description: |-
                            Whether the fee is automatically deducted from the customer's account balance after an
                            order is submitted. The non-automatic payment mode is used by default.
                          type: boolean
                        isAutoRenew:
                          description: |-
                            Whether to automatically renew the subscription after expiration. By default, it is
                            not renewed.
                          type: boolean
                        objectType:
                          description: Object type. One of server, disk, turbo.
                          type: string
                        periodNum:
                          type: number
                        periodType:
                          description: Type of the resource to be backed up. Possible
                            values are OS::Nova::Server, OS::Cinder::Volume and OS::Sfs::Turbo.
                          type: string
                        protectType:
                          description: Operation type. One of backup, replication
                          type: string
                        size:
                          description: Capacity, in GB. Minimum 1, maximum 10485760
                          type: number
                      type: object
                    type: array
                  bindRules:
                    description: |-
                      Tag map, a rules for automatic association. You can only select tag keys and values from
                      the existing ones. If there are no tags available, go to the corresponding service to create one.
                      You can add a maximum of 5 tags for a search. If more than one tag is added, the backups containing one of the
                      specified tags will be returned.
                    items:
                      properties:
                        key:
                          type: string
                        value:
                          type: string
                      type: object
                    type: array
                  description:
                    description: User-defined vault description.
                    type: string
                  enterpriseProjectId:
                    description: ID of the resource to be backed up.
                    type: string
                  locked:
                    description: |-
                      Specifies whether the vault is locked. A locked vault cannot be unlocked.
                      Defaults to false.
                    type: boolean
                  name:
                    description: Vault name.
                    type: string
                  policy:
                    description: |-
                      Specifies the policy details to associate with the CBR vault.
                      The object structure is documented below.
                      The policy details to associate with the CBR vault.
                    items:
                      properties:
                        destinationVaultId:
                          description: |-
                            Specifies the ID of destination vault to which the replication policy
                            will be associated.
                          type: string
                        id:
                          description: Specifies the policy ID.
                          type: string
                      type: object
                    type: array
                  resource:
                    description: 'Associated resources. Multiple. Resource fields:'
                    items:
                      properties:
                        backupCount:
                          type: number
                        backupSize:
                          description: Capacity, in GB. Minimum 1, maximum 10485760
                          type: number
                        excludeVolumes:
                          description: List of excluded volumes.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        id:
                          description: Specifies the policy ID.
                          type: string
                        includeVolumes:
                          description: List of included volumes.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        name:
                          description: Vault name.
                          type: string
                        protectStatus:
                          description: Vault status.
                          type: string
                        size:
                          description: Capacity, in GB. Minimum 1, maximum 10485760
                          type: number
                        type:
                          description: Type of the resource to be backed up. Possible
                            values are OS::Nova::Server, OS::Cinder::Volume and OS::Sfs::Turbo.
                          type: string
                      type: object
                    type: array
                  tags:
                    additionalProperties:
                      type: string
                    description: Tag map.
                    type: object
                    x-kubernetes-map-type: granular
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.billing is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.billing)
                || (has(self.initProvider) && has(self.initProvider.billing))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: VaultV3Status defines the observed state of VaultV3.
            properties:
              atProvider:
                properties:
                  autoBind:
                    description: Whether automatic association is supported.
                    type: boolean
                  autoExpand:
                    description: |-
                      Whether to automatically expand the vault capacity. Only pay-per-use vaults support this
                      function.
                    type: boolean
                  backupPolicyId:
                    description: |-
                      Backup policy ID. If the value of this parameter is empty, automatic backup is not
                      performed. Deprecated use policy instead.
                    type: string
                  billing:
                    description: 'Billing parameter information for creation. Billing
                      fields:'
                    items:
                      properties:
                        allocated:
                          description: Allocated capacity, in MB.
                          type: number
                        chargingMode:
                          description: Billing mode. Possible value is post_paid (pay-per-use).
                          type: string
                        cloudType:
                          description: Cloud platform. One of public (default), hybrid
                          type: string
                        consistentLevel:
                          description: Backup specifications. The default value is
                            crash_consistent
                          type: string
                        consoleUrl:
                          description: Redirection URL.
                          type: string
                        extraInfo:
                          additionalProperties:
                            type: string
                          description: Map of extra info.
                          type: object
                          x-kubernetes-map-type: granular
                        frozenScene:
                          description: Scenario when an account is frozen.
                          type: string
                        isAutoPay:
                          description: |-
                            Whether the fee is automatically deducted from the customer's account balance after an
                            order is submitted. The non-automatic payment mode is used by default.
                          type: boolean
                        isAutoRenew:
                          description: |-
                            Whether to automatically renew the subscription after expiration. By default, it is
                            not renewed.
                          type: boolean
                        objectType:
                          description: Object type. One of server, disk, turbo.
                          type: string
                        orderId:
                          description: Order ID.
                          type: string
                        periodNum:
                          type: number
                        periodType:
                          description: Type of the resource to be backed up. Possible
                            values are OS::Nova::Server, OS::Cinder::Volume and OS::Sfs::Turbo.
                          type: string
                        productId:
                          description: Product ID.
                          type: string
                        protectType:
                          description: Operation type. One of backup, replication
                          type: string
                        size:
                          description: Capacity, in GB. Minimum 1, maximum 10485760
                          type: number
                        specCode:
                          description: Specification code.
                          type: string
                        status:
                          description: Vault status.
                          type: string
                        storageUnit:
                          description: Name of the bucket for the vault.
                          type: string
                        used:
                          description: Used capacity, in MB.
                          type: number
                      type: object
                    type: array
                  bindRules:
                    description: |-
                      Tag map, a rules for automatic association. You can only select tag keys and values from
                      the existing ones. If there are no tags available, go to the corresponding service to create one.
                      You can add a maximum of 5 tags for a search. If more than one tag is added, the backups containing one of the
                      specified tags will be returned.
                    items:
                      properties:
                        key:
                          type: string
                        value:
                          type: string
                      type: object
                    type: array
                  createdAt:
                    type: string
                  description:
                    description: User-defined vault description.
                    type: string
                  enterpriseProjectId:
                    description: ID of the resource to be backed up.
                    type: string
                  id:
                    description: ID of the resource to be backed up.
                    type: string
                  locked:
                    description: |-
                      Specifies whether the vault is locked. A locked vault cannot be unlocked.
                      Defaults to false.
                    type: boolean
                  name:
                    description: Vault name.
                    type: string
                  policy:
                    description: |-
                      Specifies the policy details to associate with the CBR vault.
                      The object structure is documented below.
                      The policy details to associate with the CBR vault.
                    items:
                      properties:
                        destinationVaultId:
                          description: |-
                            Specifies the ID of destination vault to which the replication policy
                            will be associated.
                          type: string
                        id:
                          description: Specifies the policy ID.
                          type: string
                      type: object
                    type: array
                  projectId:
                    description: ID of the resource to be backed up.
                    type: string
                  providerId:
                    description: ID of the resource to be backed up.
                    type: string
                  region:
                    type: string
                  resource:
                    description: 'Associated resources. Multiple. Resource fields:'
                    items:
                      properties:
                        backupCount:
                          type: number
                        backupSize:
                          description: Capacity, in GB. Minimum 1, maximum 10485760
                          type: number
                        excludeVolumes:
                          description: List of excluded volumes.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        id:
                          description: Specifies the policy ID.
                          type: string
                        includeVolumes:
                          description: List of included volumes.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        name:
                          description: Vault name.
                          type: string
                        protectStatus:
                          description: Vault status.
                          type: string
                        size:
                          description: Capacity, in GB. Minimum 1, maximum 10485760
                          type: number
                        type:
                          description: Type of the resource to be backed up. Possible
                            values are OS::Nova::Server, OS::Cinder::Volume and OS::Sfs::Turbo.
                          type: string
                      type: object
                    type: array
                  tags:
                    additionalProperties:
                      type: string
                    description: Tag map.
                    type: object
                    x-kubernetes-map-type: granular
                  userId:
                    description: ID of the resource to be backed up.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
