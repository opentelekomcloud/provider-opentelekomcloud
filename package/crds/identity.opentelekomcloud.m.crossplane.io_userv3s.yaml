---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: userv3s.identity.opentelekomcloud.m.crossplane.io
spec:
  group: identity.opentelekomcloud.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - opentelekomcloud
    kind: UserV3
    listKind: UserV3List
    plural: userv3s
    singular: userv3
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: UserV3 is the Schema for the UserV3s API. Manages a IAM User
          resource within OpenTelekomCloud.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: UserV3Spec defines the desired state of UserV3
            properties:
              forProvider:
                properties:
                  accessType:
                    description: 'Specifies the access type of the user. Available
                      values are:'
                    type: string
                  countryCode:
                    description: Specifies the country code. This parameter must be
                      used together with phone.
                    type: string
                  description:
                    description: Specifies the description of the user.
                    type: string
                  email:
                    description: Specifies the email address with a maximum of 255
                      characters.
                    type: string
                  enabled:
                    description: Specifies whether the user is enabled or disabled.
                      Valid values are true and false.
                    type: boolean
                  loginProtection:
                    description: |-
                      Login protection configuration.
                      The login_protection block supports:
                    items:
                      properties:
                        enabled:
                          description: Specifies whether the user is enabled or disabled.
                            Valid values are true and false.
                          type: boolean
                        verificationMethod:
                          description: 'Login authentication method of the user. Options:
                            sms, email, and vmfa.'
                          type: string
                      type: object
                    type: array
                  name:
                    description: |-
                      Specifies the name of the user. The user name consists of 5 to 32 characters. It can
                      contain only uppercase letters, lowercase letters, digits, spaces, and special characters (-_) and cannot start with a
                      digit.
                    type: string
                  passwordSecretRef:
                    description: |-
                      Specifies the password for the user with 6 to 32 characters. It must contain at least
                      two of the following character types: uppercase letters, lowercase letters, digits, and special characters.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  phone:
                    description: |-
                      Specifies the mobile number with a maximum of 32 digits. This parameter must be used
                      together with country_code.
                    type: string
                  pwdReset:
                    description: |-
                      Specifies whether the password should be reset. By default, the password is asked
                      to reset at the first login.
                    type: boolean
                  sendWelcomeEmail:
                    description: |-
                      Whether to send a Welcome Email or not.
                      Possible values are true and false.
                    type: boolean
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  accessType:
                    description: 'Specifies the access type of the user. Available
                      values are:'
                    type: string
                  countryCode:
                    description: Specifies the country code. This parameter must be
                      used together with phone.
                    type: string
                  description:
                    description: Specifies the description of the user.
                    type: string
                  email:
                    description: Specifies the email address with a maximum of 255
                      characters.
                    type: string
                  enabled:
                    description: Specifies whether the user is enabled or disabled.
                      Valid values are true and false.
                    type: boolean
                  loginProtection:
                    description: |-
                      Login protection configuration.
                      The login_protection block supports:
                    items:
                      properties:
                        enabled:
                          description: Specifies whether the user is enabled or disabled.
                            Valid values are true and false.
                          type: boolean
                        verificationMethod:
                          description: 'Login authentication method of the user. Options:
                            sms, email, and vmfa.'
                          type: string
                      type: object
                    type: array
                  name:
                    description: |-
                      Specifies the name of the user. The user name consists of 5 to 32 characters. It can
                      contain only uppercase letters, lowercase letters, digits, spaces, and special characters (-_) and cannot start with a
                      digit.
                    type: string
                  passwordSecretRef:
                    description: |-
                      Specifies the password for the user with 6 to 32 characters. It must contain at least
                      two of the following character types: uppercase letters, lowercase letters, digits, and special characters.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  phone:
                    description: |-
                      Specifies the mobile number with a maximum of 32 digits. This parameter must be used
                      together with country_code.
                    type: string
                  pwdReset:
                    description: |-
                      Specifies whether the password should be reset. By default, the password is asked
                      to reset at the first login.
                    type: boolean
                  sendWelcomeEmail:
                    description: |-
                      Whether to send a Welcome Email or not.
                      Possible values are true and false.
                    type: boolean
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: UserV3Status defines the observed state of UserV3.
            properties:
              atProvider:
                properties:
                  accessType:
                    description: 'Specifies the access type of the user. Available
                      values are:'
                    type: string
                  countryCode:
                    description: Specifies the country code. This parameter must be
                      used together with phone.
                    type: string
                  createTime:
                    description: The time when the IAM user was created.
                    type: string
                  description:
                    description: Specifies the description of the user.
                    type: string
                  domainId:
                    description: The domain user belongs to.
                    type: string
                  email:
                    description: Specifies the email address with a maximum of 255
                      characters.
                    type: string
                  enabled:
                    description: Specifies whether the user is enabled or disabled.
                      Valid values are true and false.
                    type: boolean
                  id:
                    description: The resource ID in UUID format.
                    type: string
                  lastLogin:
                    description: The time when the IAM user last login.
                    type: string
                  loginProtection:
                    description: |-
                      Login protection configuration.
                      The login_protection block supports:
                    items:
                      properties:
                        enabled:
                          description: Specifies whether the user is enabled or disabled.
                            Valid values are true and false.
                          type: boolean
                        verificationMethod:
                          description: 'Login authentication method of the user. Options:
                            sms, email, and vmfa.'
                          type: string
                      type: object
                    type: array
                  name:
                    description: |-
                      Specifies the name of the user. The user name consists of 5 to 32 characters. It can
                      contain only uppercase letters, lowercase letters, digits, spaces, and special characters (-_) and cannot start with a
                      digit.
                    type: string
                  passwordStrength:
                    description: Indicates the password strength.
                    type: string
                  phone:
                    description: |-
                      Specifies the mobile number with a maximum of 32 digits. This parameter must be used
                      together with country_code.
                    type: string
                  pwdReset:
                    description: |-
                      Specifies whether the password should be reset. By default, the password is asked
                      to reset at the first login.
                    type: boolean
                  sendWelcomeEmail:
                    description: |-
                      Whether to send a Welcome Email or not.
                      Possible values are true and false.
                    type: boolean
                  xuserId:
                    description: ID of the user in the external system.
                    type: string
                  xuserType:
                    description: Type of the user in the external system.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
