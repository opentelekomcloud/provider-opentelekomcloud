---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: clusterv1s.css.opentelekomcloud.m.crossplane.io
spec:
  group: css.opentelekomcloud.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - opentelekomcloud
    kind: ClusterV1
    listKind: ClusterV1List
    plural: clusterv1s
    singular: clusterv1
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterV1 is the Schema for the ClusterV1s API. Manages a CSS
          Cluster resource within OpenTelekomCloud.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClusterV1Spec defines the desired state of ClusterV1
            properties:
              forProvider:
                properties:
                  adminPass:
                    description: |-
                      Password of the cluster user admin in security mode.
                      This parameter is mandatory only when enable_authority is set to true.
                    type: string
                  backupStrategy:
                    description: Specifies the advanced backup policy. Structure is
                      documented below.
                    items:
                      properties:
                        keepDays:
                          description: |-
                            Specifies the number of days to retain the generated snapshots. Snapshots are reserved
                            for seven days by default.
                          type: number
                        prefix:
                          description: Specifies the prefix of the snapshot that is
                            automatically created.
                          type: string
                        startTime:
                          description: |-
                            Specifies the time when a snapshot is automatically created everyday. Snapshots can
                            only be created on the hour. The time format is the time followed by the time zone, specifically, HH:mm z. In the
                            format, HH:mm refers to the hour time and z refers to the time zone. For example, "00:00 GMT+08:00"
                            and "01:00 GMT+08:00".
                          type: string
                      type: object
                    type: array
                  datastore:
                    description: |-
                      Type of the data search engine. Structure is documented below.
                      Changing this parameter will create a new resource.
                    items:
                      properties:
                        type:
                          description: Engine type. The default value is elasticsearch.
                            Currently, the value can be elasticsearch or opensearch.
                          type: string
                        version:
                          description: |-
                            Engine version. The value can be 7.6.2, 7.9.3, 7.10.2 or 1.3.6 for opensearch.
                            The default value is 7.6.2.
                          type: string
                      type: object
                    type: array
                  enableAuthority:
                    description: |-
                      Whether to enable authentication.
                      Authentication is disabled by default.
                      When authentication is enabled, enable_https must be set to true.
                      Changing this parameter will create a new resource.
                    type: boolean
                  enableHttps:
                    description: |-
                      Whether communication encryption is performed on the cluster.
                      By default, communication encryption is disabled.
                      Value true indicates that communication encryption is performed on the cluster.
                      Value false indicates that communication encryption is not performed on the cluster.
                      Changing this parameter will create a new resource.
                    type: boolean
                  expectNodeNum:
                    description: Number of cluster instances. The value range is 1
                      to 32.
                    type: number
                  name:
                    description: |-
                      Cluster name. It contains 4 to 32 characters. Only letters, digits,
                      hyphens (-), and underscores (_) are allowed. The value must start with a letter.
                      Changing this parameter will create a new resource.
                    type: string
                  nodeConfig:
                    description: |-
                      Instance object. Structure is documented below.
                      Changing this parameter will create a new resource.
                    items:
                      properties:
                        availabilityZone:
                          description: Availability zone (AZ). Changing this parameter
                            will create a new resource.
                          type: string
                        flavor:
                          description: Instance flavor name.
                          type: string
                        networkInfo:
                          description: |-
                            Network information. Structure is documented below.
                            Changing this parameter will create a new resource.
                          items:
                            properties:
                              networkId:
                                description: |-
                                  Network ID. All instances in a cluster must have the same
                                  networks and security groups. Changing this parameter will create a new resource.
                                type: string
                              securityGroupId:
                                description: |-
                                  Security group ID. All instances in a cluster must have the
                                  same subnets and security groups. Changing this parameter will create a new resource.
                                type: string
                              vpcId:
                                description: |-
                                  VPC ID, which is used for configuring cluster network.
                                  Changing this parameter will create a new resource.
                                type: string
                            type: object
                          type: array
                        volume:
                          description: |-
                            Information about the volume. Structure is documented below.
                            Changing this parameter will create a new resource.
                          items:
                            properties:
                              encryptionKey:
                                description: |-
                                  Key ID. The Default Master Keys cannot be used to create
                                  grants. Specifically, you cannot use Default Master Keys
                                  whose aliases end with /default in KMS to create clusters.
                                  After a cluster is created, do not delete the key used by the cluster.
                                  Otherwise, the cluster will become unavailable.
                                  Changing this parameter will create a new resource.
                                type: string
                              size:
                                description: Volume size, which must be a multiple
                                  of 4 and 10.
                                type: number
                              volumeType:
                                description: |-
                                  COMMON: Common I/O. The SATA disk is used. HIGH: High I/O.
                                  The SAS disk is used. ULTRAHIGH: Ultra-high I/O. The solid-state drive (SSD) is used.
                                  Changing this parameter will create a new resource.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  publicAccess:
                    description: Specifies the public network access information.
                    items:
                      properties:
                        bandwidth:
                          description: Specifies the public network bandwidth.
                          type: number
                        whitelist:
                          description: |-
                            Specifies the whitelist of Kibana access control.
                            Separate the whitelisted network segments or IP addresses with commas (,), and each of them must be unique.
                          type: string
                        whitelistEnabled:
                          description: Specifies whether to enable the Kibana access
                            control.
                          type: boolean
                      type: object
                    type: array
                  tags:
                    additionalProperties:
                      type: string
                    description: Tags key/value pairs to associate with the cluster.
                    type: object
                    x-kubernetes-map-type: granular
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  adminPass:
                    description: |-
                      Password of the cluster user admin in security mode.
                      This parameter is mandatory only when enable_authority is set to true.
                    type: string
                  backupStrategy:
                    description: Specifies the advanced backup policy. Structure is
                      documented below.
                    items:
                      properties:
                        keepDays:
                          description: |-
                            Specifies the number of days to retain the generated snapshots. Snapshots are reserved
                            for seven days by default.
                          type: number
                        prefix:
                          description: Specifies the prefix of the snapshot that is
                            automatically created.
                          type: string
                        startTime:
                          description: |-
                            Specifies the time when a snapshot is automatically created everyday. Snapshots can
                            only be created on the hour. The time format is the time followed by the time zone, specifically, HH:mm z. In the
                            format, HH:mm refers to the hour time and z refers to the time zone. For example, "00:00 GMT+08:00"
                            and "01:00 GMT+08:00".
                          type: string
                      type: object
                    type: array
                  datastore:
                    description: |-
                      Type of the data search engine. Structure is documented below.
                      Changing this parameter will create a new resource.
                    items:
                      properties:
                        type:
                          description: Engine type. The default value is elasticsearch.
                            Currently, the value can be elasticsearch or opensearch.
                          type: string
                        version:
                          description: |-
                            Engine version. The value can be 7.6.2, 7.9.3, 7.10.2 or 1.3.6 for opensearch.
                            The default value is 7.6.2.
                          type: string
                      type: object
                    type: array
                  enableAuthority:
                    description: |-
                      Whether to enable authentication.
                      Authentication is disabled by default.
                      When authentication is enabled, enable_https must be set to true.
                      Changing this parameter will create a new resource.
                    type: boolean
                  enableHttps:
                    description: |-
                      Whether communication encryption is performed on the cluster.
                      By default, communication encryption is disabled.
                      Value true indicates that communication encryption is performed on the cluster.
                      Value false indicates that communication encryption is not performed on the cluster.
                      Changing this parameter will create a new resource.
                    type: boolean
                  expectNodeNum:
                    description: Number of cluster instances. The value range is 1
                      to 32.
                    type: number
                  name:
                    description: |-
                      Cluster name. It contains 4 to 32 characters. Only letters, digits,
                      hyphens (-), and underscores (_) are allowed. The value must start with a letter.
                      Changing this parameter will create a new resource.
                    type: string
                  nodeConfig:
                    description: |-
                      Instance object. Structure is documented below.
                      Changing this parameter will create a new resource.
                    items:
                      properties:
                        availabilityZone:
                          description: Availability zone (AZ). Changing this parameter
                            will create a new resource.
                          type: string
                        flavor:
                          description: Instance flavor name.
                          type: string
                        networkInfo:
                          description: |-
                            Network information. Structure is documented below.
                            Changing this parameter will create a new resource.
                          items:
                            properties:
                              networkId:
                                description: |-
                                  Network ID. All instances in a cluster must have the same
                                  networks and security groups. Changing this parameter will create a new resource.
                                type: string
                              securityGroupId:
                                description: |-
                                  Security group ID. All instances in a cluster must have the
                                  same subnets and security groups. Changing this parameter will create a new resource.
                                type: string
                              vpcId:
                                description: |-
                                  VPC ID, which is used for configuring cluster network.
                                  Changing this parameter will create a new resource.
                                type: string
                            type: object
                          type: array
                        volume:
                          description: |-
                            Information about the volume. Structure is documented below.
                            Changing this parameter will create a new resource.
                          items:
                            properties:
                              encryptionKey:
                                description: |-
                                  Key ID. The Default Master Keys cannot be used to create
                                  grants. Specifically, you cannot use Default Master Keys
                                  whose aliases end with /default in KMS to create clusters.
                                  After a cluster is created, do not delete the key used by the cluster.
                                  Otherwise, the cluster will become unavailable.
                                  Changing this parameter will create a new resource.
                                type: string
                              size:
                                description: Volume size, which must be a multiple
                                  of 4 and 10.
                                type: number
                              volumeType:
                                description: |-
                                  COMMON: Common I/O. The SATA disk is used. HIGH: High I/O.
                                  The SAS disk is used. ULTRAHIGH: Ultra-high I/O. The solid-state drive (SSD) is used.
                                  Changing this parameter will create a new resource.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  publicAccess:
                    description: Specifies the public network access information.
                    items:
                      properties:
                        bandwidth:
                          description: Specifies the public network bandwidth.
                          type: number
                        whitelist:
                          description: |-
                            Specifies the whitelist of Kibana access control.
                            Separate the whitelisted network segments or IP addresses with commas (,), and each of them must be unique.
                          type: string
                        whitelistEnabled:
                          description: Specifies whether to enable the Kibana access
                            control.
                          type: boolean
                      type: object
                    type: array
                  tags:
                    additionalProperties:
                      type: string
                    description: Tags key/value pairs to associate with the cluster.
                    type: object
                    x-kubernetes-map-type: granular
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.nodeConfig is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.nodeConfig)
                || (has(self.initProvider) && has(self.initProvider.nodeConfig))'
          status:
            description: ClusterV1Status defines the observed state of ClusterV1.
            properties:
              atProvider:
                properties:
                  adminPass:
                    description: |-
                      Password of the cluster user admin in security mode.
                      This parameter is mandatory only when enable_authority is set to true.
                    type: string
                  backupAvailable:
                    description: Indicates whether the snapshot function is enabled.
                    type: boolean
                  backupStrategy:
                    description: Specifies the advanced backup policy. Structure is
                      documented below.
                    items:
                      properties:
                        keepDays:
                          description: |-
                            Specifies the number of days to retain the generated snapshots. Snapshots are reserved
                            for seven days by default.
                          type: number
                        prefix:
                          description: Specifies the prefix of the snapshot that is
                            automatically created.
                          type: string
                        startTime:
                          description: |-
                            Specifies the time when a snapshot is automatically created everyday. Snapshots can
                            only be created on the hour. The time format is the time followed by the time zone, specifically, HH:mm z. In the
                            format, HH:mm refers to the hour time and z refers to the time zone. For example, "00:00 GMT+08:00"
                            and "01:00 GMT+08:00".
                          type: string
                      type: object
                    type: array
                  created:
                    description: 'Time when a cluster is created. The format is ISO8601:
                      CCYY-MM-DDThh:mm:ss.'
                    type: string
                  datastore:
                    description: |-
                      Type of the data search engine. Structure is documented below.
                      Changing this parameter will create a new resource.
                    items:
                      properties:
                        type:
                          description: Engine type. The default value is elasticsearch.
                            Currently, the value can be elasticsearch or opensearch.
                          type: string
                        version:
                          description: |-
                            Engine version. The value can be 7.6.2, 7.9.3, 7.10.2 or 1.3.6 for opensearch.
                            The default value is 7.6.2.
                          type: string
                      type: object
                    type: array
                  enableAuthority:
                    description: |-
                      Whether to enable authentication.
                      Authentication is disabled by default.
                      When authentication is enabled, enable_https must be set to true.
                      Changing this parameter will create a new resource.
                    type: boolean
                  enableHttps:
                    description: |-
                      Whether communication encryption is performed on the cluster.
                      By default, communication encryption is disabled.
                      Value true indicates that communication encryption is performed on the cluster.
                      Value false indicates that communication encryption is not performed on the cluster.
                      Changing this parameter will create a new resource.
                    type: boolean
                  endpoint:
                    description: Indicates the IP address and port number of the user
                      used to access the VPC.
                    type: string
                  expectNodeNum:
                    description: Number of cluster instances. The value range is 1
                      to 32.
                    type: number
                  id:
                    description: Instance ID.
                    type: string
                  name:
                    description: |-
                      Cluster name. It contains 4 to 32 characters. Only letters, digits,
                      hyphens (-), and underscores (_) are allowed. The value must start with a letter.
                      Changing this parameter will create a new resource.
                    type: string
                  nodeConfig:
                    description: |-
                      Instance object. Structure is documented below.
                      Changing this parameter will create a new resource.
                    items:
                      properties:
                        availabilityZone:
                          description: Availability zone (AZ). Changing this parameter
                            will create a new resource.
                          type: string
                        flavor:
                          description: Instance flavor name.
                          type: string
                        networkInfo:
                          description: |-
                            Network information. Structure is documented below.
                            Changing this parameter will create a new resource.
                          items:
                            properties:
                              networkId:
                                description: |-
                                  Network ID. All instances in a cluster must have the same
                                  networks and security groups. Changing this parameter will create a new resource.
                                type: string
                              securityGroupId:
                                description: |-
                                  Security group ID. All instances in a cluster must have the
                                  same subnets and security groups. Changing this parameter will create a new resource.
                                type: string
                              vpcId:
                                description: |-
                                  VPC ID, which is used for configuring cluster network.
                                  Changing this parameter will create a new resource.
                                type: string
                            type: object
                          type: array
                        volume:
                          description: |-
                            Information about the volume. Structure is documented below.
                            Changing this parameter will create a new resource.
                          items:
                            properties:
                              encryptionKey:
                                description: |-
                                  Key ID. The Default Master Keys cannot be used to create
                                  grants. Specifically, you cannot use Default Master Keys
                                  whose aliases end with /default in KMS to create clusters.
                                  After a cluster is created, do not delete the key used by the cluster.
                                  Otherwise, the cluster will become unavailable.
                                  Changing this parameter will create a new resource.
                                type: string
                              size:
                                description: Volume size, which must be a multiple
                                  of 4 and 10.
                                type: number
                              volumeType:
                                description: |-
                                  COMMON: Common I/O. The SATA disk is used. HIGH: High I/O.
                                  The SAS disk is used. ULTRAHIGH: Ultra-high I/O. The solid-state drive (SSD) is used.
                                  Changing this parameter will create a new resource.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  nodes:
                    description: List of node objects. Structure is documented below.
                    items:
                      properties:
                        id:
                          description: Instance ID.
                          type: string
                        name:
                          description: Instance name.
                          type: string
                        type:
                          description: 'Supported type: ess (indicating the Elasticsearch
                            node)'
                          type: string
                      type: object
                    type: array
                  publicAccess:
                    description: Specifies the public network access information.
                    items:
                      properties:
                        bandwidth:
                          description: Specifies the public network bandwidth.
                          type: number
                        publicIp:
                          type: string
                        whitelist:
                          description: |-
                            Specifies the whitelist of Kibana access control.
                            Separate the whitelisted network segments or IP addresses with commas (,), and each of them must be unique.
                          type: string
                        whitelistEnabled:
                          description: Specifies whether to enable the Kibana access
                            control.
                          type: boolean
                      type: object
                    type: array
                  tags:
                    additionalProperties:
                      type: string
                    description: Tags key/value pairs to associate with the cluster.
                    type: object
                    x-kubernetes-map-type: granular
                  updated:
                    description: 'Last modification time of a cluster. The format
                      is ISO8601: CCYY-MM-DDThh:mm:ss.'
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
