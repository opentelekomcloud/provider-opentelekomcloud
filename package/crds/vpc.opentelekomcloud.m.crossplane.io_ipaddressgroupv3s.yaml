---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: ipaddressgroupv3s.vpc.opentelekomcloud.m.crossplane.io
spec:
  group: vpc.opentelekomcloud.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - opentelekomcloud
    kind: IPAddressGroupV3
    listKind: IPAddressGroupV3List
    plural: ipaddressgroupv3s
    singular: ipaddressgroupv3
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: IPAddressGroupV3 is the Schema for the IPAddressGroupV3s API.
          Manages a VPC IP Address Group resource within OpenTelekomCloud.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: IPAddressGroupV3Spec defines the desired state of IPAddressGroupV3
            properties:
              forProvider:
                properties:
                  description:
                    description: |-
                      Description about the IP address group.
                      The value can contain up to 255 characters and cannot contain angle brackets (< or >).
                    type: string
                  enterpriseProjectId:
                    description: |-
                      ID of the enterprise project to which the IP address group belongs.
                      Default: 0.
                    type: string
                  ipExtraSet:
                    description: |-
                      IP address entries with remarks.
                      The ip_set and ip_extra_set parameters cannot be both specified in a request.
                      Structure is documented below.
                    items:
                      properties:
                        ip:
                          description: IP address entry. Can be a single IP address,
                            an IP address range, or a CIDR block.
                          type: string
                        remarks:
                          description: |-
                            Remarks of the IP address entry.
                            The value can contain 0 to 255 characters and cannot contain angle brackets (< or >).
                          type: string
                      type: object
                    type: array
                  ipSet:
                    description: |-
                      IP address entries in the IP address group.
                      Both IPv4 and IPv6 addresses are supported. An IP address entry can be:
                    items:
                      type: string
                    type: array
                  ipVersion:
                    description: |-
                      IP address version of the IP address group.
                      Valid values are 4 (IPv4) and 6 (IPv6).
                    type: number
                  maxCapacity:
                    description: |-
                      Maximum number of IP address entries in the IP address group.
                      Range: 0 to 20. Default: 0.
                    type: number
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  description:
                    description: |-
                      Description about the IP address group.
                      The value can contain up to 255 characters and cannot contain angle brackets (< or >).
                    type: string
                  enterpriseProjectId:
                    description: |-
                      ID of the enterprise project to which the IP address group belongs.
                      Default: 0.
                    type: string
                  ipExtraSet:
                    description: |-
                      IP address entries with remarks.
                      The ip_set and ip_extra_set parameters cannot be both specified in a request.
                      Structure is documented below.
                    items:
                      properties:
                        ip:
                          description: IP address entry. Can be a single IP address,
                            an IP address range, or a CIDR block.
                          type: string
                        remarks:
                          description: |-
                            Remarks of the IP address entry.
                            The value can contain 0 to 255 characters and cannot contain angle brackets (< or >).
                          type: string
                      type: object
                    type: array
                  ipSet:
                    description: |-
                      IP address entries in the IP address group.
                      Both IPv4 and IPv6 addresses are supported. An IP address entry can be:
                    items:
                      type: string
                    type: array
                  ipVersion:
                    description: |-
                      IP address version of the IP address group.
                      Valid values are 4 (IPv4) and 6 (IPv6).
                    type: number
                  maxCapacity:
                    description: |-
                      Maximum number of IP address entries in the IP address group.
                      Range: 0 to 20. Default: 0.
                    type: number
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.ipVersion is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.ipVersion)
                || (has(self.initProvider) && has(self.initProvider.ipVersion))'
          status:
            description: IPAddressGroupV3Status defines the observed state of IPAddressGroupV3.
            properties:
              atProvider:
                properties:
                  createdAt:
                    description: |-
                      Indicates the time when the IP address group was created.
                      It is a UTC time in the format of yyyy-MM-ddTHH:mm:ss.
                    type: string
                  description:
                    description: |-
                      Description about the IP address group.
                      The value can contain up to 255 characters and cannot contain angle brackets (< or >).
                    type: string
                  enterpriseProjectId:
                    description: |-
                      ID of the enterprise project to which the IP address group belongs.
                      Default: 0.
                    type: string
                  id:
                    description: IP address group ID.
                    type: string
                  ipExtraSet:
                    description: |-
                      IP address entries with remarks.
                      The ip_set and ip_extra_set parameters cannot be both specified in a request.
                      Structure is documented below.
                    items:
                      properties:
                        ip:
                          description: IP address entry. Can be a single IP address,
                            an IP address range, or a CIDR block.
                          type: string
                        remarks:
                          description: |-
                            Remarks of the IP address entry.
                            The value can contain 0 to 255 characters and cannot contain angle brackets (< or >).
                          type: string
                      type: object
                    type: array
                  ipSet:
                    description: |-
                      IP address entries in the IP address group.
                      Both IPv4 and IPv6 addresses are supported. An IP address entry can be:
                    items:
                      type: string
                    type: array
                  ipVersion:
                    description: |-
                      IP address version of the IP address group.
                      Valid values are 4 (IPv4) and 6 (IPv6).
                    type: number
                  maxCapacity:
                    description: |-
                      Maximum number of IP address entries in the IP address group.
                      Range: 0 to 20. Default: 0.
                    type: number
                  projectId:
                    description: Indicates the project ID that the IP address group
                      belongs to.
                    type: string
                  status:
                    description: |-
                      Status of the IP address group.
                      Valid values are NORMAL (normal), UPDATING (being updated), and UPDATE_FAILED (update failed).
                    type: string
                  statusMessage:
                    description: Details about the IP address group status.
                    type: string
                  updatedAt:
                    description: |-
                      Indicates the time when the IP address group was last updated.
                      It is a UTC time in the format of yyyy-MM-ddTHH:mm:ss.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
