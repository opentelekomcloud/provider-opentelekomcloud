---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: turbosharev1s.sfs.opentelekomcloud.crossplane.io
spec:
  group: sfs.opentelekomcloud.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - opentelekomcloud
    kind: TurboShareV1
    listKind: TurboShareV1List
    plural: turbosharev1s
    singular: turbosharev1
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TurboShareV1 is the Schema for the TurboShareV1s API. Manages
          an SFS Turbo Share resource within OpenTelekomCloud.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TurboShareV1Spec defines the desired state of TurboShareV1
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  availabilityZone:
                    description: |-
                      Specifies the availability zone where the file system is located.
                      Changing this will create a new resource.
                    type: string
                  cryptKeyId:
                    description: |-
                      Specifies the ID of a KMS key to encrypt the file system.
                      Changing this will create a new resource.
                    type: string
                  enhanced:
                    type: boolean
                  expandType:
                    description: 'Specifies the extension type. This parameter is
                      mandatory when you are creating an SFS Turbo 1,000 MB/s/TiB,
                      500 MB/s/TiB, 250 MB/s/TiB, 125 MB/s/TiB, 40 MB/s/TiB, 20 MB/s/TiB,
                      or Enhanced file system. Accepted values:'
                    type: string
                  hpcBw:
                    description: 'Specifies the file system bandwidth. This parameter
                      is mandatory when you are creating an SFS Turbo 1,000 MB/s/TiB,
                      500 MB/s/TiB, 250 MB/s/TiB, 125 MB/s/TiB, 40 MB/s/TiB, 20 MB/s/TiB
                      file system. Accepted values: 20M (for a 20 MB/s/TiB file system),
                      40M (for a 40 MB/s/TiB file system), 125M (for a 125 MB/s/TiB
                      file system), 250M (for a 250 MB/s/TiB file system), 500M (for
                      a 500 MB/s/TiB file system), and 1000M (for a 1000 MB/s/TiB
                      file system).'
                    type: string
                  name:
                    description: |-
                      Specifies the name of an SFS Turbo file system. The value contains 4 to 64
                      characters and must start with a letter. Changing this will create a new resource.
                    type: string
                  region:
                    description: |-
                      The region in which to create the SFS Turbo resource. If omitted, the
                      provider-level region will be used. Changing this creates a new SFS Turbo resource.
                    type: string
                  securityGroupId:
                    description: Specifies the security group ID.
                    type: string
                  shareProto:
                    description: |-
                      Specifies the protocol for sharing file systems. The valid value is NFS.
                      Changing this will create a new resource.
                    type: string
                  shareType:
                    description: |-
                      Specifies the file system type.
                      For a previous-generation SFS Turbo file system, specify STANDARD for a Standard or Standard-Enhanced file system, and PERFORMANCE for a Performance or Performance-Enhanced file system.
                      For a 1,000 MB/s/TiB, 500 MB/s/TiB, 250 MB/s/TiB, 125 MB/s/TiB, 40 MB/s/TiB, or 20 MB/s/TiB file system, this field is not verified. Specify either STANDARD or PERFORMANCE.
                      Changing this will create a new resource.
                    type: string
                  size:
                    description: |-
                      Specifies the capacity of a file system, in GiB.
                      For a previous-generation SFS Turbo file system, the capacity ranges from 500 to 32768, in GiB.
                      For a previous-generation SFS Turbo file system with expand_type set to bandwidth in metadata, the capacity ranges from 10240 to 327680, in GiB.
                      For a 20 MB/s/TiB file system with expand_type set to hpc and hpc_bw set to 20M in metadata, the capacity ranges from 3686 to 1048576, in GiB, and must be a multiple of 1.2 TiB.
                      For a 40 MB/s/TiB file system with expand_type set to hpc and hpc_bw set to 40M in metadata, the capacity ranges from 1228 to 1048576, in GiB, and must be a multiple of 1.2 TiB.
                      For a 125 MB/s/TiB file system with expand_type set to hpc and hpc_bw set to 125M in metadata, the capacity ranges from 1228 to 1048576, in GiB, and must be a multiple of 1.2 TiB.
                      For a 250 MB/s/TiB file system with expand_type set to hpc and hpc_bw set to 250M in metadata, the capacity ranges from 1228 to 1048576, in GiB, and must be a multiple of 1.2 TiB.
                      For a 500 MB/s/TiB file system with expand_type set to hpc and hpc_bw set to 500M in metadata, the capacity ranges from 1228 to 1048576, in GiB, and must be a multiple of 1.2 TiB.
                      For a 1,000 MB/s/TiB file system with expand_type set to hpc and hpc_bw set to 1000M in metadata, the capacity ranges from 1228 to 1048576, in GiB, and must be a multiple of 1.2 TiB.
                      The desired capacity must be converted to GiB and rounded down to the nearest integer. For example, specify 3686 GiB for a 3.6 TiB file system, 4915 GiB for a 4.8 TiB file system, and 8601 GiB for an 8.4 TiB file system.
                    type: number
                  subnetId:
                    description: Specifies the network ID of the subnet. Changing
                      this will create a new resource.
                    type: string
                  vpcId:
                    description: Specifies the VPC ID. Changing this will create a
                      new resource.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  availabilityZone:
                    description: |-
                      Specifies the availability zone where the file system is located.
                      Changing this will create a new resource.
                    type: string
                  cryptKeyId:
                    description: |-
                      Specifies the ID of a KMS key to encrypt the file system.
                      Changing this will create a new resource.
                    type: string
                  enhanced:
                    type: boolean
                  expandType:
                    description: 'Specifies the extension type. This parameter is
                      mandatory when you are creating an SFS Turbo 1,000 MB/s/TiB,
                      500 MB/s/TiB, 250 MB/s/TiB, 125 MB/s/TiB, 40 MB/s/TiB, 20 MB/s/TiB,
                      or Enhanced file system. Accepted values:'
                    type: string
                  hpcBw:
                    description: 'Specifies the file system bandwidth. This parameter
                      is mandatory when you are creating an SFS Turbo 1,000 MB/s/TiB,
                      500 MB/s/TiB, 250 MB/s/TiB, 125 MB/s/TiB, 40 MB/s/TiB, 20 MB/s/TiB
                      file system. Accepted values: 20M (for a 20 MB/s/TiB file system),
                      40M (for a 40 MB/s/TiB file system), 125M (for a 125 MB/s/TiB
                      file system), 250M (for a 250 MB/s/TiB file system), 500M (for
                      a 500 MB/s/TiB file system), and 1000M (for a 1000 MB/s/TiB
                      file system).'
                    type: string
                  name:
                    description: |-
                      Specifies the name of an SFS Turbo file system. The value contains 4 to 64
                      characters and must start with a letter. Changing this will create a new resource.
                    type: string
                  region:
                    description: |-
                      The region in which to create the SFS Turbo resource. If omitted, the
                      provider-level region will be used. Changing this creates a new SFS Turbo resource.
                    type: string
                  securityGroupId:
                    description: Specifies the security group ID.
                    type: string
                  shareProto:
                    description: |-
                      Specifies the protocol for sharing file systems. The valid value is NFS.
                      Changing this will create a new resource.
                    type: string
                  shareType:
                    description: |-
                      Specifies the file system type.
                      For a previous-generation SFS Turbo file system, specify STANDARD for a Standard or Standard-Enhanced file system, and PERFORMANCE for a Performance or Performance-Enhanced file system.
                      For a 1,000 MB/s/TiB, 500 MB/s/TiB, 250 MB/s/TiB, 125 MB/s/TiB, 40 MB/s/TiB, or 20 MB/s/TiB file system, this field is not verified. Specify either STANDARD or PERFORMANCE.
                      Changing this will create a new resource.
                    type: string
                  size:
                    description: |-
                      Specifies the capacity of a file system, in GiB.
                      For a previous-generation SFS Turbo file system, the capacity ranges from 500 to 32768, in GiB.
                      For a previous-generation SFS Turbo file system with expand_type set to bandwidth in metadata, the capacity ranges from 10240 to 327680, in GiB.
                      For a 20 MB/s/TiB file system with expand_type set to hpc and hpc_bw set to 20M in metadata, the capacity ranges from 3686 to 1048576, in GiB, and must be a multiple of 1.2 TiB.
                      For a 40 MB/s/TiB file system with expand_type set to hpc and hpc_bw set to 40M in metadata, the capacity ranges from 1228 to 1048576, in GiB, and must be a multiple of 1.2 TiB.
                      For a 125 MB/s/TiB file system with expand_type set to hpc and hpc_bw set to 125M in metadata, the capacity ranges from 1228 to 1048576, in GiB, and must be a multiple of 1.2 TiB.
                      For a 250 MB/s/TiB file system with expand_type set to hpc and hpc_bw set to 250M in metadata, the capacity ranges from 1228 to 1048576, in GiB, and must be a multiple of 1.2 TiB.
                      For a 500 MB/s/TiB file system with expand_type set to hpc and hpc_bw set to 500M in metadata, the capacity ranges from 1228 to 1048576, in GiB, and must be a multiple of 1.2 TiB.
                      For a 1,000 MB/s/TiB file system with expand_type set to hpc and hpc_bw set to 1000M in metadata, the capacity ranges from 1228 to 1048576, in GiB, and must be a multiple of 1.2 TiB.
                      The desired capacity must be converted to GiB and rounded down to the nearest integer. For example, specify 3686 GiB for a 3.6 TiB file system, 4915 GiB for a 4.8 TiB file system, and 8601 GiB for an 8.4 TiB file system.
                    type: number
                  subnetId:
                    description: Specifies the network ID of the subnet. Changing
                      this will create a new resource.
                    type: string
                  vpcId:
                    description: Specifies the VPC ID. Changing this will create a
                      new resource.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.availabilityZone is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.availabilityZone)
                || (has(self.initProvider) && has(self.initProvider.availabilityZone))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.securityGroupId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.securityGroupId)
                || (has(self.initProvider) && has(self.initProvider.securityGroupId))'
            - message: spec.forProvider.size is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.size)
                || (has(self.initProvider) && has(self.initProvider.size))'
            - message: spec.forProvider.subnetId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.subnetId)
                || (has(self.initProvider) && has(self.initProvider.subnetId))'
            - message: spec.forProvider.vpcId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.vpcId)
                || (has(self.initProvider) && has(self.initProvider.vpcId))'
          status:
            description: TurboShareV1Status defines the observed state of TurboShareV1.
            properties:
              atProvider:
                properties:
                  availabilityZone:
                    description: |-
                      Specifies the availability zone where the file system is located.
                      Changing this will create a new resource.
                    type: string
                  availableCapacity:
                    description: The available capacity of the SFS Turbo file system
                      in the unit of GB.
                    type: string
                  cryptKeyId:
                    description: |-
                      Specifies the ID of a KMS key to encrypt the file system.
                      Changing this will create a new resource.
                    type: string
                  enhanced:
                    type: boolean
                  expandType:
                    description: 'Specifies the extension type. This parameter is
                      mandatory when you are creating an SFS Turbo 1,000 MB/s/TiB,
                      500 MB/s/TiB, 250 MB/s/TiB, 125 MB/s/TiB, 40 MB/s/TiB, 20 MB/s/TiB,
                      or Enhanced file system. Accepted values:'
                    type: string
                  exportLocation:
                    description: The mount point of the SFS Turbo file system.
                    type: string
                  hpcBw:
                    description: 'Specifies the file system bandwidth. This parameter
                      is mandatory when you are creating an SFS Turbo 1,000 MB/s/TiB,
                      500 MB/s/TiB, 250 MB/s/TiB, 125 MB/s/TiB, 40 MB/s/TiB, 20 MB/s/TiB
                      file system. Accepted values: 20M (for a 20 MB/s/TiB file system),
                      40M (for a 40 MB/s/TiB file system), 125M (for a 125 MB/s/TiB
                      file system), 250M (for a 250 MB/s/TiB file system), 500M (for
                      a 500 MB/s/TiB file system), and 1000M (for a 1000 MB/s/TiB
                      file system).'
                    type: string
                  id:
                    description: The UUID of the SFS Turbo file system.
                    type: string
                  name:
                    description: |-
                      Specifies the name of an SFS Turbo file system. The value contains 4 to 64
                      characters and must start with a letter. Changing this will create a new resource.
                    type: string
                  region:
                    description: |-
                      The region in which to create the SFS Turbo resource. If omitted, the
                      provider-level region will be used. Changing this creates a new SFS Turbo resource.
                    type: string
                  securityGroupId:
                    description: Specifies the security group ID.
                    type: string
                  shareProto:
                    description: |-
                      Specifies the protocol for sharing file systems. The valid value is NFS.
                      Changing this will create a new resource.
                    type: string
                  shareType:
                    description: |-
                      Specifies the file system type.
                      For a previous-generation SFS Turbo file system, specify STANDARD for a Standard or Standard-Enhanced file system, and PERFORMANCE for a Performance or Performance-Enhanced file system.
                      For a 1,000 MB/s/TiB, 500 MB/s/TiB, 250 MB/s/TiB, 125 MB/s/TiB, 40 MB/s/TiB, or 20 MB/s/TiB file system, this field is not verified. Specify either STANDARD or PERFORMANCE.
                      Changing this will create a new resource.
                    type: string
                  size:
                    description: |-
                      Specifies the capacity of a file system, in GiB.
                      For a previous-generation SFS Turbo file system, the capacity ranges from 500 to 32768, in GiB.
                      For a previous-generation SFS Turbo file system with expand_type set to bandwidth in metadata, the capacity ranges from 10240 to 327680, in GiB.
                      For a 20 MB/s/TiB file system with expand_type set to hpc and hpc_bw set to 20M in metadata, the capacity ranges from 3686 to 1048576, in GiB, and must be a multiple of 1.2 TiB.
                      For a 40 MB/s/TiB file system with expand_type set to hpc and hpc_bw set to 40M in metadata, the capacity ranges from 1228 to 1048576, in GiB, and must be a multiple of 1.2 TiB.
                      For a 125 MB/s/TiB file system with expand_type set to hpc and hpc_bw set to 125M in metadata, the capacity ranges from 1228 to 1048576, in GiB, and must be a multiple of 1.2 TiB.
                      For a 250 MB/s/TiB file system with expand_type set to hpc and hpc_bw set to 250M in metadata, the capacity ranges from 1228 to 1048576, in GiB, and must be a multiple of 1.2 TiB.
                      For a 500 MB/s/TiB file system with expand_type set to hpc and hpc_bw set to 500M in metadata, the capacity ranges from 1228 to 1048576, in GiB, and must be a multiple of 1.2 TiB.
                      For a 1,000 MB/s/TiB file system with expand_type set to hpc and hpc_bw set to 1000M in metadata, the capacity ranges from 1228 to 1048576, in GiB, and must be a multiple of 1.2 TiB.
                      The desired capacity must be converted to GiB and rounded down to the nearest integer. For example, specify 3686 GiB for a 3.6 TiB file system, 4915 GiB for a 4.8 TiB file system, and 8601 GiB for an 8.4 TiB file system.
                    type: number
                  subnetId:
                    description: Specifies the network ID of the subnet. Changing
                      this will create a new resource.
                    type: string
                  version:
                    description: The version ID of the SFS Turbo file system.
                    type: string
                  vpcId:
                    description: Specifies the VPC ID. Changing this will create a
                      new resource.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
