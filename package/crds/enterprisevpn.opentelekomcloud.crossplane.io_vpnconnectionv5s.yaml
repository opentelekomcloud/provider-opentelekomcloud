---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: vpnconnectionv5s.enterprisevpn.opentelekomcloud.crossplane.io
spec:
  group: enterprisevpn.opentelekomcloud.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - opentelekomcloud
    kind: VPNConnectionV5
    listKind: VPNConnectionV5List
    plural: vpnconnectionv5s
    singular: vpnconnectionv5
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VPNConnectionV5 is the Schema for the VPNConnectionV5s API. Manages
          a Enterprise VPN connection Service resource within OpenTelekomCloud.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VPNConnectionV5Spec defines the desired state of VPNConnectionV5
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  customerGatewayId:
                    description: The customer gateway ID.
                    type: string
                  enableNqa:
                    description: Whether to enable NQA check. Defaults to false.
                    type: boolean
                  gatewayId:
                    description: The VPN gateway ID.
                    type: string
                  gatewayIp:
                    description: The VPN gateway IP ID.
                    type: string
                  haRole:
                    description: |-
                      Specifies the mode of the VPN connection.
                      The valid values are master and slave, defaults to master.
                      This parameter is optional when you create a connection for a VPN gateway in active-active mode.
                      When you create a connection for a VPN gateway in active-standby mode, master indicates
                      the active connection, and slave indicates the standby connection.
                      In active-active mode, this field must be set to master for the connection established
                      using the active EIP or active private IP address of the VPN gateway, and must be set to slave
                      for the connection established using active EIP 2 or active private IP address 2 of the VPN gateway.
                    type: string
                  ikepolicy:
                    description: |-
                      The IKE policy configurations.
                      The ikepolicy structure is documented below.
                    items:
                      properties:
                        authenticationAlgorithm:
                          description: |-
                            The authentication algorithm. The value can be sha1, md5,
                            sha2-256, sha2-384, sha2-512. Defaults to sha2-256. sha1 and md5 are less secure,
                            please use them with caution.
                          type: string
                        authenticationMethod:
                          description: |-
                            The authentication method during IKE negotiation.
                            The value can be pre-share. Defaults to pre-share.
                          type: string
                        dhGroup:
                          description: |-
                            Specifies the DH group used for key exchange in phase 1.
                            The value can be group1, group2, group5, group14, group15, group16, group19, group20,
                            or group21. Exercise caution when using group1, group2, group5,
                            or group14 as they have low security. Defaults to group15.
                          type: string
                        dpd:
                          description: |-
                            Specifies the dead peer detection (DPD) object.
                            The dpd structure is documented below.
                          items:
                            properties:
                              interval:
                                description: |-
                                  Specifies the DPD idle timeout period.
                                  The value ranges from 10 to 3,600, in seconds. Defaults to 30.
                                type: number
                              msg:
                                description: 'Specifies the format of DPD packets.
                                  The value can be:'
                                type: string
                              timeout:
                                description: |-
                                  Specifies the interval for retransmitting DPD packets.
                                  The value ranges from 2 to 60, in seconds. Defaults to 15.
                                type: number
                            type: object
                          type: array
                        encryptionAlgorithm:
                          description: |-
                            The encryption algorithm. The value can be 3des, aes-128, aes-192,
                            aes-256, aes-128-gcm-16, aes-256-gcm-16, aes-128-gcm-128, aes-256-gcm-128. Defaults to aes-128.
                            3des is less secure, please use it with caution.
                          type: string
                        ikeVersion:
                          description: The IKE negotiation version. The value can
                            be v1 and v2. Defaults to v2.
                          type: string
                        lifetimeSeconds:
                          description: |-
                            The life cycle of SA in seconds. The value ranges from 60 to 604,800.
                            Defaults to 86,400. When the life cycle expires, IKE SA will be automatically updated.
                          type: number
                        localId:
                          description: The local ID.
                          type: string
                        localIdType:
                          description: The local ID type. The value can be ip or fqdn.
                            Defaults to ip.
                          type: string
                        peerId:
                          description: The peer ID.
                          type: string
                        peerIdType:
                          description: The peer ID type. The value can be ip, fqdn.
                            Defaults to ip.
                          type: string
                        phaseOneNegotiationMode:
                          description: |-
                            The negotiation mode, only works when the ike_version is v1.
                            The value can be main or aggressive. Defaults to main.
                          type: string
                      type: object
                    type: array
                  ipsecpolicy:
                    description: |-
                      The IPsec policy configurations.
                      The ipsecpolicy structure is documented below.
                    items:
                      properties:
                        authenticationAlgorithm:
                          description: |-
                            The authentication algorithm. The value can be sha1, md5,
                            sha2-256, sha2-384, sha2-512. Defaults to sha2-256. sha1 and md5 are less secure,
                            please use them with caution.
                          type: string
                        encapsulationMode:
                          description: |-
                            The encapsulation mode, only tunnel supported for now.
                            Defaults to tunnel.
                          type: string
                        encryptionAlgorithm:
                          description: |-
                            The encryption algorithm. The value can be 3des, aes-128, aes-192,
                            aes-256, aes-128-gcm-16, aes-256-gcm-16, aes-128-gcm-128, aes-256-gcm-128. Defaults to aes-128.
                            `3des** is less secure, please use it with caution.
                          type: string
                        lifetimeSeconds:
                          description: |-
                            The lifecycle time of Ipsec tunnel in seconds.
                            The value ranges from 60 to 604,800. Defaults to 3600.
                          type: number
                        pfs:
                          description: |-
                            The DH key group used by PFS. The value can be group1, group2, group5, group14
                            group16, group19, group20, group21. Defaults to group14.
                          type: string
                        transformProtocol:
                          description: |-
                            The transform protocol. Only esp supported for now.
                            Defaults to esp.
                          type: string
                      type: object
                    type: array
                  name:
                    description: The name of the VPN connection.
                    type: string
                  peerSubnets:
                    description: |-
                      The CIDR list of customer subnets. This parameter must be empty
                      when the attachment_type of the VPN gateway is set to er and vpn_type is set to policy or bgp.
                      This parameter is mandatory in other scenarios.
                    items:
                      type: string
                    type: array
                  policyRules:
                    description: |-
                      The policy rules. Only works when vpn_type is set to policy
                      The policy_rules structure is documented below.
                    items:
                      properties:
                        destination:
                          description: The list of destination CIDRs.
                          items:
                            type: string
                          type: array
                        source:
                          description: The source CIDR.
                          type: string
                      type: object
                    type: array
                  psk:
                    description: The pre-shared key.
                    type: string
                  tags:
                    additionalProperties:
                      type: string
                    description: Specifies the tags of the VPN connection.
                    type: object
                    x-kubernetes-map-type: granular
                  tunnelLocalAddress:
                    description: The local tunnel address.
                    type: string
                  tunnelPeerAddress:
                    description: The peer tunnel address.
                    type: string
                  vpnType:
                    description: The connection type. The value can be policy, static
                      or bgp.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  customerGatewayId:
                    description: The customer gateway ID.
                    type: string
                  enableNqa:
                    description: Whether to enable NQA check. Defaults to false.
                    type: boolean
                  gatewayId:
                    description: The VPN gateway ID.
                    type: string
                  gatewayIp:
                    description: The VPN gateway IP ID.
                    type: string
                  haRole:
                    description: |-
                      Specifies the mode of the VPN connection.
                      The valid values are master and slave, defaults to master.
                      This parameter is optional when you create a connection for a VPN gateway in active-active mode.
                      When you create a connection for a VPN gateway in active-standby mode, master indicates
                      the active connection, and slave indicates the standby connection.
                      In active-active mode, this field must be set to master for the connection established
                      using the active EIP or active private IP address of the VPN gateway, and must be set to slave
                      for the connection established using active EIP 2 or active private IP address 2 of the VPN gateway.
                    type: string
                  ikepolicy:
                    description: |-
                      The IKE policy configurations.
                      The ikepolicy structure is documented below.
                    items:
                      properties:
                        authenticationAlgorithm:
                          description: |-
                            The authentication algorithm. The value can be sha1, md5,
                            sha2-256, sha2-384, sha2-512. Defaults to sha2-256. sha1 and md5 are less secure,
                            please use them with caution.
                          type: string
                        authenticationMethod:
                          description: |-
                            The authentication method during IKE negotiation.
                            The value can be pre-share. Defaults to pre-share.
                          type: string
                        dhGroup:
                          description: |-
                            Specifies the DH group used for key exchange in phase 1.
                            The value can be group1, group2, group5, group14, group15, group16, group19, group20,
                            or group21. Exercise caution when using group1, group2, group5,
                            or group14 as they have low security. Defaults to group15.
                          type: string
                        dpd:
                          description: |-
                            Specifies the dead peer detection (DPD) object.
                            The dpd structure is documented below.
                          items:
                            properties:
                              interval:
                                description: |-
                                  Specifies the DPD idle timeout period.
                                  The value ranges from 10 to 3,600, in seconds. Defaults to 30.
                                type: number
                              msg:
                                description: 'Specifies the format of DPD packets.
                                  The value can be:'
                                type: string
                              timeout:
                                description: |-
                                  Specifies the interval for retransmitting DPD packets.
                                  The value ranges from 2 to 60, in seconds. Defaults to 15.
                                type: number
                            type: object
                          type: array
                        encryptionAlgorithm:
                          description: |-
                            The encryption algorithm. The value can be 3des, aes-128, aes-192,
                            aes-256, aes-128-gcm-16, aes-256-gcm-16, aes-128-gcm-128, aes-256-gcm-128. Defaults to aes-128.
                            3des is less secure, please use it with caution.
                          type: string
                        ikeVersion:
                          description: The IKE negotiation version. The value can
                            be v1 and v2. Defaults to v2.
                          type: string
                        lifetimeSeconds:
                          description: |-
                            The life cycle of SA in seconds. The value ranges from 60 to 604,800.
                            Defaults to 86,400. When the life cycle expires, IKE SA will be automatically updated.
                          type: number
                        localId:
                          description: The local ID.
                          type: string
                        localIdType:
                          description: The local ID type. The value can be ip or fqdn.
                            Defaults to ip.
                          type: string
                        peerId:
                          description: The peer ID.
                          type: string
                        peerIdType:
                          description: The peer ID type. The value can be ip, fqdn.
                            Defaults to ip.
                          type: string
                        phaseOneNegotiationMode:
                          description: |-
                            The negotiation mode, only works when the ike_version is v1.
                            The value can be main or aggressive. Defaults to main.
                          type: string
                      type: object
                    type: array
                  ipsecpolicy:
                    description: |-
                      The IPsec policy configurations.
                      The ipsecpolicy structure is documented below.
                    items:
                      properties:
                        authenticationAlgorithm:
                          description: |-
                            The authentication algorithm. The value can be sha1, md5,
                            sha2-256, sha2-384, sha2-512. Defaults to sha2-256. sha1 and md5 are less secure,
                            please use them with caution.
                          type: string
                        encapsulationMode:
                          description: |-
                            The encapsulation mode, only tunnel supported for now.
                            Defaults to tunnel.
                          type: string
                        encryptionAlgorithm:
                          description: |-
                            The encryption algorithm. The value can be 3des, aes-128, aes-192,
                            aes-256, aes-128-gcm-16, aes-256-gcm-16, aes-128-gcm-128, aes-256-gcm-128. Defaults to aes-128.
                            `3des** is less secure, please use it with caution.
                          type: string
                        lifetimeSeconds:
                          description: |-
                            The lifecycle time of Ipsec tunnel in seconds.
                            The value ranges from 60 to 604,800. Defaults to 3600.
                          type: number
                        pfs:
                          description: |-
                            The DH key group used by PFS. The value can be group1, group2, group5, group14
                            group16, group19, group20, group21. Defaults to group14.
                          type: string
                        transformProtocol:
                          description: |-
                            The transform protocol. Only esp supported for now.
                            Defaults to esp.
                          type: string
                      type: object
                    type: array
                  name:
                    description: The name of the VPN connection.
                    type: string
                  peerSubnets:
                    description: |-
                      The CIDR list of customer subnets. This parameter must be empty
                      when the attachment_type of the VPN gateway is set to er and vpn_type is set to policy or bgp.
                      This parameter is mandatory in other scenarios.
                    items:
                      type: string
                    type: array
                  policyRules:
                    description: |-
                      The policy rules. Only works when vpn_type is set to policy
                      The policy_rules structure is documented below.
                    items:
                      properties:
                        destination:
                          description: The list of destination CIDRs.
                          items:
                            type: string
                          type: array
                        source:
                          description: The source CIDR.
                          type: string
                      type: object
                    type: array
                  psk:
                    description: The pre-shared key.
                    type: string
                  tags:
                    additionalProperties:
                      type: string
                    description: Specifies the tags of the VPN connection.
                    type: object
                    x-kubernetes-map-type: granular
                  tunnelLocalAddress:
                    description: The local tunnel address.
                    type: string
                  tunnelPeerAddress:
                    description: The peer tunnel address.
                    type: string
                  vpnType:
                    description: The connection type. The value can be policy, static
                      or bgp.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.customerGatewayId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.customerGatewayId)
                || (has(self.initProvider) && has(self.initProvider.customerGatewayId))'
            - message: spec.forProvider.gatewayId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.gatewayId)
                || (has(self.initProvider) && has(self.initProvider.gatewayId))'
            - message: spec.forProvider.gatewayIp is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.gatewayIp)
                || (has(self.initProvider) && has(self.initProvider.gatewayIp))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.psk is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.psk)
                || (has(self.initProvider) && has(self.initProvider.psk))'
            - message: spec.forProvider.vpnType is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.vpnType)
                || (has(self.initProvider) && has(self.initProvider.vpnType))'
          status:
            description: VPNConnectionV5Status defines the observed state of VPNConnectionV5.
            properties:
              atProvider:
                properties:
                  createdAt:
                    description: The create time.
                    type: string
                  customerGatewayId:
                    description: The customer gateway ID.
                    type: string
                  enableNqa:
                    description: Whether to enable NQA check. Defaults to false.
                    type: boolean
                  gatewayId:
                    description: The VPN gateway ID.
                    type: string
                  gatewayIp:
                    description: The VPN gateway IP ID.
                    type: string
                  haRole:
                    description: |-
                      Specifies the mode of the VPN connection.
                      The valid values are master and slave, defaults to master.
                      This parameter is optional when you create a connection for a VPN gateway in active-active mode.
                      When you create a connection for a VPN gateway in active-standby mode, master indicates
                      the active connection, and slave indicates the standby connection.
                      In active-active mode, this field must be set to master for the connection established
                      using the active EIP or active private IP address of the VPN gateway, and must be set to slave
                      for the connection established using active EIP 2 or active private IP address 2 of the VPN gateway.
                    type: string
                  id:
                    description: The resource ID.
                    type: string
                  ikepolicy:
                    description: |-
                      The IKE policy configurations.
                      The ikepolicy structure is documented below.
                    items:
                      properties:
                        authenticationAlgorithm:
                          description: |-
                            The authentication algorithm. The value can be sha1, md5,
                            sha2-256, sha2-384, sha2-512. Defaults to sha2-256. sha1 and md5 are less secure,
                            please use them with caution.
                          type: string
                        authenticationMethod:
                          description: |-
                            The authentication method during IKE negotiation.
                            The value can be pre-share. Defaults to pre-share.
                          type: string
                        dhGroup:
                          description: |-
                            Specifies the DH group used for key exchange in phase 1.
                            The value can be group1, group2, group5, group14, group15, group16, group19, group20,
                            or group21. Exercise caution when using group1, group2, group5,
                            or group14 as they have low security. Defaults to group15.
                          type: string
                        dpd:
                          description: |-
                            Specifies the dead peer detection (DPD) object.
                            The dpd structure is documented below.
                          items:
                            properties:
                              interval:
                                description: |-
                                  Specifies the DPD idle timeout period.
                                  The value ranges from 10 to 3,600, in seconds. Defaults to 30.
                                type: number
                              msg:
                                description: 'Specifies the format of DPD packets.
                                  The value can be:'
                                type: string
                              timeout:
                                description: |-
                                  Specifies the interval for retransmitting DPD packets.
                                  The value ranges from 2 to 60, in seconds. Defaults to 15.
                                type: number
                            type: object
                          type: array
                        encryptionAlgorithm:
                          description: |-
                            The encryption algorithm. The value can be 3des, aes-128, aes-192,
                            aes-256, aes-128-gcm-16, aes-256-gcm-16, aes-128-gcm-128, aes-256-gcm-128. Defaults to aes-128.
                            3des is less secure, please use it with caution.
                          type: string
                        ikeVersion:
                          description: The IKE negotiation version. The value can
                            be v1 and v2. Defaults to v2.
                          type: string
                        lifetimeSeconds:
                          description: |-
                            The life cycle of SA in seconds. The value ranges from 60 to 604,800.
                            Defaults to 86,400. When the life cycle expires, IKE SA will be automatically updated.
                          type: number
                        localId:
                          description: The local ID.
                          type: string
                        localIdType:
                          description: The local ID type. The value can be ip or fqdn.
                            Defaults to ip.
                          type: string
                        peerId:
                          description: The peer ID.
                          type: string
                        peerIdType:
                          description: The peer ID type. The value can be ip, fqdn.
                            Defaults to ip.
                          type: string
                        phaseOneNegotiationMode:
                          description: |-
                            The negotiation mode, only works when the ike_version is v1.
                            The value can be main or aggressive. Defaults to main.
                          type: string
                      type: object
                    type: array
                  ipsecpolicy:
                    description: |-
                      The IPsec policy configurations.
                      The ipsecpolicy structure is documented below.
                    items:
                      properties:
                        authenticationAlgorithm:
                          description: |-
                            The authentication algorithm. The value can be sha1, md5,
                            sha2-256, sha2-384, sha2-512. Defaults to sha2-256. sha1 and md5 are less secure,
                            please use them with caution.
                          type: string
                        encapsulationMode:
                          description: |-
                            The encapsulation mode, only tunnel supported for now.
                            Defaults to tunnel.
                          type: string
                        encryptionAlgorithm:
                          description: |-
                            The encryption algorithm. The value can be 3des, aes-128, aes-192,
                            aes-256, aes-128-gcm-16, aes-256-gcm-16, aes-128-gcm-128, aes-256-gcm-128. Defaults to aes-128.
                            `3des** is less secure, please use it with caution.
                          type: string
                        lifetimeSeconds:
                          description: |-
                            The lifecycle time of Ipsec tunnel in seconds.
                            The value ranges from 60 to 604,800. Defaults to 3600.
                          type: number
                        pfs:
                          description: |-
                            The DH key group used by PFS. The value can be group1, group2, group5, group14
                            group16, group19, group20, group21. Defaults to group14.
                          type: string
                        transformProtocol:
                          description: |-
                            The transform protocol. Only esp supported for now.
                            Defaults to esp.
                          type: string
                      type: object
                    type: array
                  name:
                    description: The name of the VPN connection.
                    type: string
                  peerSubnets:
                    description: |-
                      The CIDR list of customer subnets. This parameter must be empty
                      when the attachment_type of the VPN gateway is set to er and vpn_type is set to policy or bgp.
                      This parameter is mandatory in other scenarios.
                    items:
                      type: string
                    type: array
                  policyRules:
                    description: |-
                      The policy rules. Only works when vpn_type is set to policy
                      The policy_rules structure is documented below.
                    items:
                      properties:
                        destination:
                          description: The list of destination CIDRs.
                          items:
                            type: string
                          type: array
                        source:
                          description: The source CIDR.
                          type: string
                      type: object
                    type: array
                  psk:
                    description: The pre-shared key.
                    type: string
                  region:
                    description: Specifies the region in which resource is created.
                    type: string
                  status:
                    description: The status of the VPN connection.
                    type: string
                  tags:
                    additionalProperties:
                      type: string
                    description: Specifies the tags of the VPN connection.
                    type: object
                    x-kubernetes-map-type: granular
                  tunnelLocalAddress:
                    description: The local tunnel address.
                    type: string
                  tunnelPeerAddress:
                    description: The peer tunnel address.
                    type: string
                  updatedAt:
                    description: The update time.
                    type: string
                  vpnType:
                    description: The connection type. The value can be policy, static
                      or bgp.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
