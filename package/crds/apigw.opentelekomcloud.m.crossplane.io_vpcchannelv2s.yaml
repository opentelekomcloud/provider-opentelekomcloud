---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: vpcchannelv2s.apigw.opentelekomcloud.m.crossplane.io
spec:
  group: apigw.opentelekomcloud.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - opentelekomcloud
    kind: VPCChannelV2
    listKind: VPCChannelV2List
    plural: vpcchannelv2s
    singular: vpcchannelv2
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VPCChannelV2 is the Schema for the VPCChannelV2s API. Manages
          a APIGW VPC Channel resource within OpenTelekomCloud.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VPCChannelV2Spec defines the desired state of VPCChannelV2
            properties:
              forProvider:
                properties:
                  gatewayId:
                    description: |-
                      Specifies the ID of the dedicated API gateway to which the channel
                      belongs.
                      Changing this will create a new resource.
                    type: string
                  healthCheck:
                    description: |-
                      Specifies the health configuration of cloud servers associated with the load balance
                      channel for APIG regularly check.
                      The object structure is documented below.
                    items:
                      properties:
                        enableClientSsl:
                          description: |-
                            Specifies whether to enable two-way authentication.
                            Defaults to false.
                          type: boolean
                        httpCodes:
                          description: |-
                            Specifies the response codes for determining a successful HTTP response.
                            The valid value ranges from 100 to 599 and the valid formats are as follows:
                          type: string
                        interval:
                          description: |-
                            Specifies the interval between consecutive checks, in second.
                            The valid value ranges from 1 to 300.
                          type: number
                        method:
                          description: |-
                            Specifies the request method for health check.
                            The valid values are GET and HEAD.
                          type: string
                        path:
                          description: |-
                            Specifies the destination path for health checks.
                            Required if the protocol is HTTP or HTTPS.
                          type: string
                        port:
                          description: |-
                            Specifies the destination host port for health check.
                            The valid value ranges from 0 to 65,535.
                          type: number
                        protocol:
                          description: |-
                            Specifies the microservice for performing health check on backend servers.
                            The valid values are TCP, HTTP and HTTPS, defaults to TCP.
                          type: string
                        status:
                          description: |-
                            Specifies the status of health check.
                            The valid values are as follows:
                          type: number
                        thresholdAbnormal:
                          description: |-
                            Specifies the unhealthy threshold, which refers to the number of consecutive
                            failed checks required for a backend server to be considered unhealthy.
                            The valid value ranges from 1 to 10.
                          type: number
                        thresholdNormal:
                          description: |-
                            Specifies the healthy threshold, which refers to the number of consecutive
                            successful checks required for a backend server to be considered healthy.
                            The valid value ranges from 1 to 10.
                          type: number
                        timeout:
                          description: |-
                            Specifies the timeout for determining whether a health check fails, in second.
                            The value must be less than the value of the time interval.
                            The valid value ranges from 1 to 30.
                          type: number
                      type: object
                    type: array
                  lbAlgorithm:
                    description: |-
                      Specifies the distribution algorithm.
                      The valid values are as follows:
                    type: number
                  member:
                    description: |-
                      Specifies the backend servers of the channel.
                      This parameter is required and only available if the type is 2.
                      The object structure is documented below.
                    items:
                      properties:
                        groupName:
                          description: |-
                            Specifies the IP address each backend servers.
                            If omitted, means that all backend servers are both in one group.
                          type: string
                        host:
                          description: Specifies the IP address each backend servers.
                          type: string
                        id:
                          description: Specifies the ECS ID for each backend servers.
                          type: string
                        isBackup:
                          description: |-
                            Specifies whether this member is the backup member.
                            Defaults to false.
                          type: boolean
                        name:
                          description: |-
                            Specifies the name of the backend server.
                            Required if the parameter member.id is set.
                          type: string
                        port:
                          description: |-
                            Specifies the port of the backend server.
                            The valid value ranges from 0 to 65,535.
                            If omitted, the default port of channel will be used.
                          type: number
                        status:
                          description: |-
                            Specifies the status of the backend server.
                            The valid values are as follows:
                          type: number
                        weight:
                          description: |-
                            Specifies the weight of current backend server.
                            The valid value ranges from 0 to 10,000, defaults to 0.
                          type: number
                      type: object
                    type: array
                  memberGroup:
                    description: |-
                      Specifies the backend (server) groups of the channel.
                      The object structure is documented below.
                    items:
                      properties:
                        description:
                          description: Specifies the description of the member group.
                          type: string
                        microservicePort:
                          description: |-
                            Specifies the microservice port of the backend server group.
                            The valid value ranges from 0 to 65,535.
                          type: number
                        microserviceTags:
                          additionalProperties:
                            type: string
                          description: Specifies the microservice tags of the backend
                            server group.
                          type: object
                          x-kubernetes-map-type: granular
                        microserviceVersion:
                          description: Specifies the microservice version of the backend
                            server group.
                          type: string
                        name:
                          description: |-
                            Specifies the name of the member group.
                            The valid length is limited from 3 to 64, only chinese and english letters, digits, hyphens (-), underscores (_)
                            and dots (.) are allowed.
                            The name must start with a Chinese or English letter.
                          type: string
                        weight:
                          description: Specifies the weight of the current member
                            group.
                          type: number
                      type: object
                    type: array
                  memberType:
                    description: |-
                      Specifies the member type of the channel.
                      The valid values are as follows:
                    type: string
                  microservice:
                    description: |-
                      Specifies the configuration of the microservice.
                      The object structure is documented below.
                    items:
                      properties:
                        cceConfig:
                          description: |-
                            Specifies the CCE microservice details.
                            The object structure is documented below.
                          items:
                            properties:
                              clusterId:
                                description: Specifies the CCE cluster ID.
                                type: string
                              labelKey:
                                description: Specifies the service label key.
                                type: string
                              labelValue:
                                description: Specifies the service label value.
                                type: string
                              namespace:
                                description: 'Specifies the namespace, such as the
                                  default namespace for CCE cluster: default.'
                                type: string
                              workloadName:
                                description: |-
                                  Specifies the channel name.
                                  The valid length is limited from 3 to 64, only chinese and english letters, digits, hyphens -, underscores _
                                  and dots (.) are allowed.
                                  The name must start with a Chinese or English letter.
                                type: string
                              workloadType:
                                description: Specifies the workload type.
                                type: string
                            type: object
                          type: array
                        cseConfig:
                          items:
                            properties:
                              engineId:
                                description: The ID of the channel.
                                type: string
                              serviceId:
                                description: The ID of the channel.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  name:
                    description: |-
                      Specifies the channel name.
                      The valid length is limited from 3 to 64, only chinese and english letters, digits, hyphens -, underscores _
                      and dots (.) are allowed.
                      The name must start with a Chinese or English letter.
                    type: string
                  port:
                    description: |-
                      Specifies the default port for health check in channel.
                      The valid value ranges from 1 to 65,535.
                    type: number
                  type:
                    description: |-
                      Specifies the type of the channel.
                      The valid values are as follows:
                    type: number
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  gatewayId:
                    description: |-
                      Specifies the ID of the dedicated API gateway to which the channel
                      belongs.
                      Changing this will create a new resource.
                    type: string
                  healthCheck:
                    description: |-
                      Specifies the health configuration of cloud servers associated with the load balance
                      channel for APIG regularly check.
                      The object structure is documented below.
                    items:
                      properties:
                        enableClientSsl:
                          description: |-
                            Specifies whether to enable two-way authentication.
                            Defaults to false.
                          type: boolean
                        httpCodes:
                          description: |-
                            Specifies the response codes for determining a successful HTTP response.
                            The valid value ranges from 100 to 599 and the valid formats are as follows:
                          type: string
                        interval:
                          description: |-
                            Specifies the interval between consecutive checks, in second.
                            The valid value ranges from 1 to 300.
                          type: number
                        method:
                          description: |-
                            Specifies the request method for health check.
                            The valid values are GET and HEAD.
                          type: string
                        path:
                          description: |-
                            Specifies the destination path for health checks.
                            Required if the protocol is HTTP or HTTPS.
                          type: string
                        port:
                          description: |-
                            Specifies the destination host port for health check.
                            The valid value ranges from 0 to 65,535.
                          type: number
                        protocol:
                          description: |-
                            Specifies the microservice for performing health check on backend servers.
                            The valid values are TCP, HTTP and HTTPS, defaults to TCP.
                          type: string
                        status:
                          description: |-
                            Specifies the status of health check.
                            The valid values are as follows:
                          type: number
                        thresholdAbnormal:
                          description: |-
                            Specifies the unhealthy threshold, which refers to the number of consecutive
                            failed checks required for a backend server to be considered unhealthy.
                            The valid value ranges from 1 to 10.
                          type: number
                        thresholdNormal:
                          description: |-
                            Specifies the healthy threshold, which refers to the number of consecutive
                            successful checks required for a backend server to be considered healthy.
                            The valid value ranges from 1 to 10.
                          type: number
                        timeout:
                          description: |-
                            Specifies the timeout for determining whether a health check fails, in second.
                            The value must be less than the value of the time interval.
                            The valid value ranges from 1 to 30.
                          type: number
                      type: object
                    type: array
                  lbAlgorithm:
                    description: |-
                      Specifies the distribution algorithm.
                      The valid values are as follows:
                    type: number
                  member:
                    description: |-
                      Specifies the backend servers of the channel.
                      This parameter is required and only available if the type is 2.
                      The object structure is documented below.
                    items:
                      properties:
                        groupName:
                          description: |-
                            Specifies the IP address each backend servers.
                            If omitted, means that all backend servers are both in one group.
                          type: string
                        host:
                          description: Specifies the IP address each backend servers.
                          type: string
                        id:
                          description: Specifies the ECS ID for each backend servers.
                          type: string
                        isBackup:
                          description: |-
                            Specifies whether this member is the backup member.
                            Defaults to false.
                          type: boolean
                        name:
                          description: |-
                            Specifies the name of the backend server.
                            Required if the parameter member.id is set.
                          type: string
                        port:
                          description: |-
                            Specifies the port of the backend server.
                            The valid value ranges from 0 to 65,535.
                            If omitted, the default port of channel will be used.
                          type: number
                        status:
                          description: |-
                            Specifies the status of the backend server.
                            The valid values are as follows:
                          type: number
                        weight:
                          description: |-
                            Specifies the weight of current backend server.
                            The valid value ranges from 0 to 10,000, defaults to 0.
                          type: number
                      type: object
                    type: array
                  memberGroup:
                    description: |-
                      Specifies the backend (server) groups of the channel.
                      The object structure is documented below.
                    items:
                      properties:
                        description:
                          description: Specifies the description of the member group.
                          type: string
                        microservicePort:
                          description: |-
                            Specifies the microservice port of the backend server group.
                            The valid value ranges from 0 to 65,535.
                          type: number
                        microserviceTags:
                          additionalProperties:
                            type: string
                          description: Specifies the microservice tags of the backend
                            server group.
                          type: object
                          x-kubernetes-map-type: granular
                        microserviceVersion:
                          description: Specifies the microservice version of the backend
                            server group.
                          type: string
                        name:
                          description: |-
                            Specifies the name of the member group.
                            The valid length is limited from 3 to 64, only chinese and english letters, digits, hyphens (-), underscores (_)
                            and dots (.) are allowed.
                            The name must start with a Chinese or English letter.
                          type: string
                        weight:
                          description: Specifies the weight of the current member
                            group.
                          type: number
                      type: object
                    type: array
                  memberType:
                    description: |-
                      Specifies the member type of the channel.
                      The valid values are as follows:
                    type: string
                  microservice:
                    description: |-
                      Specifies the configuration of the microservice.
                      The object structure is documented below.
                    items:
                      properties:
                        cceConfig:
                          description: |-
                            Specifies the CCE microservice details.
                            The object structure is documented below.
                          items:
                            properties:
                              clusterId:
                                description: Specifies the CCE cluster ID.
                                type: string
                              labelKey:
                                description: Specifies the service label key.
                                type: string
                              labelValue:
                                description: Specifies the service label value.
                                type: string
                              namespace:
                                description: 'Specifies the namespace, such as the
                                  default namespace for CCE cluster: default.'
                                type: string
                              workloadName:
                                description: |-
                                  Specifies the channel name.
                                  The valid length is limited from 3 to 64, only chinese and english letters, digits, hyphens -, underscores _
                                  and dots (.) are allowed.
                                  The name must start with a Chinese or English letter.
                                type: string
                              workloadType:
                                description: Specifies the workload type.
                                type: string
                            type: object
                          type: array
                        cseConfig:
                          items:
                            properties:
                              engineId:
                                description: The ID of the channel.
                                type: string
                              serviceId:
                                description: The ID of the channel.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  name:
                    description: |-
                      Specifies the channel name.
                      The valid length is limited from 3 to 64, only chinese and english letters, digits, hyphens -, underscores _
                      and dots (.) are allowed.
                      The name must start with a Chinese or English letter.
                    type: string
                  port:
                    description: |-
                      Specifies the default port for health check in channel.
                      The valid value ranges from 1 to 65,535.
                    type: number
                  type:
                    description: |-
                      Specifies the type of the channel.
                      The valid values are as follows:
                    type: number
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.gatewayId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.gatewayId)
                || (has(self.initProvider) && has(self.initProvider.gatewayId))'
            - message: spec.forProvider.lbAlgorithm is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.lbAlgorithm)
                || (has(self.initProvider) && has(self.initProvider.lbAlgorithm))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.port is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.port)
                || (has(self.initProvider) && has(self.initProvider.port))'
          status:
            description: VPCChannelV2Status defines the observed state of VPCChannelV2.
            properties:
              atProvider:
                properties:
                  createdAt:
                    description: The time when the channel was created.
                    type: string
                  gatewayId:
                    description: |-
                      Specifies the ID of the dedicated API gateway to which the channel
                      belongs.
                      Changing this will create a new resource.
                    type: string
                  healthCheck:
                    description: |-
                      Specifies the health configuration of cloud servers associated with the load balance
                      channel for APIG regularly check.
                      The object structure is documented below.
                    items:
                      properties:
                        enableClientSsl:
                          description: |-
                            Specifies whether to enable two-way authentication.
                            Defaults to false.
                          type: boolean
                        httpCodes:
                          description: |-
                            Specifies the response codes for determining a successful HTTP response.
                            The valid value ranges from 100 to 599 and the valid formats are as follows:
                          type: string
                        interval:
                          description: |-
                            Specifies the interval between consecutive checks, in second.
                            The valid value ranges from 1 to 300.
                          type: number
                        method:
                          description: |-
                            Specifies the request method for health check.
                            The valid values are GET and HEAD.
                          type: string
                        path:
                          description: |-
                            Specifies the destination path for health checks.
                            Required if the protocol is HTTP or HTTPS.
                          type: string
                        port:
                          description: |-
                            Specifies the destination host port for health check.
                            The valid value ranges from 0 to 65,535.
                          type: number
                        protocol:
                          description: |-
                            Specifies the microservice for performing health check on backend servers.
                            The valid values are TCP, HTTP and HTTPS, defaults to TCP.
                          type: string
                        status:
                          description: |-
                            Specifies the status of health check.
                            The valid values are as follows:
                          type: number
                        thresholdAbnormal:
                          description: |-
                            Specifies the unhealthy threshold, which refers to the number of consecutive
                            failed checks required for a backend server to be considered unhealthy.
                            The valid value ranges from 1 to 10.
                          type: number
                        thresholdNormal:
                          description: |-
                            Specifies the healthy threshold, which refers to the number of consecutive
                            successful checks required for a backend server to be considered healthy.
                            The valid value ranges from 1 to 10.
                          type: number
                        timeout:
                          description: |-
                            Specifies the timeout for determining whether a health check fails, in second.
                            The value must be less than the value of the time interval.
                            The valid value ranges from 1 to 30.
                          type: number
                      type: object
                    type: array
                  id:
                    description: The ID of the channel.
                    type: string
                  lbAlgorithm:
                    description: |-
                      Specifies the distribution algorithm.
                      The valid values are as follows:
                    type: number
                  member:
                    description: |-
                      Specifies the backend servers of the channel.
                      This parameter is required and only available if the type is 2.
                      The object structure is documented below.
                    items:
                      properties:
                        groupName:
                          description: |-
                            Specifies the IP address each backend servers.
                            If omitted, means that all backend servers are both in one group.
                          type: string
                        host:
                          description: Specifies the IP address each backend servers.
                          type: string
                        id:
                          description: Specifies the ECS ID for each backend servers.
                          type: string
                        isBackup:
                          description: |-
                            Specifies whether this member is the backup member.
                            Defaults to false.
                          type: boolean
                        name:
                          description: |-
                            Specifies the name of the backend server.
                            Required if the parameter member.id is set.
                          type: string
                        port:
                          description: |-
                            Specifies the port of the backend server.
                            The valid value ranges from 0 to 65,535.
                            If omitted, the default port of channel will be used.
                          type: number
                        status:
                          description: |-
                            Specifies the status of the backend server.
                            The valid values are as follows:
                          type: number
                        weight:
                          description: |-
                            Specifies the weight of current backend server.
                            The valid value ranges from 0 to 10,000, defaults to 0.
                          type: number
                      type: object
                    type: array
                  memberGroup:
                    description: |-
                      Specifies the backend (server) groups of the channel.
                      The object structure is documented below.
                    items:
                      properties:
                        description:
                          description: Specifies the description of the member group.
                          type: string
                        microservicePort:
                          description: |-
                            Specifies the microservice port of the backend server group.
                            The valid value ranges from 0 to 65,535.
                          type: number
                        microserviceTags:
                          additionalProperties:
                            type: string
                          description: Specifies the microservice tags of the backend
                            server group.
                          type: object
                          x-kubernetes-map-type: granular
                        microserviceVersion:
                          description: Specifies the microservice version of the backend
                            server group.
                          type: string
                        name:
                          description: |-
                            Specifies the name of the member group.
                            The valid length is limited from 3 to 64, only chinese and english letters, digits, hyphens (-), underscores (_)
                            and dots (.) are allowed.
                            The name must start with a Chinese or English letter.
                          type: string
                        weight:
                          description: Specifies the weight of the current member
                            group.
                          type: number
                      type: object
                    type: array
                  memberType:
                    description: |-
                      Specifies the member type of the channel.
                      The valid values are as follows:
                    type: string
                  microservice:
                    description: |-
                      Specifies the configuration of the microservice.
                      The object structure is documented below.
                    items:
                      properties:
                        cceConfig:
                          description: |-
                            Specifies the CCE microservice details.
                            The object structure is documented below.
                          items:
                            properties:
                              clusterId:
                                description: Specifies the CCE cluster ID.
                                type: string
                              labelKey:
                                description: Specifies the service label key.
                                type: string
                              labelValue:
                                description: Specifies the service label value.
                                type: string
                              namespace:
                                description: 'Specifies the namespace, such as the
                                  default namespace for CCE cluster: default.'
                                type: string
                              workloadName:
                                description: |-
                                  Specifies the channel name.
                                  The valid length is limited from 3 to 64, only chinese and english letters, digits, hyphens -, underscores _
                                  and dots (.) are allowed.
                                  The name must start with a Chinese or English letter.
                                type: string
                              workloadType:
                                description: Specifies the workload type.
                                type: string
                            type: object
                          type: array
                        cseConfig:
                          items:
                            properties:
                              engineId:
                                description: The ID of the channel.
                                type: string
                              serviceId:
                                description: The ID of the channel.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  name:
                    description: |-
                      Specifies the channel name.
                      The valid length is limited from 3 to 64, only chinese and english letters, digits, hyphens -, underscores _
                      and dots (.) are allowed.
                      The name must start with a Chinese or English letter.
                    type: string
                  port:
                    description: |-
                      Specifies the default port for health check in channel.
                      The valid value ranges from 1 to 65,535.
                    type: number
                  region:
                    description: The region where the channel is located.
                    type: string
                  status:
                    description: The current status of the channel.
                    type: number
                  type:
                    description: |-
                      Specifies the type of the channel.
                      The valid values are as follows:
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
