---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: flowlogv3s.er.opentelekomcloud.m.crossplane.io
spec:
  group: er.opentelekomcloud.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - opentelekomcloud
    kind: FlowLogV3
    listKind: FlowLogV3List
    plural: flowlogv3s
    singular: flowlogv3
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: FlowLogV3 is the Schema for the FlowLogV3s API. Manages an Enterprise
          Router Flow Log resource within OpenTelekomCloud.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: FlowLogV3Spec defines the desired state of FlowLogV3
            properties:
              forProvider:
                properties:
                  description:
                    description: Specifies the description of the flow log.
                    type: string
                  enabled:
                    description: Specifies whether to enable the flow log function.
                      The default value is true.
                    type: boolean
                  instanceId:
                    description: |-
                      Specifies the ID of the ER instance to which the flow log belongs.
                      Changing this creates a new resource.
                    type: string
                  logGroupId:
                    description: |-
                      Specifies the LTS log group ID.
                      Changing this creates a new resource.
                    type: string
                  logStoreType:
                    description: |-
                      Specifies the storage type of flow log. The valid value is LTS.
                      Changing this creates a new resource.
                    type: string
                  logStreamId:
                    description: |-
                      Specifies the LTS log stream ID.
                      Changing this creates a new resource.
                    type: string
                  name:
                    description: Specifies the name of the flow log.
                    type: string
                  resourceId:
                    description: |-
                      Specifies the resource ID to which the logs to be collected.
                      Changing this creates a new resource.
                    type: string
                  resourceType:
                    description: |-
                      Specifies the resource type to which the logs to be collected.
                      The valid value is attachment.
                      Changing this creates a new resource.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  description:
                    description: Specifies the description of the flow log.
                    type: string
                  enabled:
                    description: Specifies whether to enable the flow log function.
                      The default value is true.
                    type: boolean
                  instanceId:
                    description: |-
                      Specifies the ID of the ER instance to which the flow log belongs.
                      Changing this creates a new resource.
                    type: string
                  logGroupId:
                    description: |-
                      Specifies the LTS log group ID.
                      Changing this creates a new resource.
                    type: string
                  logStoreType:
                    description: |-
                      Specifies the storage type of flow log. The valid value is LTS.
                      Changing this creates a new resource.
                    type: string
                  logStreamId:
                    description: |-
                      Specifies the LTS log stream ID.
                      Changing this creates a new resource.
                    type: string
                  name:
                    description: Specifies the name of the flow log.
                    type: string
                  resourceId:
                    description: |-
                      Specifies the resource ID to which the logs to be collected.
                      Changing this creates a new resource.
                    type: string
                  resourceType:
                    description: |-
                      Specifies the resource type to which the logs to be collected.
                      The valid value is attachment.
                      Changing this creates a new resource.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.instanceId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.instanceId)
                || (has(self.initProvider) && has(self.initProvider.instanceId))'
            - message: spec.forProvider.logGroupId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.logGroupId)
                || (has(self.initProvider) && has(self.initProvider.logGroupId))'
            - message: spec.forProvider.logStoreType is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.logStoreType)
                || (has(self.initProvider) && has(self.initProvider.logStoreType))'
            - message: spec.forProvider.logStreamId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.logStreamId)
                || (has(self.initProvider) && has(self.initProvider.logStreamId))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.resourceId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.resourceId)
                || (has(self.initProvider) && has(self.initProvider.resourceId))'
            - message: spec.forProvider.resourceType is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.resourceType)
                || (has(self.initProvider) && has(self.initProvider.resourceType))'
          status:
            description: FlowLogV3Status defines the observed state of FlowLogV3.
            properties:
              atProvider:
                properties:
                  createdAt:
                    description: The creation time of the flow log.
                    type: string
                  description:
                    description: Specifies the description of the flow log.
                    type: string
                  enabled:
                    description: Specifies whether to enable the flow log function.
                      The default value is true.
                    type: boolean
                  id:
                    description: The resource ID.
                    type: string
                  instanceId:
                    description: |-
                      Specifies the ID of the ER instance to which the flow log belongs.
                      Changing this creates a new resource.
                    type: string
                  logGroupId:
                    description: |-
                      Specifies the LTS log group ID.
                      Changing this creates a new resource.
                    type: string
                  logStoreType:
                    description: |-
                      Specifies the storage type of flow log. The valid value is LTS.
                      Changing this creates a new resource.
                    type: string
                  logStreamId:
                    description: |-
                      Specifies the LTS log stream ID.
                      Changing this creates a new resource.
                    type: string
                  name:
                    description: Specifies the name of the flow log.
                    type: string
                  region:
                    description: The region of the resource.
                    type: string
                  resourceId:
                    description: |-
                      Specifies the resource ID to which the logs to be collected.
                      Changing this creates a new resource.
                    type: string
                  resourceType:
                    description: |-
                      Specifies the resource type to which the logs to be collected.
                      The valid value is attachment.
                      Changing this creates a new resource.
                    type: string
                  state:
                    description: The current status of the flow log.
                    type: string
                  updatedAt:
                    description: The latest update time of the flow log.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
