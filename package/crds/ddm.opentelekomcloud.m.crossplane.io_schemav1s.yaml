---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: schemav1s.ddm.opentelekomcloud.m.crossplane.io
spec:
  group: ddm.opentelekomcloud.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - opentelekomcloud
    kind: SchemaV1
    listKind: SchemaV1List
    plural: schemav1s
    singular: schemav1
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SchemaV1 is the Schema for the SchemaV1s API. Manages a DDM Schema
          resource within OpenTelekomCloud.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SchemaV1Spec defines the desired state of SchemaV1
            properties:
              forProvider:
                properties:
                  instanceId:
                    description: Specifies the DDM instance ID.
                    type: string
                  name:
                    description: |-
                      Specifies the DDM schema name. The DDM instance name of the same
                      type is unique in the same tenant. It can be  2 to 48 characters long. It must start with a letter and it can only contain etters, digits, and underscores (_).
                    type: string
                  purgeRdsOnDelete:
                    description: 'Specifies whether data stored on the associated
                      DB instances is deleted. The value can be: true or false (default)'
                    type: boolean
                  rds:
                    description: Specifies the rds instance information. The structure
                      is described below.
                    items:
                      properties:
                        adminPasswordSecretRef:
                          description: Specifies the password of RDS admin.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        adminUsernameSecretRef:
                          description: Specifies the username of RDS admin.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        id:
                          description: Specifies the ID of the rds instance.
                          type: string
                      type: object
                    type: array
                  shardMode:
                    description: Specifies the sharding mode of the schema. The values
                      for this can be cluster or single. Cluster indicates that the
                      schema is in sharded mode. Single indicates that the schema
                      is in unsharded mode.
                    type: string
                  shardNumber:
                    description: Specifies the number of shards in the same working
                      mode. If shard_unit is not empty, the value is the product of
                      shard_unit multiplied by the associated RDS instances. If shard_unit
                      is left blank, the value must be greater than the number of
                      associated RDS instances and less than or equal to the product
                      of the associated RDS instances multiplied by 64.
                    type: number
                  shardUnit:
                    description: Specifies the Number of shards per RDS instance.
                      The value is 1 if the schema is unsharded. The value ranges
                      from 1 to 64 if the schema is sharded.
                    type: number
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  instanceId:
                    description: Specifies the DDM instance ID.
                    type: string
                  name:
                    description: |-
                      Specifies the DDM schema name. The DDM instance name of the same
                      type is unique in the same tenant. It can be  2 to 48 characters long. It must start with a letter and it can only contain etters, digits, and underscores (_).
                    type: string
                  purgeRdsOnDelete:
                    description: 'Specifies whether data stored on the associated
                      DB instances is deleted. The value can be: true or false (default)'
                    type: boolean
                  rds:
                    description: Specifies the rds instance information. The structure
                      is described below.
                    items:
                      properties:
                        adminPasswordSecretRef:
                          description: Specifies the password of RDS admin.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        adminUsernameSecretRef:
                          description: Specifies the username of RDS admin.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        id:
                          description: Specifies the ID of the rds instance.
                          type: string
                      required:
                      - adminPasswordSecretRef
                      - adminUsernameSecretRef
                      type: object
                    type: array
                  shardMode:
                    description: Specifies the sharding mode of the schema. The values
                      for this can be cluster or single. Cluster indicates that the
                      schema is in sharded mode. Single indicates that the schema
                      is in unsharded mode.
                    type: string
                  shardNumber:
                    description: Specifies the number of shards in the same working
                      mode. If shard_unit is not empty, the value is the product of
                      shard_unit multiplied by the associated RDS instances. If shard_unit
                      is left blank, the value must be greater than the number of
                      associated RDS instances and less than or equal to the product
                      of the associated RDS instances multiplied by 64.
                    type: number
                  shardUnit:
                    description: Specifies the Number of shards per RDS instance.
                      The value is 1 if the schema is unsharded. The value ranges
                      from 1 to 64 if the schema is sharded.
                    type: number
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.instanceId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.instanceId)
                || (has(self.initProvider) && has(self.initProvider.instanceId))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.rds is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.rds)
                || (has(self.initProvider) && has(self.initProvider.rds))'
            - message: spec.forProvider.shardMode is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.shardMode)
                || (has(self.initProvider) && has(self.initProvider.shardMode))'
            - message: spec.forProvider.shardNumber is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.shardNumber)
                || (has(self.initProvider) && has(self.initProvider.shardNumber))'
          status:
            description: SchemaV1Status defines the observed state of SchemaV1.
            properties:
              atProvider:
                properties:
                  createdAt:
                    description: (uint64) Indicates the creation time.
                    type: number
                  dataVips:
                    description: (List) Indicates the IP address and port number for
                      connecting to the schema.
                    items:
                      type: string
                    type: array
                  databases:
                    description: (List) Indicates the Sharding information of the
                      schema. The structure is described below.
                    items:
                      properties:
                        createdAt:
                          description: (uint64) Indicates the creation time.
                          type: number
                        dbSlot:
                          description: (String) Indicates the Number of shards.
                          type: number
                        id:
                          description: (String) ID of the RDS instance where the shard
                            is located.
                          type: string
                        name:
                          description: (String) Indicates the shard name.
                          type: string
                        rdsName:
                          description: (String) Name of the RDS instance where the
                            shard is located
                          type: string
                        status:
                          description: (String) Indicates the shard status.
                          type: string
                        updatedAt:
                          description: (uint64) Indicates the update time.
                          type: number
                      type: object
                    type: array
                  id:
                    description: (String) Indicates the RDS ID.
                    type: string
                  instanceId:
                    description: Specifies the DDM instance ID.
                    type: string
                  name:
                    description: |-
                      Specifies the DDM schema name. The DDM instance name of the same
                      type is unique in the same tenant. It can be  2 to 48 characters long. It must start with a letter and it can only contain etters, digits, and underscores (_).
                    type: string
                  purgeRdsOnDelete:
                    description: 'Specifies whether data stored on the associated
                      DB instances is deleted. The value can be: true or false (default)'
                    type: boolean
                  rds:
                    description: Specifies the rds instance information. The structure
                      is described below.
                    items:
                      properties:
                        id:
                          description: Specifies the ID of the rds instance.
                          type: string
                      type: object
                    type: array
                  region:
                    description: The region of the DDM instance.
                    type: string
                  shardMode:
                    description: Specifies the sharding mode of the schema. The values
                      for this can be cluster or single. Cluster indicates that the
                      schema is in sharded mode. Single indicates that the schema
                      is in unsharded mode.
                    type: string
                  shardNumber:
                    description: Specifies the number of shards in the same working
                      mode. If shard_unit is not empty, the value is the product of
                      shard_unit multiplied by the associated RDS instances. If shard_unit
                      is left blank, the value must be greater than the number of
                      associated RDS instances and less than or equal to the product
                      of the associated RDS instances multiplied by 64.
                    type: number
                  shardUnit:
                    description: Specifies the Number of shards per RDS instance.
                      The value is 1 if the schema is unsharded. The value ranges
                      from 1 to 64 if the schema is sharded.
                    type: number
                  status:
                    description: (String) Indicates the DDM schema status.
                    type: string
                  updatedAt:
                    description: (uint64) Indicates the update time.
                    type: number
                  usedRds:
                    description: (List) Indicates the associated RDS instances. The
                      structure is described below.
                    items:
                      properties:
                        id:
                          description: (String) Indicates the RDS ID.
                          type: string
                        name:
                          description: Indicates the RDS name.
                          type: string
                        status:
                          description: Indicates the RDS status.
                          type: string
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
