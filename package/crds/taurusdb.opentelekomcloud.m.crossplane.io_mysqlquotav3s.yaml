---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: mysqlquotav3s.taurusdb.opentelekomcloud.m.crossplane.io
spec:
  group: taurusdb.opentelekomcloud.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - opentelekomcloud
    kind: MySQLQuotaV3
    listKind: MySQLQuotaV3List
    plural: mysqlquotav3s
    singular: mysqlquotav3
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MySQLQuotaV3 is the Schema for the MySQLQuotaV3s API. Manages
          a TaurusDB MySQL quota resource within OpenTelekomCloud.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MySQLQuotaV3Spec defines the desired state of MySQLQuotaV3
            properties:
              forProvider:
                properties:
                  enterpriseProjectId:
                    description: |-
                      Specifies the enterprise project ID.
                      Changing this parameter will create a new resource.
                    type: string
                  enterpriseProjectName:
                    description: Indicates the enterprise project name.
                    type: string
                  instanceQuota:
                    description: |-
                      Specifies the instance quantity quota. Value range: -1 to 100000. The value
                      -1 indicates no limit. If there are already instances created, this parameter value must be greater than the number
                      of existing instances. Defaults to -1.
                    type: number
                  ramQuota:
                    description: |-
                      Specifies the memory quota in GB. Value range: -1 to 2147483646. The value -1
                      indicates no limit. If there are already instances created, this parameter value must be greater than the used memory
                      size. Defaults to -1.
                    type: number
                  vcpusQuota:
                    description: |-
                      Specifies the vCPU quota. Value range: -1 to 2147483646. The value -1
                      indicates no limit. If there are already instances created, this parameter value must be greater than the number of
                      used vCPUs. Defaults to -1.
                    type: number
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  enterpriseProjectId:
                    description: |-
                      Specifies the enterprise project ID.
                      Changing this parameter will create a new resource.
                    type: string
                  enterpriseProjectName:
                    description: Indicates the enterprise project name.
                    type: string
                  instanceQuota:
                    description: |-
                      Specifies the instance quantity quota. Value range: -1 to 100000. The value
                      -1 indicates no limit. If there are already instances created, this parameter value must be greater than the number
                      of existing instances. Defaults to -1.
                    type: number
                  ramQuota:
                    description: |-
                      Specifies the memory quota in GB. Value range: -1 to 2147483646. The value -1
                      indicates no limit. If there are already instances created, this parameter value must be greater than the used memory
                      size. Defaults to -1.
                    type: number
                  vcpusQuota:
                    description: |-
                      Specifies the vCPU quota. Value range: -1 to 2147483646. The value -1
                      indicates no limit. If there are already instances created, this parameter value must be greater than the number of
                      used vCPUs. Defaults to -1.
                    type: number
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.enterpriseProjectId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.enterpriseProjectId)
                || (has(self.initProvider) && has(self.initProvider.enterpriseProjectId))'
            - message: spec.forProvider.enterpriseProjectName is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.enterpriseProjectName)
                || (has(self.initProvider) && has(self.initProvider.enterpriseProjectName))'
          status:
            description: MySQLQuotaV3Status defines the observed state of MySQLQuotaV3.
            properties:
              atProvider:
                properties:
                  availabilityInstanceQuota:
                    description: Indicates the remaining instance quota.
                    type: number
                  availabilityRamQuota:
                    description: Indicates the remaining memory quota.
                    type: number
                  availabilityVcpusQuota:
                    description: Indicates the remaining vCPU quota.
                    type: number
                  enterpriseProjectId:
                    description: |-
                      Specifies the enterprise project ID.
                      Changing this parameter will create a new resource.
                    type: string
                  enterpriseProjectName:
                    description: Indicates the enterprise project name.
                    type: string
                  id:
                    description: Indicates the resource ID. The value is enterprise_project_id.
                    type: string
                  instanceQuota:
                    description: |-
                      Specifies the instance quantity quota. Value range: -1 to 100000. The value
                      -1 indicates no limit. If there are already instances created, this parameter value must be greater than the number
                      of existing instances. Defaults to -1.
                    type: number
                  ramQuota:
                    description: |-
                      Specifies the memory quota in GB. Value range: -1 to 2147483646. The value -1
                      indicates no limit. If there are already instances created, this parameter value must be greater than the used memory
                      size. Defaults to -1.
                    type: number
                  region:
                    description: Indicates the region in which to create the resource.
                    type: string
                  vcpusQuota:
                    description: |-
                      Specifies the vCPU quota. Value range: -1 to 2147483646. The value -1
                      indicates no limit. If there are already instances created, this parameter value must be greater than the number of
                      used vCPUs. Defaults to -1.
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
