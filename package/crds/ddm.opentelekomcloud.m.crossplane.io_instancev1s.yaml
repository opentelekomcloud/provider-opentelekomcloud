---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: instancev1s.ddm.opentelekomcloud.m.crossplane.io
spec:
  group: ddm.opentelekomcloud.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - opentelekomcloud
    kind: InstanceV1
    listKind: InstanceV1List
    plural: instancev1s
    singular: instancev1
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: InstanceV1 is the Schema for the InstanceV1s API. Manages a DDM
          Instance resource within OpenTelekomCloud.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: InstanceV1Spec defines the desired state of InstanceV1
            properties:
              forProvider:
                properties:
                  availabilityZones:
                    description: Specifies the list of availability zones.
                    items:
                      type: string
                    type: array
                  engineId:
                    description: Specifies the Engine ID of the instance.
                    type: string
                  flavorId:
                    description: Specifies the flavor ID of the instance nodes.
                    type: string
                  name:
                    description: |-
                      Specifies the DDM instance name. The DDM instance name of the same
                      type is unique in the same tenant. It can be  4 to 64 characters long. It must start with a letter and it can only contain etters, digits, and hyphens (-).
                    type: string
                  nodeNum:
                    description: Specifies the disk encryption ID of the instance.
                    type: number
                  paramGroupId:
                    description: Specifies the parameters group ID.
                    type: string
                  passwordSecretRef:
                    description: 'Specifies the Administrator password of the DDM
                      instance. it can be 8 to 32 characters long. It must be a combination
                      of uppercase letters, lowercase letters, digits, and the following
                      special characters: ~ ! @ # % ^ * - _ = + ?. It must be a strong
                      password to improve security and prevent security risks such
                      as brute force cracking.'
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  purgeRdsOnDelete:
                    description: 'Specifies whether data stored on the associated
                      DB instances is deleted. The value can be: true or false (default).'
                    type: boolean
                  securityGroupId:
                    description: Specifies the security group ID of the DDM instance.
                    type: string
                  subnetId:
                    description: Specifies the subnet Network ID.
                    type: string
                  timeZone:
                    description: Specifies the timezone. Valid formats are UTC+12:00,
                      UTC+11:00, ... ,UTC+01:00, UTC, UTC-01:00, ... , UTC-11:00,
                      UTC-12:00
                    type: string
                  usernameSecretRef:
                    description: Specifies the Administrator username of the DDM instance.
                      It can be 1 to 32 characters long and can contain letters, digits,
                      and underscores (_). It must start with a letter.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  vpcId:
                    description: Specifies the VPC ID.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  availabilityZones:
                    description: Specifies the list of availability zones.
                    items:
                      type: string
                    type: array
                  engineId:
                    description: Specifies the Engine ID of the instance.
                    type: string
                  flavorId:
                    description: Specifies the flavor ID of the instance nodes.
                    type: string
                  name:
                    description: |-
                      Specifies the DDM instance name. The DDM instance name of the same
                      type is unique in the same tenant. It can be  4 to 64 characters long. It must start with a letter and it can only contain etters, digits, and hyphens (-).
                    type: string
                  nodeNum:
                    description: Specifies the disk encryption ID of the instance.
                    type: number
                  paramGroupId:
                    description: Specifies the parameters group ID.
                    type: string
                  passwordSecretRef:
                    description: 'Specifies the Administrator password of the DDM
                      instance. it can be 8 to 32 characters long. It must be a combination
                      of uppercase letters, lowercase letters, digits, and the following
                      special characters: ~ ! @ # % ^ * - _ = + ?. It must be a strong
                      password to improve security and prevent security risks such
                      as brute force cracking.'
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  purgeRdsOnDelete:
                    description: 'Specifies whether data stored on the associated
                      DB instances is deleted. The value can be: true or false (default).'
                    type: boolean
                  securityGroupId:
                    description: Specifies the security group ID of the DDM instance.
                    type: string
                  subnetId:
                    description: Specifies the subnet Network ID.
                    type: string
                  timeZone:
                    description: Specifies the timezone. Valid formats are UTC+12:00,
                      UTC+11:00, ... ,UTC+01:00, UTC, UTC-01:00, ... , UTC-11:00,
                      UTC-12:00
                    type: string
                  usernameSecretRef:
                    description: Specifies the Administrator username of the DDM instance.
                      It can be 1 to 32 characters long and can contain letters, digits,
                      and underscores (_). It must start with a letter.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  vpcId:
                    description: Specifies the VPC ID.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.availabilityZones is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.availabilityZones)
                || (has(self.initProvider) && has(self.initProvider.availabilityZones))'
            - message: spec.forProvider.engineId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.engineId)
                || (has(self.initProvider) && has(self.initProvider.engineId))'
            - message: spec.forProvider.flavorId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.flavorId)
                || (has(self.initProvider) && has(self.initProvider.flavorId))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.nodeNum is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.nodeNum)
                || (has(self.initProvider) && has(self.initProvider.nodeNum))'
            - message: spec.forProvider.securityGroupId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.securityGroupId)
                || (has(self.initProvider) && has(self.initProvider.securityGroupId))'
            - message: spec.forProvider.subnetId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.subnetId)
                || (has(self.initProvider) && has(self.initProvider.subnetId))'
            - message: spec.forProvider.vpcId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.vpcId)
                || (has(self.initProvider) && has(self.initProvider.vpcId))'
          status:
            description: InstanceV1Status defines the observed state of InstanceV1.
            properties:
              atProvider:
                properties:
                  accessIp:
                    type: string
                  accessPort:
                    description: Indicates the DDM access port.
                    type: string
                  availabilityZone:
                    description: Indicates the availability zone of DDM instance.
                    type: string
                  availabilityZones:
                    description: Specifies the list of availability zones.
                    items:
                      type: string
                    type: array
                  createdAt:
                    description: Indicates the creation time.
                    type: string
                  engineId:
                    description: Specifies the Engine ID of the instance.
                    type: string
                  flavorId:
                    description: Specifies the flavor ID of the instance nodes.
                    type: string
                  id:
                    type: string
                  name:
                    description: |-
                      Specifies the DDM instance name. The DDM instance name of the same
                      type is unique in the same tenant. It can be  4 to 64 characters long. It must start with a letter and it can only contain etters, digits, and hyphens (-).
                    type: string
                  nodeNum:
                    description: Specifies the disk encryption ID of the instance.
                    type: number
                  nodeStatus:
                    description: Indicates the DDM nodes status.
                    type: string
                  nodes:
                    description: Indicates the instance nodes information. Structure
                      is documented below.
                    items:
                      properties:
                        ip:
                          description: Indicates the node IP.
                          type: string
                        port:
                          description: Indicates the node port.
                          type: string
                        status:
                          description: Indicates the node status.
                          type: string
                      type: object
                    type: array
                  paramGroupId:
                    description: Specifies the parameters group ID.
                    type: string
                  purgeRdsOnDelete:
                    description: 'Specifies whether data stored on the associated
                      DB instances is deleted. The value can be: true or false (default).'
                    type: boolean
                  region:
                    description: The region of the DDM instance.
                    type: string
                  securityGroupId:
                    description: Specifies the security group ID of the DDM instance.
                    type: string
                  status:
                    description: Indicates the DDM instance status.
                    type: string
                  subnetId:
                    description: Specifies the subnet Network ID.
                    type: string
                  timeZone:
                    description: Specifies the timezone. Valid formats are UTC+12:00,
                      UTC+11:00, ... ,UTC+01:00, UTC, UTC-01:00, ... , UTC-11:00,
                      UTC-12:00
                    type: string
                  updatedAt:
                    description: Indicates the update time.
                    type: string
                  vpcId:
                    description: Specifies the VPC ID.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
