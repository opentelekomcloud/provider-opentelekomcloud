---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: dependencyversionv2s.fgs.opentelekomcloud.m.crossplane.io
spec:
  group: fgs.opentelekomcloud.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - opentelekomcloud
    kind: DependencyVersionV2
    listKind: DependencyVersionV2List
    plural: dependencyversionv2s
    singular: dependencyversionv2
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DependencyVersionV2 is the Schema for the DependencyVersionV2s
          API. Manages a custom dependency version within OpenTelekomCloud.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DependencyVersionV2Spec defines the desired state of DependencyVersionV2
            properties:
              forProvider:
                properties:
                  description:
                    description: |-
                      Specifies the description of the custom dependency version.
                      The description can contain a maximum of 512 characters.
                      Changing this will create a new resource.
                    type: string
                  file:
                    description: |-
                      Specifies the file contents in the file stream format and must be a ZIP file encoded using Base64.
                      The size of the ZIP file cannot exceed 40 MB. For a larger file, upload it through OBS.
                      Either link or file must be specified.
                    type: string
                  link:
                    description: |-
                      Specifies the OBS bucket path where the dependency package is located.
                      The OBS object URL must be in ZIP format, such as
                      https://test-bucket.obs.eu-de.otc.t-systems.com/index.zip.
                      Either link or file must be specified.
                      Changing this will create a new resource.
                    type: string
                  name:
                    description: |-
                      Specifies the name of the custom dependency package to which the version
                      belongs.
                      The name can contain a maximum of 96 characters and must start with a letter and end with a letter or digit.
                      Only letters, digits, underscores (_), periods (.), and hyphens (-) are allowed.
                      Changing this will create a new resource.
                    type: string
                  runtime:
                    description: |-
                      Specifies the runtime of the custom dependency version.
                      The valid values are as follows:
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  description:
                    description: |-
                      Specifies the description of the custom dependency version.
                      The description can contain a maximum of 512 characters.
                      Changing this will create a new resource.
                    type: string
                  file:
                    description: |-
                      Specifies the file contents in the file stream format and must be a ZIP file encoded using Base64.
                      The size of the ZIP file cannot exceed 40 MB. For a larger file, upload it through OBS.
                      Either link or file must be specified.
                    type: string
                  link:
                    description: |-
                      Specifies the OBS bucket path where the dependency package is located.
                      The OBS object URL must be in ZIP format, such as
                      https://test-bucket.obs.eu-de.otc.t-systems.com/index.zip.
                      Either link or file must be specified.
                      Changing this will create a new resource.
                    type: string
                  name:
                    description: |-
                      Specifies the name of the custom dependency package to which the version
                      belongs.
                      The name can contain a maximum of 96 characters and must start with a letter and end with a letter or digit.
                      Only letters, digits, underscores (_), periods (.), and hyphens (-) are allowed.
                      Changing this will create a new resource.
                    type: string
                  runtime:
                    description: |-
                      Specifies the runtime of the custom dependency version.
                      The valid values are as follows:
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.runtime is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.runtime)
                || (has(self.initProvider) && has(self.initProvider.runtime))'
          status:
            description: DependencyVersionV2Status defines the observed state of DependencyVersionV2.
            properties:
              atProvider:
                properties:
                  dependencyId:
                    description: The ID of the dependency package corresponding to
                      the version.
                    type: string
                  description:
                    description: |-
                      Specifies the description of the custom dependency version.
                      The description can contain a maximum of 512 characters.
                      Changing this will create a new resource.
                    type: string
                  downloadLink:
                    description: The temporary download link of a dependency file.
                    type: string
                  etag:
                    description: The unique ID of the dependency.
                    type: string
                  file:
                    description: |-
                      Specifies the file contents in the file stream format and must be a ZIP file encoded using Base64.
                      The size of the ZIP file cannot exceed 40 MB. For a larger file, upload it through OBS.
                      Either link or file must be specified.
                    type: string
                  fileName:
                    description: The dependency file name.
                    type: string
                  id:
                    description: |-
                      The resource ID, consists of dependency ID and version number, separated by a slash (/).
                      The format is <name>/<version>.
                    type: string
                  link:
                    description: |-
                      Specifies the OBS bucket path where the dependency package is located.
                      The OBS object URL must be in ZIP format, such as
                      https://test-bucket.obs.eu-de.otc.t-systems.com/index.zip.
                      Either link or file must be specified.
                      Changing this will create a new resource.
                    type: string
                  name:
                    description: |-
                      Specifies the name of the custom dependency package to which the version
                      belongs.
                      The name can contain a maximum of 96 characters and must start with a letter and end with a letter or digit.
                      Only letters, digits, underscores (_), periods (.), and hyphens (-) are allowed.
                      Changing this will create a new resource.
                    type: string
                  owner:
                    description: The dependency owner, public indicates a public dependency.
                    type: string
                  runtime:
                    description: |-
                      Specifies the runtime of the custom dependency version.
                      The valid values are as follows:
                    type: string
                  size:
                    description: The dependency size, in bytes.
                    type: number
                  version:
                    description: The dependency package version.
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
