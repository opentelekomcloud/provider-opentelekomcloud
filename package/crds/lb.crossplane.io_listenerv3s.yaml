---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: listenerv3s.lb.crossplane.io
spec:
  group: lb.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - opentelekomcloud
    kind: ListenerV3
    listKind: ListenerV3List
    plural: listenerv3s
    singular: listenerv3
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ListenerV3 is the Schema for the ListenerV3s API. Manages a LB
          Listener resource within OpenTelekomCloud.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ListenerV3Spec defines the desired state of ListenerV3
            properties:
              deletionPolicy:
                default: Delete
                description: 'DeletionPolicy specifies what will happen to the underlying
                  external when this managed resource is deleted - either "Delete"
                  or "Orphan" the external resource. This field is planned to be deprecated
                  in favor of the ManagementPolicies field in a future release. Currently,
                  both could be set independently and non-default values would be
                  honored if the feature flag is enabled. See the design doc for more
                  information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223'
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  adminStateUp:
                    type: boolean
                  advancedForwarding:
                    description: Specifies whether to enable advanced forwarding.
                      If advanced forwarding is enabled, more flexible forwarding
                      policies and rules are supported. The value can be true (enable
                      advanced forwarding) or false (disable advanced forwarding),
                      and the default value is false. Changing this creates a new
                      Listener.
                    type: boolean
                  clientCaTlsContainerRef:
                    description: Specifies the ID of the CA certificate used by the
                      listener.
                    type: string
                  clientTimeout:
                    description: Specifies the timeout duration for waiting for a
                      request from a client, in seconds. This parameter is available
                      only for HTTP and HTTPS listeners. The value ranges from 1 to
                      300, and the default value is 60. An error will be returned
                      if you configure this parameter for TCP and UDP listeners.
                    type: number
                  defaultPoolId:
                    description: Specifies the ID of the default backend server group.
                      If there is no matched forwarding policy, requests are forwarded
                      to the default backend server for processing.
                    type: string
                  defaultTlsContainerRef:
                    description: Specifies the ID of the server certificate used by
                      the listener.
                    type: string
                  description:
                    description: Provides supplementary information about the listener.
                    type: string
                  http2Enable:
                    description: Specifies whether to use HTTP/2. This parameter is
                      available only for HTTPS listeners. If you configure this parameter
                      for other types of listeners, it will not take effect. Enable
                      HTTP/2 if you want the clients to use HTTP/2 to communicate
                      with the load balancer. However, connections between the load
                      balancer and backend servers use HTTP/1.x by default.
                    type: boolean
                  insertHeaders:
                    description: Specifies the HTTP header fields.
                    items:
                      properties:
                        forwardElbIp:
                          description: Specifies whether to transparently transmit
                            the load balancer EIP to backend servers. If forward_elb_ip
                            is set to true, the load balancer EIP will be stored in
                            the HTTP header and passed to backend servers.
                          type: boolean
                        forwardedForPort:
                          description: Specifies whether to transparently transmit
                            the source port of the client to backend servers. If forwarded_for_port
                            is set to true, the source port of the client will be
                            stored in the HTTP header and passed to backend servers.
                          type: boolean
                        forwardedHost:
                          description: Specifies whether to rewrite the X-Forwarded-Host
                            header. If forwarded_host is set to true, X-Forwarded-Host
                            in the request header from the clients can be set to Host
                            in the request header sent from the load balancer to backend
                            servers.
                          type: boolean
                        forwardedPort:
                          description: Specifies whether to transparently transmit
                            the listening port of the load balancer to backend servers.
                            If forwarded_port is set to true, the listening port of
                            the load balancer will be stored in the HTTP header and
                            passed to backend servers.
                          type: boolean
                      type: object
                    type: array
                  ipGroup:
                    description: Specifies the IP address group associated with the
                      listener.
                    items:
                      properties:
                        enable:
                          description: 'Specifies whether to enable access control.
                            true: Access control will be enabled. false (default):
                            Access control will be disabled.'
                          type: boolean
                        id:
                          description: Specifies the ID of the IP address group associated
                            with the listener. Specifies the ID of the IP address
                            group associated with the listener. If ip_list in opentelekomcloud_lb_ipgroup_v3
                            is set to an empty array [] and type to whitelist, no
                            IP addresses are allowed to access the listener. If ip_list
                            in opentelekomcloud_lb_ipgroup_v3 is set to an empty array
                            [] and type to blacklist, any IP address is allowed to
                            access the listener.
                          type: string
                        type:
                          description: 'Specifies how access to the listener is controlled.
                            white (default): A whitelist will be configured. Only
                            IP addresses in the whitelist can access the listener.
                            black: A blacklist will be configured. IP addresses in
                            the blacklist are not allowed to access the listener.'
                          type: string
                      type: object
                    type: array
                  keepAliveTimeout:
                    description: Specifies the idle timeout duration, in seconds.
                    type: number
                  loadbalancerId:
                    description: Specifies the ID of the load balancer that the listener
                      is added to.
                    type: string
                  memberRetryEnable:
                    description: Specifies whether to enable health check retries
                      for backend servers. This parameter is available only for HTTP
                      and HTTPS listeners. An error will be returned if you configure
                      this parameter for TCP and UDP listeners.
                    type: boolean
                  memberTimeout:
                    description: Specifies the timeout duration for waiting for a
                      request from a backend server, in seconds. This parameter is
                      available only for HTTP and HTTPS listeners. The value ranges
                      from 1 to 300, and the default value is 60. An error will be
                      returned if you configure this parameter for TCP and UDP listeners.
                    type: number
                  name:
                    description: Specifies the listener name.
                    type: string
                  protocol:
                    description: The protocol - can either be TCP, HTTP, HTTPS or
                      UDP. Changing this creates a new Listener.
                    type: string
                  protocolPort:
                    description: Specifies the port used by the listener. Changing
                      this creates a new Listener.
                    type: number
                  securityPolicyId:
                    description: Specifies the ID of the custom security policy.
                    type: string
                  sniContainerRefs:
                    description: Lists the IDs of SNI certificates (server certificates
                      with domain names) used by the listener. Each SNI certificate
                      can have up to 30 domain names, and each domain name in the
                      SNI certificate must be unique. This parameter will be ignored
                      and an empty array will be returned if the listener's protocol
                      is not HTTPS.
                    items:
                      type: string
                    type: array
                  sniMatchAlgo:
                    description: Specifies how wildcard domain name matches with the
                      SNI certificates used by the listener.
                    type: string
                  tags:
                    additionalProperties:
                      type: string
                    description: Tags key/value pairs to associate with the loadbalancer
                      listener.
                    type: object
                  tlsCiphersPolicy:
                    description: 'Specifies the security policy that will be used
                      by the listener. This parameter is available only for HTTPS
                      listeners. An error will be returned if the protocol of the
                      listener is not HTTPS. Possible values are: tls-1-0, tls-1-1,
                      tls-1-0-inherit, tls-1-2, tls-1-2-strict, tls-1-2-fs, tls-1-0-with-1-3,
                      tls-1-2-fs-with-1-3, hybrid-policy-1-0, tls-1-2-strict-no-cbc.'
                    type: string
                type: object
              initProvider:
                description: THIS IS A BETA FIELD. It will be honored unless the Management
                  Policies feature flag is disabled. InitProvider holds the same fields
                  as ForProvider, with the exception of Identifier and other resource
                  reference fields. The fields that are in InitProvider are merged
                  into ForProvider when the resource is created. The same fields are
                  also added to the terraform ignore_changes hook, to avoid updating
                  them after creation. This is useful for fields that are required
                  on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them,
                  like an autoscaler.
                properties:
                  adminStateUp:
                    type: boolean
                  advancedForwarding:
                    description: Specifies whether to enable advanced forwarding.
                      If advanced forwarding is enabled, more flexible forwarding
                      policies and rules are supported. The value can be true (enable
                      advanced forwarding) or false (disable advanced forwarding),
                      and the default value is false. Changing this creates a new
                      Listener.
                    type: boolean
                  clientCaTlsContainerRef:
                    description: Specifies the ID of the CA certificate used by the
                      listener.
                    type: string
                  clientTimeout:
                    description: Specifies the timeout duration for waiting for a
                      request from a client, in seconds. This parameter is available
                      only for HTTP and HTTPS listeners. The value ranges from 1 to
                      300, and the default value is 60. An error will be returned
                      if you configure this parameter for TCP and UDP listeners.
                    type: number
                  defaultPoolId:
                    description: Specifies the ID of the default backend server group.
                      If there is no matched forwarding policy, requests are forwarded
                      to the default backend server for processing.
                    type: string
                  defaultTlsContainerRef:
                    description: Specifies the ID of the server certificate used by
                      the listener.
                    type: string
                  description:
                    description: Provides supplementary information about the listener.
                    type: string
                  http2Enable:
                    description: Specifies whether to use HTTP/2. This parameter is
                      available only for HTTPS listeners. If you configure this parameter
                      for other types of listeners, it will not take effect. Enable
                      HTTP/2 if you want the clients to use HTTP/2 to communicate
                      with the load balancer. However, connections between the load
                      balancer and backend servers use HTTP/1.x by default.
                    type: boolean
                  insertHeaders:
                    description: Specifies the HTTP header fields.
                    items:
                      properties:
                        forwardElbIp:
                          description: Specifies whether to transparently transmit
                            the load balancer EIP to backend servers. If forward_elb_ip
                            is set to true, the load balancer EIP will be stored in
                            the HTTP header and passed to backend servers.
                          type: boolean
                        forwardedForPort:
                          description: Specifies whether to transparently transmit
                            the source port of the client to backend servers. If forwarded_for_port
                            is set to true, the source port of the client will be
                            stored in the HTTP header and passed to backend servers.
                          type: boolean
                        forwardedHost:
                          description: Specifies whether to rewrite the X-Forwarded-Host
                            header. If forwarded_host is set to true, X-Forwarded-Host
                            in the request header from the clients can be set to Host
                            in the request header sent from the load balancer to backend
                            servers.
                          type: boolean
                        forwardedPort:
                          description: Specifies whether to transparently transmit
                            the listening port of the load balancer to backend servers.
                            If forwarded_port is set to true, the listening port of
                            the load balancer will be stored in the HTTP header and
                            passed to backend servers.
                          type: boolean
                      type: object
                    type: array
                  ipGroup:
                    description: Specifies the IP address group associated with the
                      listener.
                    items:
                      properties:
                        enable:
                          description: 'Specifies whether to enable access control.
                            true: Access control will be enabled. false (default):
                            Access control will be disabled.'
                          type: boolean
                        id:
                          description: Specifies the ID of the IP address group associated
                            with the listener. Specifies the ID of the IP address
                            group associated with the listener. If ip_list in opentelekomcloud_lb_ipgroup_v3
                            is set to an empty array [] and type to whitelist, no
                            IP addresses are allowed to access the listener. If ip_list
                            in opentelekomcloud_lb_ipgroup_v3 is set to an empty array
                            [] and type to blacklist, any IP address is allowed to
                            access the listener.
                          type: string
                        type:
                          description: 'Specifies how access to the listener is controlled.
                            white (default): A whitelist will be configured. Only
                            IP addresses in the whitelist can access the listener.
                            black: A blacklist will be configured. IP addresses in
                            the blacklist are not allowed to access the listener.'
                          type: string
                      type: object
                    type: array
                  keepAliveTimeout:
                    description: Specifies the idle timeout duration, in seconds.
                    type: number
                  loadbalancerId:
                    description: Specifies the ID of the load balancer that the listener
                      is added to.
                    type: string
                  memberRetryEnable:
                    description: Specifies whether to enable health check retries
                      for backend servers. This parameter is available only for HTTP
                      and HTTPS listeners. An error will be returned if you configure
                      this parameter for TCP and UDP listeners.
                    type: boolean
                  memberTimeout:
                    description: Specifies the timeout duration for waiting for a
                      request from a backend server, in seconds. This parameter is
                      available only for HTTP and HTTPS listeners. The value ranges
                      from 1 to 300, and the default value is 60. An error will be
                      returned if you configure this parameter for TCP and UDP listeners.
                    type: number
                  name:
                    description: Specifies the listener name.
                    type: string
                  protocol:
                    description: The protocol - can either be TCP, HTTP, HTTPS or
                      UDP. Changing this creates a new Listener.
                    type: string
                  protocolPort:
                    description: Specifies the port used by the listener. Changing
                      this creates a new Listener.
                    type: number
                  securityPolicyId:
                    description: Specifies the ID of the custom security policy.
                    type: string
                  sniContainerRefs:
                    description: Lists the IDs of SNI certificates (server certificates
                      with domain names) used by the listener. Each SNI certificate
                      can have up to 30 domain names, and each domain name in the
                      SNI certificate must be unique. This parameter will be ignored
                      and an empty array will be returned if the listener's protocol
                      is not HTTPS.
                    items:
                      type: string
                    type: array
                  sniMatchAlgo:
                    description: Specifies how wildcard domain name matches with the
                      SNI certificates used by the listener.
                    type: string
                  tags:
                    additionalProperties:
                      type: string
                    description: Tags key/value pairs to associate with the loadbalancer
                      listener.
                    type: object
                  tlsCiphersPolicy:
                    description: 'Specifies the security policy that will be used
                      by the listener. This parameter is available only for HTTPS
                      listeners. An error will be returned if the protocol of the
                      listener is not HTTPS. Possible values are: tls-1-0, tls-1-1,
                      tls-1-0-inherit, tls-1-2, tls-1-2-strict, tls-1-2-fs, tls-1-0-with-1-3,
                      tls-1-2-fs-with-1-3, hybrid-policy-1-0, tls-1-2-strict-no-cbc.'
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: 'THIS IS A BETA FIELD. It is on by default but can be
                  opted out through a Crossplane feature flag. ManagementPolicies
                  specify the array of actions Crossplane is allowed to take on the
                  managed and external resources. This field is planned to replace
                  the DeletionPolicy field in a future release. Currently, both could
                  be set independently and non-default values would be honored if
                  the feature flag is enabled. If both are custom, the DeletionPolicy
                  field will be ignored. See the design doc for more information:
                  https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md'
                items:
                  description: A ManagementAction represents an action that the Crossplane
                    controllers can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: ProviderConfigReference specifies how the provider that
                  will be used to create, observe, update, and delete this managed
                  resource should be configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: Resolution specifies whether resolution of this
                          reference is required. The default is 'Required', which
                          means the reconcile will fail if the reference cannot be
                          resolved. 'Optional' means this reference will be a no-op
                          if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: Resolve specifies when this reference should
                          be resolved. The default is 'IfNotPresent', which will attempt
                          to resolve the reference only when the corresponding field
                          is not present. Use 'Always' to resolve the reference on
                          every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              publishConnectionDetailsTo:
                description: PublishConnectionDetailsTo specifies the connection secret
                  config which contains a name, metadata and a reference to secret
                  store config to which any connection details for this managed resource
                  should be written. Connection details frequently include the endpoint,
                  username, and password required to connect to the managed resource.
                properties:
                  configRef:
                    default:
                      name: default
                    description: SecretStoreConfigRef specifies which secret store
                      config should be used for this ConnectionSecret.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: Resolution specifies whether resolution of
                              this reference is required. The default is 'Required',
                              which means the reconcile will fail if the reference
                              cannot be resolved. 'Optional' means this reference
                              will be a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: Resolve specifies when this reference should
                              be resolved. The default is 'IfNotPresent', which will
                              attempt to resolve the reference only when the corresponding
                              field is not present. Use 'Always' to resolve the reference
                              on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  metadata:
                    description: Metadata is the metadata for connection secret.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Annotations are the annotations to be added to
                          connection secret. - For Kubernetes secrets, this will be
                          used as "metadata.annotations". - It is up to Secret Store
                          implementation for others store types.
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: Labels are the labels/tags to be added to connection
                          secret. - For Kubernetes secrets, this will be used as "metadata.labels".
                          - It is up to Secret Store implementation for others store
                          types.
                        type: object
                      type:
                        description: Type is the SecretType for the connection secret.
                          - Only valid for Kubernetes Secret Stores.
                        type: string
                    type: object
                  name:
                    description: Name is the name of the connection secret.
                    type: string
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: WriteConnectionSecretToReference specifies the namespace
                  and name of a Secret to which any connection details for this managed
                  resource should be written. Connection details frequently include
                  the endpoint, username, and password required to connect to the
                  managed resource. This field is planned to be replaced in a future
                  release in favor of PublishConnectionDetailsTo. Currently, both
                  could be set independently and connection details would be published
                  to both without affecting each other.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.loadbalancerId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.loadbalancerId)
                || (has(self.initProvider) && has(self.initProvider.loadbalancerId))'
            - message: spec.forProvider.protocol is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.protocol)
                || (has(self.initProvider) && has(self.initProvider.protocol))'
            - message: spec.forProvider.protocolPort is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.protocolPort)
                || (has(self.initProvider) && has(self.initProvider.protocolPort))'
          status:
            description: ListenerV3Status defines the observed state of ListenerV3.
            properties:
              atProvider:
                properties:
                  adminStateUp:
                    type: boolean
                  advancedForwarding:
                    description: Specifies whether to enable advanced forwarding.
                      If advanced forwarding is enabled, more flexible forwarding
                      policies and rules are supported. The value can be true (enable
                      advanced forwarding) or false (disable advanced forwarding),
                      and the default value is false. Changing this creates a new
                      Listener.
                    type: boolean
                  clientCaTlsContainerRef:
                    description: Specifies the ID of the CA certificate used by the
                      listener.
                    type: string
                  clientTimeout:
                    description: Specifies the timeout duration for waiting for a
                      request from a client, in seconds. This parameter is available
                      only for HTTP and HTTPS listeners. The value ranges from 1 to
                      300, and the default value is 60. An error will be returned
                      if you configure this parameter for TCP and UDP listeners.
                    type: number
                  createdAt:
                    description: Indicates the creation time.
                    type: string
                  defaultPoolId:
                    description: Specifies the ID of the default backend server group.
                      If there is no matched forwarding policy, requests are forwarded
                      to the default backend server for processing.
                    type: string
                  defaultTlsContainerRef:
                    description: Specifies the ID of the server certificate used by
                      the listener.
                    type: string
                  description:
                    description: Provides supplementary information about the listener.
                    type: string
                  http2Enable:
                    description: Specifies whether to use HTTP/2. This parameter is
                      available only for HTTPS listeners. If you configure this parameter
                      for other types of listeners, it will not take effect. Enable
                      HTTP/2 if you want the clients to use HTTP/2 to communicate
                      with the load balancer. However, connections between the load
                      balancer and backend servers use HTTP/1.x by default.
                    type: boolean
                  id:
                    description: Specifies the ID of the IP address group associated
                      with the listener. Specifies the ID of the IP address group
                      associated with the listener. If ip_list in opentelekomcloud_lb_ipgroup_v3
                      is set to an empty array [] and type to whitelist, no IP addresses
                      are allowed to access the listener. If ip_list in opentelekomcloud_lb_ipgroup_v3
                      is set to an empty array [] and type to blacklist, any IP address
                      is allowed to access the listener.
                    type: string
                  insertHeaders:
                    description: Specifies the HTTP header fields.
                    items:
                      properties:
                        forwardElbIp:
                          description: Specifies whether to transparently transmit
                            the load balancer EIP to backend servers. If forward_elb_ip
                            is set to true, the load balancer EIP will be stored in
                            the HTTP header and passed to backend servers.
                          type: boolean
                        forwardedForPort:
                          description: Specifies whether to transparently transmit
                            the source port of the client to backend servers. If forwarded_for_port
                            is set to true, the source port of the client will be
                            stored in the HTTP header and passed to backend servers.
                          type: boolean
                        forwardedHost:
                          description: Specifies whether to rewrite the X-Forwarded-Host
                            header. If forwarded_host is set to true, X-Forwarded-Host
                            in the request header from the clients can be set to Host
                            in the request header sent from the load balancer to backend
                            servers.
                          type: boolean
                        forwardedPort:
                          description: Specifies whether to transparently transmit
                            the listening port of the load balancer to backend servers.
                            If forwarded_port is set to true, the listening port of
                            the load balancer will be stored in the HTTP header and
                            passed to backend servers.
                          type: boolean
                      type: object
                    type: array
                  ipGroup:
                    description: Specifies the IP address group associated with the
                      listener.
                    items:
                      properties:
                        enable:
                          description: 'Specifies whether to enable access control.
                            true: Access control will be enabled. false (default):
                            Access control will be disabled.'
                          type: boolean
                        id:
                          description: Specifies the ID of the IP address group associated
                            with the listener. Specifies the ID of the IP address
                            group associated with the listener. If ip_list in opentelekomcloud_lb_ipgroup_v3
                            is set to an empty array [] and type to whitelist, no
                            IP addresses are allowed to access the listener. If ip_list
                            in opentelekomcloud_lb_ipgroup_v3 is set to an empty array
                            [] and type to blacklist, any IP address is allowed to
                            access the listener.
                          type: string
                        type:
                          description: 'Specifies how access to the listener is controlled.
                            white (default): A whitelist will be configured. Only
                            IP addresses in the whitelist can access the listener.
                            black: A blacklist will be configured. IP addresses in
                            the blacklist are not allowed to access the listener.'
                          type: string
                      type: object
                    type: array
                  keepAliveTimeout:
                    description: Specifies the idle timeout duration, in seconds.
                    type: number
                  loadbalancerId:
                    description: Specifies the ID of the load balancer that the listener
                      is added to.
                    type: string
                  memberRetryEnable:
                    description: Specifies whether to enable health check retries
                      for backend servers. This parameter is available only for HTTP
                      and HTTPS listeners. An error will be returned if you configure
                      this parameter for TCP and UDP listeners.
                    type: boolean
                  memberTimeout:
                    description: Specifies the timeout duration for waiting for a
                      request from a backend server, in seconds. This parameter is
                      available only for HTTP and HTTPS listeners. The value ranges
                      from 1 to 300, and the default value is 60. An error will be
                      returned if you configure this parameter for TCP and UDP listeners.
                    type: number
                  name:
                    description: Specifies the listener name.
                    type: string
                  protocol:
                    description: The protocol - can either be TCP, HTTP, HTTPS or
                      UDP. Changing this creates a new Listener.
                    type: string
                  protocolPort:
                    description: Specifies the port used by the listener. Changing
                      this creates a new Listener.
                    type: number
                  securityPolicyId:
                    description: Specifies the ID of the custom security policy.
                    type: string
                  sniContainerRefs:
                    description: Lists the IDs of SNI certificates (server certificates
                      with domain names) used by the listener. Each SNI certificate
                      can have up to 30 domain names, and each domain name in the
                      SNI certificate must be unique. This parameter will be ignored
                      and an empty array will be returned if the listener's protocol
                      is not HTTPS.
                    items:
                      type: string
                    type: array
                  sniMatchAlgo:
                    description: Specifies how wildcard domain name matches with the
                      SNI certificates used by the listener.
                    type: string
                  tags:
                    additionalProperties:
                      type: string
                    description: Tags key/value pairs to associate with the loadbalancer
                      listener.
                    type: object
                  tlsCiphersPolicy:
                    description: 'Specifies the security policy that will be used
                      by the listener. This parameter is available only for HTTPS
                      listeners. An error will be returned if the protocol of the
                      listener is not HTTPS. Possible values are: tls-1-0, tls-1-1,
                      tls-1-0-inherit, tls-1-2, tls-1-2-strict, tls-1-2-fs, tls-1-0-with-1-3,
                      tls-1-2-fs-with-1-3, hybrid-policy-1-0, tls-1-2-strict-no-cbc.'
                    type: string
                  updatedAt:
                    description: Indicates the update time.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time this condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: A Message containing details about this condition's
                        last transition from one status to another, if any.
                      type: string
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: Type of this condition. At most one of each condition
                        type may apply to a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
