---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: mysqlproxyv3s.taurusdb.opentelekomcloud.m.crossplane.io
spec:
  group: taurusdb.opentelekomcloud.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - opentelekomcloud
    kind: MySQLProxyV3
    listKind: MySQLProxyV3List
    plural: mysqlproxyv3s
    singular: mysqlproxyv3
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MySQLProxyV3 is the Schema for the MySQLProxyV3s API. Manages
          a TaurusDB MySQL proxy resource within OpenTelekomCloud.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MySQLProxyV3Spec defines the desired state of MySQLProxyV3
            properties:
              forProvider:
                properties:
                  flavor:
                    description: Specifies the flavor of the proxy.
                    type: string
                  instanceId:
                    description: |-
                      Specifies the ID of the TaurusDB MySQL instance. Changing this parameter
                      will create a new resource.
                    type: string
                  masterNodeWeight:
                    description: |-
                      Specifies the read weight of the master node.
                      The master_node_weight structure is documented below.
                    items:
                      properties:
                        id:
                          description: Specifies the ID of the node.
                          type: string
                        weight:
                          description: Specifies the weight assigned to the node.
                          type: number
                      type: object
                    type: array
                  nodeNum:
                    description: Specifies the node count of the proxy.
                    type: number
                  proxyMode:
                    description: |-
                      Specifies the type of the proxy. Changing this creates a new resource.
                      Value options:
                      readwrite: read and write.
                      readonly: read-only.
                    type: string
                  proxyName:
                    description: |-
                      Specifies the name of the proxy. The name consists of 4 to 64 characters and
                      starts with a letter. It is case-sensitive and can contain only letters, digits, hyphens (-), and underscores (_).
                    type: string
                  readonlyNodesWeight:
                    description: |-
                      Specifies the read weight of the read-only nodes.
                      The readonly_nodes_weight structure is documented below.
                    items:
                      properties:
                        id:
                          description: Specifies the ID of the node.
                          type: string
                        weight:
                          description: Specifies the weight assigned to the node.
                          type: number
                      type: object
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  flavor:
                    description: Specifies the flavor of the proxy.
                    type: string
                  instanceId:
                    description: |-
                      Specifies the ID of the TaurusDB MySQL instance. Changing this parameter
                      will create a new resource.
                    type: string
                  masterNodeWeight:
                    description: |-
                      Specifies the read weight of the master node.
                      The master_node_weight structure is documented below.
                    items:
                      properties:
                        id:
                          description: Specifies the ID of the node.
                          type: string
                        weight:
                          description: Specifies the weight assigned to the node.
                          type: number
                      type: object
                    type: array
                  nodeNum:
                    description: Specifies the node count of the proxy.
                    type: number
                  proxyMode:
                    description: |-
                      Specifies the type of the proxy. Changing this creates a new resource.
                      Value options:
                      readwrite: read and write.
                      readonly: read-only.
                    type: string
                  proxyName:
                    description: |-
                      Specifies the name of the proxy. The name consists of 4 to 64 characters and
                      starts with a letter. It is case-sensitive and can contain only letters, digits, hyphens (-), and underscores (_).
                    type: string
                  readonlyNodesWeight:
                    description: |-
                      Specifies the read weight of the read-only nodes.
                      The readonly_nodes_weight structure is documented below.
                    items:
                      properties:
                        id:
                          description: Specifies the ID of the node.
                          type: string
                        weight:
                          description: Specifies the weight assigned to the node.
                          type: number
                      type: object
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.flavor is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.flavor)
                || (has(self.initProvider) && has(self.initProvider.flavor))'
            - message: spec.forProvider.instanceId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.instanceId)
                || (has(self.initProvider) && has(self.initProvider.instanceId))'
            - message: spec.forProvider.nodeNum is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.nodeNum)
                || (has(self.initProvider) && has(self.initProvider.nodeNum))'
          status:
            description: MySQLProxyV3Status defines the observed state of MySQLProxyV3.
            properties:
              atProvider:
                properties:
                  address:
                    description: Indicates the address of the proxy.
                    type: string
                  flavor:
                    description: Specifies the flavor of the proxy.
                    type: string
                  id:
                    description: Indicates the resource ID.
                    type: string
                  instanceId:
                    description: |-
                      Specifies the ID of the TaurusDB MySQL instance. Changing this parameter
                      will create a new resource.
                    type: string
                  masterNodeWeight:
                    description: |-
                      Specifies the read weight of the master node.
                      The master_node_weight structure is documented below.
                    items:
                      properties:
                        id:
                          description: Specifies the ID of the node.
                          type: string
                        weight:
                          description: Specifies the weight assigned to the node.
                          type: number
                      type: object
                    type: array
                  nodeNum:
                    description: Specifies the node count of the proxy.
                    type: number
                  nodes:
                    description: Indicates the node information of the proxy.
                    items:
                      properties:
                        azCode:
                          description: Indicates the proxy node availability zone.
                          type: string
                        frozenFlag:
                          description: 'Indicates whether the proxy node is frozen.
                            The values can be:'
                          type: number
                        id:
                          description: Specifies the ID of the node.
                          type: string
                        name:
                          description: Indicates the proxy node name.
                          type: string
                        role:
                          description: 'Indicates the proxy node role. The values
                            can be:'
                          type: string
                        status:
                          description: Indicates the status of the proxy.
                          type: string
                      type: object
                    type: array
                  port:
                    description: Indicates the port of the proxy.
                    type: number
                  proxyMode:
                    description: |-
                      Specifies the type of the proxy. Changing this creates a new resource.
                      Value options:
                      readwrite: read and write.
                      readonly: read-only.
                    type: string
                  proxyName:
                    description: |-
                      Specifies the name of the proxy. The name consists of 4 to 64 characters and
                      starts with a letter. It is case-sensitive and can contain only letters, digits, hyphens (-), and underscores (_).
                    type: string
                  readonlyNodesWeight:
                    description: |-
                      Specifies the read weight of the read-only nodes.
                      The readonly_nodes_weight structure is documented below.
                    items:
                      properties:
                        id:
                          description: Specifies the ID of the node.
                          type: string
                        weight:
                          description: Specifies the weight assigned to the node.
                          type: number
                      type: object
                    type: array
                  region:
                    description: Indicates the region in which to create the resource.
                    type: string
                  status:
                    description: Indicates the status of the proxy.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
