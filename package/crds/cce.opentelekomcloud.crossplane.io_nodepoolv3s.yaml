---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: nodepoolv3s.cce.opentelekomcloud.crossplane.io
spec:
  group: cce.opentelekomcloud.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - opentelekomcloud
    kind: NodePoolV3
    listKind: NodePoolV3List
    plural: nodepoolv3s
    singular: nodepoolv3
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NodePoolV3 is the Schema for the NodePoolV3s API. Manages a CCE
          Cluster Node Pool resource within OpenTelekomCloud.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NodePoolV3Spec defines the desired state of NodePoolV3
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  agencyName:
                    description: IAM agency name. Changing this parameter will create
                      a new resource.
                    type: string
                  availabilityZone:
                    description: |-
                      Specify the name of the available partition (AZ). If zone is not
                      specified than node_pool will be in randomly selected AZ. The default value is random. Changing
                      this parameter will create a new resource.
                    type: string
                  clusterId:
                    description: ID of the cluster. Changing this parameter will create
                      a new resource.
                    type: string
                  dataVolumes:
                    description: Represents the data disk to be created. Changing
                      this parameter will create a new resource.
                    items:
                      properties:
                        extendParam:
                          description: |-
                            Disk expansion parameters.
                            Please use alternative parameter extend_params.
                          type: string
                        extendParams:
                          additionalProperties:
                            type: string
                          description: Disk expansion parameters. A list of strings
                            which describes additional disk parameters.
                          type: object
                          x-kubernetes-map-type: granular
                        kmsId:
                          description: The Encryption KMS ID of the system volume.
                            By default, it tries to get from env by OS_KMS_ID.
                          type: string
                        size:
                          description: Disk size in GB.
                          type: number
                        volumetype:
                          description: Disk type.
                          type: string
                      type: object
                    type: array
                  dockerBaseSize:
                    description: |-
                      Available disk space of a single Docker container on the node using the device mapper.
                      Changing this parameter will create a new node pool.
                    type: number
                  dockerLvmConfigOverride:
                    description: |-
                      ConfigMap of the Docker data disk.
                      Changing this parameter will create a new node.
                    type: string
                  flavor:
                    description: Specifies the flavor id. Changing this parameter
                      will create a new resource.
                    type: string
                  initialNodeCount:
                    description: Initial number of expected nodes in the node pool.
                    type: number
                  k8sTags:
                    additionalProperties:
                      type: string
                    description: Tags of a Kubernetes node, key/value pair format.
                    type: object
                    x-kubernetes-map-type: granular
                  keyPair:
                    description: |-
                      Key pair name when logging in to select the key pair mode.
                      This parameter and password are alternative. Changing this parameter will create a new resource.
                    type: string
                  maxNodeCount:
                    description: Maximum number of nodes allowed if auto scaling is
                      enabled.
                    type: number
                  maxPods:
                    description: |-
                      The maximum number of instances a node is allowed to create.
                      Changing this parameter will create a new node pool.
                    type: number
                  minNodeCount:
                    description: Minimum number of nodes allowed if auto scaling is
                      enabled.
                    type: number
                  name:
                    description: Node Pool Name.
                    type: string
                  os:
                    description: |-
                      Node OS. Changing this parameter will create a new resource.
                      Supported OS depends on kubernetes version of the cluster.
                    type: string
                  passwordSecretRef:
                    description: |-
                      Key pair name when logging in to select the key pair mode.
                      This parameter and password are alternative. Changing this parameter will create a new resource.
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  postinstall:
                    description: |-
                      Script required after installation. The input value can be a Base64 encoded string or not.
                      Changing this parameter will create a new resource.
                    type: string
                  preinstall:
                    description: |-
                      Script required before installation. The input value can be a Base64 encoded string or not.
                      Changing this parameter will create a new resource.
                    type: string
                  priority:
                    description: Weight of a node pool. A node pool with a higher
                      weight has a higher priority during scaling.
                    type: number
                  rootVolume:
                    description: It corresponds to the system disk related configuration.
                      Changing this parameter will create a new resource.
                    items:
                      properties:
                        extendParam:
                          description: |-
                            Disk expansion parameters.
                            Please use alternative parameter extend_params.
                          type: string
                        extendParams:
                          additionalProperties:
                            type: string
                          description: Disk expansion parameters. A list of strings
                            which describes additional disk parameters.
                          type: object
                          x-kubernetes-map-type: granular
                        kmsId:
                          description: The Encryption KMS ID of the system volume.
                            By default, it tries to get from env by OS_KMS_ID.
                          type: string
                        size:
                          description: Disk size in GB.
                          type: number
                        volumetype:
                          description: Disk type.
                          type: string
                      type: object
                    type: array
                  runtime:
                    description: |-
                      Container runtime. Changing this parameter will create a new resource.
                      Use with high-caution, may trigger resource recreation. Options are:
                      docker - Docker
                      containerd - Containerd
                    type: string
                  scaleDownCooldownTime:
                    description: Interval between two scaling operations, in minutes.
                    type: number
                  scaleEnable:
                    description: Whether to enable auto scaling. If Autoscaler is
                      enabled, install the autoscaler add-on to use the auto scaling
                      feature.
                    type: boolean
                  serverGroupReference:
                    description: ECS group ID. If this parameter is specified, all
                      nodes in the node pool will be created in this ECS group.
                    type: string
                  storage:
                    description: |-
                      Specifies the json string vary depending on CCE node pools storage options.
                      -> Please refer to the documentation
                      for actual fields.
                    type: string
                  subnetId:
                    description: The ID of the subnet to which the NIC belongs. Changing
                      this parameter will create a new resource.
                    type: string
                  taints:
                    description: Taints to created nodes to configure anti-affinity.
                    items:
                      properties:
                        effect:
                          description: Available options are NoSchedule, PreferNoSchedule,
                            and NoExecute.
                          type: string
                        key:
                          description: A key must contain 1 to 63 characters starting
                            with a letter or digit. Only letters, digits, hyphens
                            (-), underscores (_), and periods (.) are allowed. A DNS
                            subdomain name can be used as the prefix of a key.
                          type: string
                        value:
                          description: A value must start with a letter or digit and
                            can contain a maximum of 63 characters, including letters,
                            digits, hyphens (-), underscores (_), and periods (.).
                          type: string
                      type: object
                    type: array
                  userTags:
                    additionalProperties:
                      type: string
                    description: Tag of a VM, key/value pair format. Changing this
                      parameter will create a new resource.
                    type: object
                    x-kubernetes-map-type: granular
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  agencyName:
                    description: IAM agency name. Changing this parameter will create
                      a new resource.
                    type: string
                  availabilityZone:
                    description: |-
                      Specify the name of the available partition (AZ). If zone is not
                      specified than node_pool will be in randomly selected AZ. The default value is random. Changing
                      this parameter will create a new resource.
                    type: string
                  clusterId:
                    description: ID of the cluster. Changing this parameter will create
                      a new resource.
                    type: string
                  dataVolumes:
                    description: Represents the data disk to be created. Changing
                      this parameter will create a new resource.
                    items:
                      properties:
                        extendParam:
                          description: |-
                            Disk expansion parameters.
                            Please use alternative parameter extend_params.
                          type: string
                        extendParams:
                          additionalProperties:
                            type: string
                          description: Disk expansion parameters. A list of strings
                            which describes additional disk parameters.
                          type: object
                          x-kubernetes-map-type: granular
                        kmsId:
                          description: The Encryption KMS ID of the system volume.
                            By default, it tries to get from env by OS_KMS_ID.
                          type: string
                        size:
                          description: Disk size in GB.
                          type: number
                        volumetype:
                          description: Disk type.
                          type: string
                      type: object
                    type: array
                  dockerBaseSize:
                    description: |-
                      Available disk space of a single Docker container on the node using the device mapper.
                      Changing this parameter will create a new node pool.
                    type: number
                  dockerLvmConfigOverride:
                    description: |-
                      ConfigMap of the Docker data disk.
                      Changing this parameter will create a new node.
                    type: string
                  flavor:
                    description: Specifies the flavor id. Changing this parameter
                      will create a new resource.
                    type: string
                  initialNodeCount:
                    description: Initial number of expected nodes in the node pool.
                    type: number
                  k8sTags:
                    additionalProperties:
                      type: string
                    description: Tags of a Kubernetes node, key/value pair format.
                    type: object
                    x-kubernetes-map-type: granular
                  keyPair:
                    description: |-
                      Key pair name when logging in to select the key pair mode.
                      This parameter and password are alternative. Changing this parameter will create a new resource.
                    type: string
                  maxNodeCount:
                    description: Maximum number of nodes allowed if auto scaling is
                      enabled.
                    type: number
                  maxPods:
                    description: |-
                      The maximum number of instances a node is allowed to create.
                      Changing this parameter will create a new node pool.
                    type: number
                  minNodeCount:
                    description: Minimum number of nodes allowed if auto scaling is
                      enabled.
                    type: number
                  name:
                    description: Node Pool Name.
                    type: string
                  os:
                    description: |-
                      Node OS. Changing this parameter will create a new resource.
                      Supported OS depends on kubernetes version of the cluster.
                    type: string
                  passwordSecretRef:
                    description: |-
                      Key pair name when logging in to select the key pair mode.
                      This parameter and password are alternative. Changing this parameter will create a new resource.
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  postinstall:
                    description: |-
                      Script required after installation. The input value can be a Base64 encoded string or not.
                      Changing this parameter will create a new resource.
                    type: string
                  preinstall:
                    description: |-
                      Script required before installation. The input value can be a Base64 encoded string or not.
                      Changing this parameter will create a new resource.
                    type: string
                  priority:
                    description: Weight of a node pool. A node pool with a higher
                      weight has a higher priority during scaling.
                    type: number
                  rootVolume:
                    description: It corresponds to the system disk related configuration.
                      Changing this parameter will create a new resource.
                    items:
                      properties:
                        extendParam:
                          description: |-
                            Disk expansion parameters.
                            Please use alternative parameter extend_params.
                          type: string
                        extendParams:
                          additionalProperties:
                            type: string
                          description: Disk expansion parameters. A list of strings
                            which describes additional disk parameters.
                          type: object
                          x-kubernetes-map-type: granular
                        kmsId:
                          description: The Encryption KMS ID of the system volume.
                            By default, it tries to get from env by OS_KMS_ID.
                          type: string
                        size:
                          description: Disk size in GB.
                          type: number
                        volumetype:
                          description: Disk type.
                          type: string
                      type: object
                    type: array
                  runtime:
                    description: |-
                      Container runtime. Changing this parameter will create a new resource.
                      Use with high-caution, may trigger resource recreation. Options are:
                      docker - Docker
                      containerd - Containerd
                    type: string
                  scaleDownCooldownTime:
                    description: Interval between two scaling operations, in minutes.
                    type: number
                  scaleEnable:
                    description: Whether to enable auto scaling. If Autoscaler is
                      enabled, install the autoscaler add-on to use the auto scaling
                      feature.
                    type: boolean
                  serverGroupReference:
                    description: ECS group ID. If this parameter is specified, all
                      nodes in the node pool will be created in this ECS group.
                    type: string
                  storage:
                    description: |-
                      Specifies the json string vary depending on CCE node pools storage options.
                      -> Please refer to the documentation
                      for actual fields.
                    type: string
                  subnetId:
                    description: The ID of the subnet to which the NIC belongs. Changing
                      this parameter will create a new resource.
                    type: string
                  taints:
                    description: Taints to created nodes to configure anti-affinity.
                    items:
                      properties:
                        effect:
                          description: Available options are NoSchedule, PreferNoSchedule,
                            and NoExecute.
                          type: string
                        key:
                          description: A key must contain 1 to 63 characters starting
                            with a letter or digit. Only letters, digits, hyphens
                            (-), underscores (_), and periods (.) are allowed. A DNS
                            subdomain name can be used as the prefix of a key.
                          type: string
                        value:
                          description: A value must start with a letter or digit and
                            can contain a maximum of 63 characters, including letters,
                            digits, hyphens (-), underscores (_), and periods (.).
                          type: string
                      type: object
                    type: array
                  userTags:
                    additionalProperties:
                      type: string
                    description: Tag of a VM, key/value pair format. Changing this
                      parameter will create a new resource.
                    type: object
                    x-kubernetes-map-type: granular
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              publishConnectionDetailsTo:
                description: |-
                  PublishConnectionDetailsTo specifies the connection secret config which
                  contains a name, metadata and a reference to secret store config to
                  which any connection details for this managed resource should be written.
                  Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  configRef:
                    default:
                      name: default
                    description: |-
                      SecretStoreConfigRef specifies which secret store config should be used
                      for this ConnectionSecret.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  metadata:
                    description: Metadata is the metadata for connection secret.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          Annotations are the annotations to be added to connection secret.
                          - For Kubernetes secrets, this will be used as "metadata.annotations".
                          - It is up to Secret Store implementation for others store types.
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: |-
                          Labels are the labels/tags to be added to connection secret.
                          - For Kubernetes secrets, this will be used as "metadata.labels".
                          - It is up to Secret Store implementation for others store types.
                        type: object
                      type:
                        description: |-
                          Type is the SecretType for the connection secret.
                          - Only valid for Kubernetes Secret Stores.
                        type: string
                    type: object
                  name:
                    description: Name is the name of the connection secret.
                    type: string
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                  This field is planned to be replaced in a future release in favor of
                  PublishConnectionDetailsTo. Currently, both could be set independently
                  and connection details would be published to both without affecting
                  each other.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.clusterId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.clusterId)
                || (has(self.initProvider) && has(self.initProvider.clusterId))'
            - message: spec.forProvider.dataVolumes is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.dataVolumes)
                || (has(self.initProvider) && has(self.initProvider.dataVolumes))'
            - message: spec.forProvider.flavor is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.flavor)
                || (has(self.initProvider) && has(self.initProvider.flavor))'
            - message: spec.forProvider.initialNodeCount is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.initialNodeCount)
                || (has(self.initProvider) && has(self.initProvider.initialNodeCount))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.rootVolume is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.rootVolume)
                || (has(self.initProvider) && has(self.initProvider.rootVolume))'
          status:
            description: NodePoolV3Status defines the observed state of NodePoolV3.
            properties:
              atProvider:
                properties:
                  agencyName:
                    description: IAM agency name. Changing this parameter will create
                      a new resource.
                    type: string
                  availabilityZone:
                    description: |-
                      Specify the name of the available partition (AZ). If zone is not
                      specified than node_pool will be in randomly selected AZ. The default value is random. Changing
                      this parameter will create a new resource.
                    type: string
                  clusterId:
                    description: ID of the cluster. Changing this parameter will create
                      a new resource.
                    type: string
                  dataVolumes:
                    description: Represents the data disk to be created. Changing
                      this parameter will create a new resource.
                    items:
                      properties:
                        extendParam:
                          description: |-
                            Disk expansion parameters.
                            Please use alternative parameter extend_params.
                          type: string
                        extendParams:
                          additionalProperties:
                            type: string
                          description: Disk expansion parameters. A list of strings
                            which describes additional disk parameters.
                          type: object
                          x-kubernetes-map-type: granular
                        kmsId:
                          description: The Encryption KMS ID of the system volume.
                            By default, it tries to get from env by OS_KMS_ID.
                          type: string
                        size:
                          description: Disk size in GB.
                          type: number
                        volumetype:
                          description: Disk type.
                          type: string
                      type: object
                    type: array
                  dockerBaseSize:
                    description: |-
                      Available disk space of a single Docker container on the node using the device mapper.
                      Changing this parameter will create a new node pool.
                    type: number
                  dockerLvmConfigOverride:
                    description: |-
                      ConfigMap of the Docker data disk.
                      Changing this parameter will create a new node.
                    type: string
                  flavor:
                    description: Specifies the flavor id. Changing this parameter
                      will create a new resource.
                    type: string
                  id:
                    description: Specifies a resource ID in UUID format.
                    type: string
                  initialNodeCount:
                    description: Initial number of expected nodes in the node pool.
                    type: number
                  k8sTags:
                    additionalProperties:
                      type: string
                    description: Tags of a Kubernetes node, key/value pair format.
                    type: object
                    x-kubernetes-map-type: granular
                  keyPair:
                    description: |-
                      Key pair name when logging in to select the key pair mode.
                      This parameter and password are alternative. Changing this parameter will create a new resource.
                    type: string
                  maxNodeCount:
                    description: Maximum number of nodes allowed if auto scaling is
                      enabled.
                    type: number
                  maxPods:
                    description: |-
                      The maximum number of instances a node is allowed to create.
                      Changing this parameter will create a new node pool.
                    type: number
                  minNodeCount:
                    description: Minimum number of nodes allowed if auto scaling is
                      enabled.
                    type: number
                  name:
                    description: Node Pool Name.
                    type: string
                  os:
                    description: |-
                      Node OS. Changing this parameter will create a new resource.
                      Supported OS depends on kubernetes version of the cluster.
                    type: string
                  postinstall:
                    description: |-
                      Script required after installation. The input value can be a Base64 encoded string or not.
                      Changing this parameter will create a new resource.
                    type: string
                  preinstall:
                    description: |-
                      Script required before installation. The input value can be a Base64 encoded string or not.
                      Changing this parameter will create a new resource.
                    type: string
                  priority:
                    description: Weight of a node pool. A node pool with a higher
                      weight has a higher priority during scaling.
                    type: number
                  rootVolume:
                    description: It corresponds to the system disk related configuration.
                      Changing this parameter will create a new resource.
                    items:
                      properties:
                        extendParam:
                          description: |-
                            Disk expansion parameters.
                            Please use alternative parameter extend_params.
                          type: string
                        extendParams:
                          additionalProperties:
                            type: string
                          description: Disk expansion parameters. A list of strings
                            which describes additional disk parameters.
                          type: object
                          x-kubernetes-map-type: granular
                        kmsId:
                          description: The Encryption KMS ID of the system volume.
                            By default, it tries to get from env by OS_KMS_ID.
                          type: string
                        size:
                          description: Disk size in GB.
                          type: number
                        volumetype:
                          description: Disk type.
                          type: string
                      type: object
                    type: array
                  runtime:
                    description: |-
                      Container runtime. Changing this parameter will create a new resource.
                      Use with high-caution, may trigger resource recreation. Options are:
                      docker - Docker
                      containerd - Containerd
                    type: string
                  scaleDownCooldownTime:
                    description: Interval between two scaling operations, in minutes.
                    type: number
                  scaleEnable:
                    description: Whether to enable auto scaling. If Autoscaler is
                      enabled, install the autoscaler add-on to use the auto scaling
                      feature.
                    type: boolean
                  serverGroupReference:
                    description: ECS group ID. If this parameter is specified, all
                      nodes in the node pool will be created in this ECS group.
                    type: string
                  status:
                    description: Node status information.
                    type: string
                  storage:
                    description: |-
                      Specifies the json string vary depending on CCE node pools storage options.
                      -> Please refer to the documentation
                      for actual fields.
                    type: string
                  subnetId:
                    description: The ID of the subnet to which the NIC belongs. Changing
                      this parameter will create a new resource.
                    type: string
                  taints:
                    description: Taints to created nodes to configure anti-affinity.
                    items:
                      properties:
                        effect:
                          description: Available options are NoSchedule, PreferNoSchedule,
                            and NoExecute.
                          type: string
                        key:
                          description: A key must contain 1 to 63 characters starting
                            with a letter or digit. Only letters, digits, hyphens
                            (-), underscores (_), and periods (.) are allowed. A DNS
                            subdomain name can be used as the prefix of a key.
                          type: string
                        value:
                          description: A value must start with a letter or digit and
                            can contain a maximum of 63 characters, including letters,
                            digits, hyphens (-), underscores (_), and periods (.).
                          type: string
                      type: object
                    type: array
                  userTags:
                    additionalProperties:
                      type: string
                    description: Tag of a VM, key/value pair format. Changing this
                      parameter will create a new resource.
                    type: object
                    x-kubernetes-map-type: granular
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
