---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: gatewayv2s.apigw.opentelekomcloud.crossplane.io
spec:
  group: apigw.opentelekomcloud.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - opentelekomcloud
    kind: GatewayV2
    listKind: GatewayV2List
    plural: gatewayv2s
    singular: gatewayv2
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: GatewayV2 is the Schema for the GatewayV2s API. Manages a API
          Gateway resource within OpenTelekomCloud.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: GatewayV2Spec defines the desired state of GatewayV2
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  availabilityZones:
                    description: Specifies gateway description.
                    items:
                      type: string
                    type: array
                  bandwidthChargingMode:
                    description: |-
                      Billing type of the public outbound access bandwidth.
                      The valid values are as follows:
                    type: string
                  bandwidthSize:
                    description: |-
                      Specifies outbound access bandwidth. This parameter is required if public outbound
                      access is enabled for the gateway. After you configure the bandwidth for the gateway,
                      users can access resources on public networks.
                    type: number
                  description:
                    description: Specifies gateway description.
                    type: string
                  enterpriseProjectId:
                    description: |-
                      Specifies the enterprise project ID to which the dedicated
                      instance belongs. This parameter is required for enterprise users.
                    type: string
                  ingressBandwidthChargingMode:
                    description: |-
                      Specifies the ingress bandwidth billing type of the dedicated instance.
                      The valid values are as follows:
                    type: string
                  ingressBandwidthSize:
                    description: |-
                      Specifies public inbound access bandwidth. This parameter is required if public
                      inbound access is enabled for the gateway and loadbalancer_provider is set to elb.
                      After you bind an EIP to the gateway, users can access APIs in the gateway from public networks using the EIP.
                    type: number
                  loadbalancerProvider:
                    description: |-
                      Specifies type of the load balancer used by the gateway.
                      This resource provides the following timeouts configuration options:
                    type: string
                  maintainBegin:
                    description: |-
                      Specifies start time of the maintenance time window.
                      It must be in the format "xx:00:00". The value of xx can be 02, 06, 10, 14, 18, or 22.
                    type: string
                  name:
                    description: Specifies gateway name.
                    type: string
                  securityGroupId:
                    description: Specifies ID of the security group to which the gateway
                      belongs.
                    type: string
                  specId:
                    description: |-
                      Gateway edition. Options:
                      This resource provides the following timeouts configuration options:
                    type: string
                  subnetId:
                    description: Specifies network ID.
                    type: string
                  tags:
                    additionalProperties:
                      type: string
                    description: Specifies the key/value pairs to associate with the
                      dedicated instance.
                    type: object
                    x-kubernetes-map-type: granular
                  vpcId:
                    description: Specifies VPC ID.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  availabilityZones:
                    description: Specifies gateway description.
                    items:
                      type: string
                    type: array
                  bandwidthChargingMode:
                    description: |-
                      Billing type of the public outbound access bandwidth.
                      The valid values are as follows:
                    type: string
                  bandwidthSize:
                    description: |-
                      Specifies outbound access bandwidth. This parameter is required if public outbound
                      access is enabled for the gateway. After you configure the bandwidth for the gateway,
                      users can access resources on public networks.
                    type: number
                  description:
                    description: Specifies gateway description.
                    type: string
                  enterpriseProjectId:
                    description: |-
                      Specifies the enterprise project ID to which the dedicated
                      instance belongs. This parameter is required for enterprise users.
                    type: string
                  ingressBandwidthChargingMode:
                    description: |-
                      Specifies the ingress bandwidth billing type of the dedicated instance.
                      The valid values are as follows:
                    type: string
                  ingressBandwidthSize:
                    description: |-
                      Specifies public inbound access bandwidth. This parameter is required if public
                      inbound access is enabled for the gateway and loadbalancer_provider is set to elb.
                      After you bind an EIP to the gateway, users can access APIs in the gateway from public networks using the EIP.
                    type: number
                  loadbalancerProvider:
                    description: |-
                      Specifies type of the load balancer used by the gateway.
                      This resource provides the following timeouts configuration options:
                    type: string
                  maintainBegin:
                    description: |-
                      Specifies start time of the maintenance time window.
                      It must be in the format "xx:00:00". The value of xx can be 02, 06, 10, 14, 18, or 22.
                    type: string
                  name:
                    description: Specifies gateway name.
                    type: string
                  securityGroupId:
                    description: Specifies ID of the security group to which the gateway
                      belongs.
                    type: string
                  specId:
                    description: |-
                      Gateway edition. Options:
                      This resource provides the following timeouts configuration options:
                    type: string
                  subnetId:
                    description: Specifies network ID.
                    type: string
                  tags:
                    additionalProperties:
                      type: string
                    description: Specifies the key/value pairs to associate with the
                      dedicated instance.
                    type: object
                    x-kubernetes-map-type: granular
                  vpcId:
                    description: Specifies VPC ID.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.availabilityZones is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.availabilityZones)
                || (has(self.initProvider) && has(self.initProvider.availabilityZones))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.securityGroupId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.securityGroupId)
                || (has(self.initProvider) && has(self.initProvider.securityGroupId))'
            - message: spec.forProvider.specId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.specId)
                || (has(self.initProvider) && has(self.initProvider.specId))'
            - message: spec.forProvider.subnetId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.subnetId)
                || (has(self.initProvider) && has(self.initProvider.subnetId))'
            - message: spec.forProvider.vpcId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.vpcId)
                || (has(self.initProvider) && has(self.initProvider.vpcId))'
          status:
            description: GatewayV2Status defines the observed state of GatewayV2.
            properties:
              atProvider:
                properties:
                  availabilityZones:
                    description: Specifies gateway description.
                    items:
                      type: string
                    type: array
                  bandwidthChargingMode:
                    description: |-
                      Billing type of the public outbound access bandwidth.
                      The valid values are as follows:
                    type: string
                  bandwidthSize:
                    description: |-
                      Specifies outbound access bandwidth. This parameter is required if public outbound
                      access is enabled for the gateway. After you configure the bandwidth for the gateway,
                      users can access resources on public networks.
                    type: number
                  defaultGroupId:
                    description: The ID of DEFAULT group.
                    type: string
                  description:
                    description: Specifies gateway description.
                    type: string
                  enterpriseProjectId:
                    description: |-
                      Specifies the enterprise project ID to which the dedicated
                      instance belongs. This parameter is required for enterprise users.
                    type: string
                  id:
                    type: string
                  ingressBandwidthChargingMode:
                    description: |-
                      Specifies the ingress bandwidth billing type of the dedicated instance.
                      The valid values are as follows:
                    type: string
                  ingressBandwidthSize:
                    description: |-
                      Specifies public inbound access bandwidth. This parameter is required if public
                      inbound access is enabled for the gateway and loadbalancer_provider is set to elb.
                      After you bind an EIP to the gateway, users can access APIs in the gateway from public networks using the EIP.
                    type: number
                  loadbalancerProvider:
                    description: |-
                      Specifies type of the load balancer used by the gateway.
                      This resource provides the following timeouts configuration options:
                    type: string
                  maintainBegin:
                    description: |-
                      Specifies start time of the maintenance time window.
                      It must be in the format "xx:00:00". The value of xx can be 02, 06, 10, 14, 18, or 22.
                    type: string
                  maintainEnd:
                    description: |-
                      End time of the maintenance time window. It must be in the format "xx:00:00".
                      There is a 4-hour difference between the start time and end time.
                    type: string
                  name:
                    description: Specifies gateway name.
                    type: string
                  privateEgressAddresses:
                    description: List of private egress addresses.
                    items:
                      type: string
                    type: array
                  projectId:
                    description: Instance project id.
                    type: string
                  publicEgressAddress:
                    description: IP address for public outbound access.
                    type: string
                  region:
                    description: Instance region.
                    type: string
                  securityGroupId:
                    description: Specifies ID of the security group to which the gateway
                      belongs.
                    type: string
                  specId:
                    description: |-
                      Gateway edition. Options:
                      This resource provides the following timeouts configuration options:
                    type: string
                  status:
                    description: Instance status.
                    type: string
                  subnetId:
                    description: Specifies network ID.
                    type: string
                  supportedFeatures:
                    description: Supported features.
                    items:
                      type: string
                    type: array
                  tags:
                    additionalProperties:
                      type: string
                    description: Specifies the key/value pairs to associate with the
                      dedicated instance.
                    type: object
                    x-kubernetes-map-type: granular
                  vpcId:
                    description: Specifies VPC ID.
                    type: string
                  vpcIngressAddress:
                    description: VPC ingress address.
                    type: string
                  vpcepServiceName:
                    description: Name of a VPC endpoint service.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
